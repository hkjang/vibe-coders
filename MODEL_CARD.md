# Model card

## 시스템 식별

| 항목 | 내용 |
| --- | --- |
| 시스템명 | vibe-coders AI coding proxy gateway |
| 소스 기준 버전 | v0.76.48 이후 개발 상태 (`47e0a3e` 기준 분석) |
| 시스템 유형 | OpenAI 호환 LLM 프록시·라우터·관측/거버넌스 게이트웨이 |
| 모델 가중치 포함 여부 | 포함하지 않음 |
| 소스 위치 | <https://github.com/hkjang/vibe-coders> |
| 프로젝트 라이선스 | AGPL-3.0-only (`LICENSE`) |

## 모델과 다운로드 위치

이 프로젝트 자체가 학습하거나 배포하는 단일 AI 모델은 없습니다. 따라서
다운로드할 자체 모델 파일, 체크포인트, 파라미터 수, 학습 데이터 또는 모델
가중치 라이선스가 없습니다. 모델은 운영자가 `UPSTREAM_BASE_URL`, provider
설정 및 모델 패턴을 통해 런타임에 연결합니다.

기본 설정과 코드에는 라우팅 예시/기본값으로 `gpt-4.1-mini`, `gpt-4.1`,
`o3`, `claude-sonnet-4` 등의 식별자가 등장합니다. 이는 모델 번들 또는
재배포를 의미하지 않습니다. 상용 API 모델은 해당 공급자의 API에서, 로컬
모델은 운영자가 선택한 Ollama/vLLM 등의 배포 경로에서 별도로 받아야 하며,
정확한 버전·다운로드 위치·라이선스는 배포 시점의 provider 등록정보와 모델
원장에서 확정해야 합니다.

## 사용 목적

- 코딩 도구의 chat completion, embeddings 및 모델 목록 요청 중계
- 요청 복잡도·위험·provider 상태에 따른 모델/공급자 라우팅
- 사용량, 비용, 지연, 감사 이벤트 및 품질 신호 관측
- 정책, 비밀정보 보호, 쿼터, 승인 흐름과 모델 일몰 정책 적용
- 선택적으로 자연어를 읽기 전용 SQL로 변환하는 Text2SQL 파이프라인

## 입력과 출력

입력은 OpenAI 호환 요청, 프롬프트, tool 정의 및 선택적 조직 메타데이터입니다.
출력은 upstream 모델 응답 또는 정책/검증 결과입니다. 모델의 정확성·편향·환각
특성은 선택한 upstream 모델과 설정에 의존하며 게이트웨이가 이를 제거한다고
보장하지 않습니다.

## 제한사항과 안전 운용

- 생성 결과를 보안·법률·의료·재무 또는 운영상 중요한 판단에 그대로 사용하지
  말고 사람이 검토해야 합니다.
- production에서는 인증, 강한 `GATEWAY_SECRET`, prompt 원문 저장 비활성화,
  최소권한 API key, 보존정책 및 provider allowlist를 설정해야 합니다.
- Text2SQL은 읽기 전용 검증과 제한을 제공하지만 별도의 최소권한 DB 사용자,
  statement timeout과 허용 테이블 목록을 함께 적용해야 합니다.
- 외부 모델로 전송되는 정보에는 해당 provider의 데이터 처리·보존 정책이
  적용됩니다. 민감정보 전송 전에 조직 정책과 계약을 확인해야 합니다.

## 배포별 모델 기록 권고

심사·운영 재현성을 위해 배포자는 실제 사용 모델마다 provider, 정확한 model ID,
provider가 알린 snapshot/version, endpoint, 접근일, 약관/라이선스 URL, 사용 목적,
temperature/seed를 별도 원장에 기록해야 합니다. `vibe/auto` 사용 시 요청별
routing decision도 함께 보존해야 동일한 모델 선택을 추적할 수 있습니다.
