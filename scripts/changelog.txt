v0.71.0:- **Pod-Aware 운영 맵 — 멀티 파드 하트비트·설정 수렴 관측 (v0.71.0)**: 로드맵 B순위. 쿠버네티스 다중 파드 환경에서 어느 파드가 살아있고 어느 파드가 아직 최신 런타임 설정을 적용 못했는지 한 화면에서 확인. 신규 store pod_status 테이블(hostname PK·build_version·applied_token·current_token·reload_interval_s·last_seen) + UpsertPodStatus(멱등 upsert)·ListPods(staleAfter 윈도우로 stale 판정, applied==current로 up_to_date 산출). 각 파드가 runtimeReloadLoop 매 tick + 시작 시 heartbeatPod로 자기 상태(빌드=AppVersion·적용/현재 설정 토큰·reload 주기)를 기록 — 기존 v0.62.x this_pod 수렴 상태를 DB 영속·교차 파드로 확장. reload 비활성(단일 파드)도 시작 시 1회 기록. 신규 admin API GET /admin/pods?stale_s=(기본 90s) — pods + summary(total·live·stale·converged). 신규 운영 탭 "파드 운영 맵" — KPI(총/live/stale/설정 최신) + 파드 표(상태·hostname·빌드·설정 최신/동기화 대기·마지막 하트비트·reload 주기). OpenAPI 반영. 단위 테스트: upsert 멱등(hostname 중복 없음)·stale 판정·up_to_date(applied vs current). 후속: build SHA·provider cache·queue lag·ClickHouse watermark 컬럼 추가, stale 파드 정리 잡.

v0.70.1:- **analytics_daily 유니크 위반(23505) 수정 — daily rollup 멱등 upsert (v0.70.1)**: 운영 Postgres에서 analytics_daily_pkey 유니크 위반(SQLSTATE 23505)이 발생하던 버그 수정. 원인: rollupDayDimension이 (day,dimension)을 DELETE 후 INSERT...SELECT 하는 구조라, 동시 실행(예: 리텐션 purge 직전 rollup + 스케줄 rollup)이 겹치면 READ COMMITTED에서 두 트랜잭션이 각각 DELETE(서로의 미커밋 행 못 봄) 후 동일 (day,dimension,dim_value) PK를 INSERT → 한쪽이 23505로 실패. 해결: DELETE+INSERT를 단일 멱등 upsert로 교체 — INSERT ... SELECT ... ON CONFLICT (day,dimension,dim_value) DO UPDATE SET requests/tokens/cost_krw/errors=excluded.*. 동시·반복 실행이 충돌 없이 재계산 값으로 수렴(SQLite·Postgres 공통 지원). 부수 효과로 잠재 버그도 제거: 기존 DELETE 방식은 리텐션이 원본 로그를 purge한 뒤 과거 날짜를 재rollup하면 살아있던 집계를 지우고 0으로 덮어썼는데(analytics_daily는 리텐션 후에도 장기 추세를 보존해야 함), upsert는 재계산 행이 없으면 기존 값을 보존. 트랜잭션 래퍼 제거(단일 문). 단위 테스트: 동일 날짜 3회 연속 rollup이 에러 없이 중복/2배 집계 없이 수렴. 기존 purge-survival 테스트 통과.

v0.70.0:- **Journey Probe — 개발도구별 합성 연결 점검 (v0.70.0)**: 로드맵 A순위. "서버는 살아있는데 Cursor만 안 됨"을 구분하기 위해, 각 개발도구가 실제로 수행하는 연결 journey를 supplied Proxy API Key로 합성 점검. 신규 admin API POST /admin/journey-probe {proxy_key, clients?[]} — journeyDefs(openai-sdk·cursor·continue=models / roo·cline=models+mcp_init+mcp_tools / claude-desktop-mcp=mcp_init+mcp_tools)에 따라 클라이언트별 단계 점검. 인증은 probe key를 실은 합성 요청으로 authenticateProxyContext 재사용, 단계는 기존 Connection Doctor의 doctorProbe(/v1/models in-process 재생)·doctorMCP(/mcp/gateway initialize·tools/list)를 재사용 — read-only·비용 0(실제 chat/업스트림 호출 없음). 응답: 클라이언트별 overall(pass/warn/fail)+단계 checks, summary(clients/passing/failing). 신규 운영 탭 "Journey Probe"(#/journey-probe, admin:read) — Proxy API Key 입력 후 실행, 도구별 결과 카드(단계별 pass/warn/fail·detail·fix). 알 수 없는 클라이언트는 기본 models journey로 폴백. OpenAPI 반영. 단위 테스트: journeyDefs 단계 유효성·MCP 클라이언트는 init/tools 포함. Connection Doctor(개인·단일)와 달리 관리자가 여러 도구를 한 번에 점검. 후속: 스케줄 합성 모니터링(주기 실행+이력), embeddings 단계, 결과 추세.

v0.69.1:- **AI 자산 SBOM 검증 상태 연계 (v0.69.1)**: v0.69.0 SBOM이 소유권 공백만 표시하던 것을 검증 공백까지 확장. 적합성(model-fitness) 게이트 대상 스킬(high 위험 또는 require_model_fitness opt-in)이 통과 근거가 기준 미만이면 "검증 미흡(N/M)" gap을 추가 — 기존 CountPassingSkillFitnessEvidence·modelFitnessRequired·skillFitnessMinEvidence 재사용. 이제 SBOM이 "책임자 없음 + 검증 미흡"을 함께 드러내, 운영자가 production 자산의 거버넌스·검증 공백을 한 화면에서 점검 가능. /admin/sbom 응답·UI 표의 gaps에 자동 반영(추가 변경 없이). 신규 스키마/엔드포인트 없음, 게이트 대상 스킬에만 추가 조회(바운드). 빌드/감사/전체 proxy 테스트 통과. 후속: 골든 테스트 연계(prompt asset 검증), 자산 의존성 그래프, owner 일괄 지정.

v0.69.0:- **AI 자산 SBOM — 소유권·의존성 명세 (v0.69.0)**: 로드맵 A순위. 게이트웨이가 관리하는 AI 자산을 하나의 명세(SBOM)로 모아 "무엇이 책임자/검증 없이 운영 중인지" 거버넌스 공백을 드러냄. 신규 admin API GET /admin/sbom?type= — 스킬(owner·allowed_models/tools)·워크플로(created_by·step 수)·AI 앱(owner·component 수)·모델 계약(created_by·task)·프롬프트 자산(approved_by·category)을 통합 행으로 조립. 각 행에 거버넌스 gaps 플래그: "owner 없음"(소유자 미지정), 스킬은 "high 위험 production" 추가. 응답: total·by_type·gap_count·entries(type·id·name·owner·status·deps·gaps)·note. 원문 지침/프롬프트 미포함(메타만). 신규 운영 내비 탭 "AI 자산 SBOM"(#/sbom, admin:read) + renderSBOMView(KPI: 유형별 수·거버넌스 공백 수, 명세 표: 유형·이름·소유자(미지정 경고)·상태·의존성·공백) + JSON 내보내기. 기존 store list 메서드 재사용(ListSkills/ListWorkflows/ListWorkApps/ListModelContracts/ListPromptAssets), 신규 스키마/store 변경 없음. OpenAPI 반영. 단위 테스트: flagOwner(미지정 정규화·공백 owner·정상 owner). 후속: 골든/검증 상태 연계(검증 없는 자산 플래그), 자산↔자산 의존성 그래프, owner 일괄 지정.

v0.68.2:- **Canary 롤아웃 현황 대시보드 + 상향 추천 (v0.68.2)**: v0.68.1이 기록한 섀도우 결정을 정책별로 집계해 canary 램프업 의사결정을 지원. 신규 store CanaryPolicyStats(since) — enabled·rollout<100 정책을 policy_decision_events와 조인해 정책별 실집행(enforced_acts: block/require_approval/deny_*/secret_block, 슬라이스 안)과 섀도우(shadow_acts: canary_shadow, 슬라이스 밖 would-block)를 집계, nextRolloutStep로 10→25→50→100 다음 단계 추천. 신규 admin API GET /admin/policies/canary-status?days=(기본 7, RBAC). 정책 어드바이저 화면에 "Canary 롤아웃 현황" 카드 추가 — 정책·적용 비율·실집행·섀도우·"N%로 상향" 버튼. 버튼은 canaryBump: 기존 정책 정의를 /admin/policies에서 가져와 rollout_percent만 바꿔 재저장(rules 보존). 이로써 운영자가 섀도우가 의도대로면(오탐 적음) 한 번에 다음 단계로 점진 상향 가능. 단위 테스트: canary만 집계(full 정책 제외)·enforced/shadow 카운트·suggested_next, nextRolloutStep 사다리. 신규 스키마 없음. 이로써 로드맵 3차 Policy Canary & Shadow Enforce 완성: 섀도우 영향 미리보기 → canary 비율 enforce → 라이브 섀도우 기록 → 현황 대시보드 → 단계적 상향.

v0.68.1:- **Canary 섀도우 상시 기록 (v0.68.1)**: v0.68.0 canary 점진 활성화의 관측 보강. canary 비율(rollout<100) 밖이라 enforce되지 않은 라이브 요청 중, 정책이 "실제로 동작했을" 건(block/require_approval/allow-deny/secret action 보유 rule)을 decision="canary_shadow" 정책 결정 이벤트로 기록 — 차단/승인으로 카운트하지 않고 미적용 상태로 로깅만. 운영자는 10%→50% 램프업 중에도 나머지 트래픽에서 이 정책이 무엇을 차단했을지(라이브 오탐/영향)를 policy_decision_events(decision=canary_shadow 필터)로 계속 관측 가능 — 시뮬레이션(과거)뿐 아니라 실시간 근거 확보. evaluatePolicyRules 카나리 스킵 분기에서 ruleWouldAct(rule)로 enforcing action이 있는 rule만 shadow 기록(no-op rule 제외), reason에 canary 비율·미적용 명시. 기존 policyDecisionEvent/recordPolicyDecisionEvents 경로 재사용, 신규 스키마/엔드포인트 없음. 단위 테스트: 슬라이스 밖 canary가 미차단+canary_shadow 이벤트 기록, 안쪽은 차단, 시뮬레이션은 전체 적용. 후속: canary 자동 승급(오탐율 낮으면 단계 상향), 섀도우 결정 대시보드.

v0.68.0:- **Policy Canary 단계적 활성화 (트래픽 비율 enforce) (v0.68.0)**: 로드맵 3차 Policy Canary & Shadow Enforce의 canary 절반 구현. 정책을 100%로 바로 켜는 대신 트래픽의 일부(예: 10%→50%→100%)에만 점진 적용해 위험을 줄임. Policy에 rollout_percent 추가(기본 100=전체, 1~99=canary, 0은 100으로 정규화) — policies 테이블 컬럼 + 레거시 ALTER, ListPolicies/scanPolicy/UpsertPolicyWithRules 반영, decodePolicyPayload가 rollout_percent 파싱. ActivePolicyRules가 각 rule에 소속 정책의 rollout_percent를 실어줌(PolicyRule.RolloutPercent, 미영속·로드 시 주입). 적용: evaluatePolicyRules가 매칭된 rule에 대해 canaryBucket(FNV 해시 기반 0~99 결정적 버킷)을 계산해, rollout<100이고 버킷>=rollout이면 해당 rule 미적용(canary 슬라이스 밖). 핵심: 라이브 요청은 SubjectID(요청 식별 해시)를 가지므로 결정적·재현 가능하게 일부만 enforce되고, 시뮬레이터 컨텍스트는 SubjectID가 비어 canary 게이팅이 적용되지 않음 → 시뮬레이션은 항상 "전체 적용 시 영향"을 보여줌(섀도우 분석과 일관). 안전 정책 화면 폼에 rollout(%) 입력 + 정책 표에 "적용" 컬럼(canary N% 배지/100%) 추가. 단위 테스트: canaryBucket 안정성·범위, canary 슬라이스 in/out enforce 분기, SubjectID 없는 시뮬레이션은 전체 적용, 100%는 항상 적용, rollout 영속·rule 주입. 신규 엔드포인트 없음(기존 /admin/policies 확장). 이로써 운영자는 섀도우로 영향 검토(v0.67.x) → canary 비율로 점진 활성화 → 100% 승격의 안전한 정책 롤아웃 흐름을 갖춤. 후속: canary 자동 승급(오탐율 낮으면 단계 상향), 섀도우 모드 상시 기록.

v0.67.1:- **정책 어드바이저에 섀도우 영향 패널 노출 (v0.67.1)**: v0.67.0의 섀도우 분석 API(/admin/policies/simulate shadow{})를 어드민 화면에서 바로 쓰도록 정책 어드바이저에 연결. 각 추천 정책 카드에 "섀도우 영향" 버튼 추가 — 클릭 시 추천 규칙(conditions/actions)을 최근 7일 트래픽에 시뮬레이션해 모달로 표시: 평가 요청 수·차단 예상(수·비율)·승인 필요·영향 사용자/팀 수·오탐 후보(과거 2xx 정상이었으나 차단될 요청, 비율 배지 ≥30% 위험/≥10% 경고)·차단 비용(절감 추정)·오탐 후보 표본 표(API키·팀·모델·상태·사유, 원문 없음). 모달에서 바로 "draft 정책 생성"으로 이어짐 → 적용 전 영향 검토 후 draft 생성하는 자연스러운 흐름. UI 전용 변경(기존 simulate API 재사용), 신규 엔드포인트/스키마 없음. 후속: canary 단계적 활성화(트래픽 비율 enforce), 섀도우 모드 상시 기록(활성 정책의 가상 차단 로깅).

v0.67.0:- **Policy Shadow Enforce — 정책 시뮬레이션에 섀도우 영향 분석 추가 (v0.67.0)**: 로드맵 3차 목표(Policy Canary & Shadow Enforce) 착수. 기존 정책 시뮬레이터(/admin/policies/simulate: 차단/승인/허용 수·block_rate·sample)에 "정책을 활성화하기 전 실제 영향"을 미리 보는 섀도우 분석을 추가. store GovernanceSimContext에 status_code·cost_krw 추가(request_logs.status_code + token_usage.estimated_cost LEFT JOIN) — 과거 요청의 성공 여부와 비용을 시뮬레이션에 사용. /admin/policies/simulate 응답에 shadow{} 추가: affected_keys(차단될 distinct API 키 수)·affected_teams(영향 팀 수)·false_positive_candidates(과거 2xx로 정상 처리됐으나 이 규칙이 차단할 요청 = 오탐 후보)·false_positive_rate(차단 중 오탐 비율)·blocked_cost_krw(차단될 요청들의 과거 비용 합 = 절감 추정)·false_positive_sample(오탐 후보 표본 20건, 원문 없이 키/팀/모델/상태/사유만). 이로써 운영자가 정책 적용 전 "누가 영향받는지·정상 요청을 막진 않는지·비용은" 근거로 보안/운영팀을 설득 가능. 단위 테스트: GovernanceSimContexts가 status_code·cost를 정확히 싣는지. 신규 스키마 없음(기존 테이블 JOIN 확장). 후속: 정책 advisor 화면에 섀도우 영향 패널 노출, canary 단계적 활성화(트래픽 비율), 섀도우 모드 상시 기록.

v0.66.3:- **Flight Recorder 4차 — 세션 RCA 요약 자동 생성 (v0.66.3)**: 비행기록 캡스톤. 운영자가 타임라인을 스캔하기 전에 세션의 핵심을 한 줄로 파악하도록, 규칙 기반(폐쇄망·LLM 미사용) 한국어 RCA 요약을 생성. handleSessionFlightRecorder가 이미 집계한 카운트(요청·오류·시크릿/정책차단/위험코드 요청·모델 수·비용)로 sessionRCASummary 산출 → summary{verdict·headline·findings}. verdict 규칙: 시크릿/정책차단/high 위험코드 중 하나라도 있으면 "위험", 아니면 오류>0 시 "주의", 그 외 "정상". headline: "N건 요청 · 모델 N종 · 오류율 N% · 비용 N만원". findings: 발생한 위험 신호별 한국어 문장(없으면 "위험 신호 없음"). 비행기록 모달 상단에 RCA 요약 카드(verdict 배지 색상화·headline·발견 목록) 추가. 신규 store/스키마/엔드포인트 없음(기존 비행기록 응답에 summary 필드 추가, 핸들러 내 계산). 단위 테스트: verdict 분기(정상/주의/위험 3종 신호)·findings 채움. 이로써 Flight Recorder는 세션 목록→타임라인→위험 오버레이→RCA 요약까지 일관 제공. 후속: workflow/text2sql 이벤트 오버레이, 세션 검색/필터.

v0.66.2:- **Flight Recorder 3차 — 세션 타임라인에 governance/위험 오버레이 (v0.66.2)**: 비행기록의 핵심 RCA 가치 강화 — "보안 이벤트가 세션의 어느 흐름에서 났는지"를 한눈에. 신규 store SessionRiskMarkersFor(sessionID) — request_logs.session_id 조인으로 세션 요청들에 묶인 위험 신호를 3개 grouped 쿼리(요청당 N쿼리 아님)로 수집: secret_events(요청별 시크릿 이벤트 수), policy_decision_events(allow/allowed/pass 외 결정=차단 수), code_verify_results(요청별 코드 위험도). handleSessionFlightRecorder가 이를 각 이벤트에 오버레이(secret_events·policy_blocks·code_risk 필드)하고 세션 롤업에 risk{secret_requests·policy_block_requests·high_risk_code_requests} 추가. 비행기록 모달: 상단 요약에 "위험 신호" 배지(시크릿/정책 차단/위험 코드), 타임라인 표에 "위험" 컬럼(🔑시크릿·⛔정책차단·⚠코드위험 배지) 추가 — 위험이 발생한 요청을 즉시 식별하고 상세로 드릴인. anomaly_events는 scope 기반(요청 비귀속)이라 제외. 원문 미포함. 신규 스키마 없음(기존 governance 테이블 조인). 단위 테스트: RecentSessions 집계·SessionRiskMarkersFor(시크릿·정책차단(allow 제외)·코드위험). 후속: workflow/text2sql 이벤트 오버레이, 세션 검색/필터, 세션 RCA 요약 생성.

v0.66.1:- **Flight Recorder 2차 — 세션 목록/인덱스 + 독립 탭 (v0.66.1)**: v0.66.0 비행기록이 요청 상세를 거쳐야만 도달 가능했던 것을 독립 탐색 가능하게. 신규 store RecentSessions(since, limit) — request_logs를 session_id로 그룹핑(빈 세션 제외)해 세션별 요청 수·기간(MIN/MAX created_at)·distinct 모델/API키·오류 수·총 토큰·총 비용 집계, 최근 활동순. 신규 admin API GET /admin/sessions?days=(기본 7, 1~365, RBAC) — 세션 롤업 목록. 신규 운영 내비 탭 "세션 비행기록"(#/sessions, ops 그룹, admin:read) — menuRegistry·정적 nav·route()·렌더 함수 추가. renderSessionsView: 기간 선택 + 세션 표(세션·요청(+오류)·모델·토큰·비용·마지막 활동·비행기록 버튼), 각 행에서 openFlightRecorder 모달로 드릴인. (기존 /admin/sessions/{id}/flight-recorder 프리픽스 핸들러와 /admin/sessions 정확매치 핸들러 공존.) OpenAPI 반영. 신규 스키마 없음(request_logs.session_id 재사용). 후속: governance/정책/workflow/text2sql 이벤트 세션 타임라인 오버레이, 세션 검색 필터, 세션 RCA 요약.

v0.66.0:- **Agent Session Flight Recorder 1차 — 세션 단위 타임라인 (v0.66.0)**: 로드맵 2차 목표 착수. trace_id가 단일 액션을 묶는다면 session_id는 여러 요청으로 이어지는 코딩 세션 전체를 묶는다 — 한 세션에서 일어난 게이트웨이 활동을 시간순 비행기록으로 재구성. 신규 admin API GET /admin/sessions/{session_id}/flight-recorder(RBAC): RecentRequests(session 필터, 상한 500)를 created_at 오름차순으로 재정렬해 이벤트 타임라인 + 세션 롤업(요청 수·기간(시작/끝)·종류별 분포(endpointKind: chat/embedding/responses/messages/completion/기타)·distinct 모델·provider·trace_id·총 토큰·총 비용·오류 수·도구 호출 수)을 반환. 각 이벤트는 요청 상세(/admin/requests/{id})·trace(/admin/requests/{id}/trace)로 연결 — 기존 trace 전파(v0.63.x)·요청 상세 위에 세션 레이어를 얹음. 원문 미포함, 안전 메타만. 어드민 요청 상세 모달의 Session 행에 "비행기록" 링크 추가 → 세션 타임라인 모달(롤업 요약 + 이벤트 표: 시각·종류·모델/provider·상태·지연·토큰/비용·도구·상세 링크). OpenAPI 반영. 단위 테스트: endpointKind 분류·keysOf 정렬. 신규 스키마 없음(기존 request_logs.session_id 재사용). 후속: 정책 결정·governance(secret/anomaly/approval)·workflow/app run·text2sql를 세션 타임라인에 오버레이, 세션 목록/검색, 세션 RCA 요약.

v0.65.1:- **코드 검증 게이트 6차 — 모델별 코드 위험 추세 리더보드 (v0.65.1)**: v0.65.0이 영속한 code_verify_results를 모델 단위로 집계해 누적 관점의 코드 위험 리더보드 제공(v0.64.1 온디맨드 멀티런 리더보드의 영속·교차요청 보완, 로드맵 7번 연계). 신규 store CodeVerifyModelStats(since, limit) — code_verify_results를 request_logs와 request_id로 조인해 모델별 verdict 수·위험도 분포(high/medium/low)·high/medium/secret 발견 합·테스트 가능 블록·블록 수 집계, high 위험 verdict 많은 순 정렬, high_risk_rate(verdict 중 high 비율) 산출. 신규 admin API GET /admin/code-verify/stats?days=(기본 30, 1~365, RBAC) — models 배열 + totals + note. 코드 없는 응답은 제외(has_code=1), 응답 텍스트 캡처된 요청만 포함. Chat 테스트 화면에 "코드 위험 리더보드(최근 N일)" 카드 추가 — 모델·verdict 수·high 비율 배지(≥50% 위험/≥20% 경고)·high/med·high 발견·시크릿·테스트 가능, 데이터 없으면 숨김. OpenAPI 반영. 단위 테스트: 모델별 집계·high 위험 우선 정렬·high_risk_rate·발견 합. 원문 코드 미포함. 이로써 코드 검증 게이트는 즉시검증(admin API)·멀티런 비교·자동평가 통합·단일테스트 인라인·요청 영속/상세·모델 추세까지 일관 제공. 후속(2차 목표): Agent Session Flight Recorder.

v0.65.0:- **코드 검증 게이트 5차 — verdict 영속화 + 어드민 요청 상세·트레이스 연계 (v0.65.0)**: 코드 검증을 /v1 본경로 요청 단위로 영속하고 관리자 화면에 노출. 신규 code_verify_results 테이블(request_id·trace_id·has_code·risk·block_count·languages·high/medium/syntax/secret/testable count·findings_json·created_at, request/trace 인덱스) + LogRecord.CodeVerify 필드. 파이프라인 훅(pipeline.go): 응답 완료 후 캡처된 completion 텍스트(응답 텍스트 로깅 또는 캐시 캡처 시)에 verifyCode를 적용해, 코드가 있으면 안전 메타(위험도·발견 규칙/줄/심각도/detail)를 LogRecord에 실어 기존 비동기 로깅 큐로 영속 — 원문 코드는 저장하지 않고 발견 항목 100개 상한. 상태코드 4xx 이상이거나 캡처 텍스트가 없으면 no-op. buildCodeVerifyLog가 codeVerifyReport→store.CodeVerifyLog 매핑. store: codeVerifyForRequest(RequestDetail에 code_verify 부착)·CodeVerifyByTrace(트레이스 단위 조회). 어드민 요청 상세 모달에 "코드 검증" 섹션 추가 — 위험도 배지·코드블록/언어·high/med·시크릿/구문·테스트 가능 블록 + 발견 항목 표(심각도·언어·줄·규칙·설명, 원문 미표시, 상한 50). 전역 트레이스 조회(GET /admin/traces/{trace_id})에 code_verdicts 배열·count 추가. 단위 테스트: InsertLogRecord→RequestDetail.CodeVerify 라운드트립·CodeVerifyByTrace·verdict 없는 요청은 nil. OpenAPI 변경 없음(기존 요청 상세·트레이스 엔드포인트에 필드 추가). 제약: 코드 검증 영속은 응답 텍스트가 캡처될 때만 동작(LOG_RESPONSE_TEXT 또는 캐시 캡처). 후속: 캐시 적중 경로(chat/semantic) 검증, 위험 코드 응답에 대한 정책/알림 연계, 모델별 코드 위험 추세 집계.

v0.64.3:- **코드 검증 게이트 4차 — 단일 Chat 테스트 응답에 검증 메타 부착 (v0.64.3)**: 멀티런·자동평가에 이어 가장 많이 쓰는 단일 Chat 테스트 콘솔에도 코드 검증을 인라인 노출. (1) 서버: 비스트리밍 POST /admin/chat-test/run 응답 JSON에 code_verify 필드 추가(verifyCode(content) — has_code·risk·languages·counts·블록별 verdict, 원문 코드 미포함). (2) UI: Chat 콘솔은 스트리밍(/admin/chat-test/stream) 경로라 응답을 클라이언트에서 조립하므로, 스트림 종료(finalizeChatStream) 후 조립된 답변을 기존 게이트(POST /admin/code-verify)로 점검해 "실행 요약" 디버그 패널에 코드 검증 섹션을 덧붙임 — 위험도 배지(none/low/medium/high)·코드블록 수·언어·high/med 발견·시크릿/구문·테스트 가능 블록 수, 그리고 발견 항목별(심각도·언어·줄번호·한국어 detail, 원문 미표시) 목록(상한 30). 코드블록이 없으면 섹션 미표시. 서버 게이트 재사용으로 JS 측 규칙 중복 구현 없음, 원문 코드는 점검을 위해 서버로만 전송되고 응답엔 규칙·줄번호 메타만 반환. OpenAPI 변경 없음(기존 엔드포인트 재사용·응답 필드 추가). 빌드/감사/전체 proxy 테스트 통과(verifyCode 로직은 기존 단위 테스트로 커버). 후속: /v1 본경로 응답 검증 메타·verdict 영속 테이블·어드민 요청 상세 연계.

v0.64.2:- **코드 검증 게이트 3차 — 자동 평가(rubric) 안전 점수 통합 (v0.64.2)**: 멀티런 자동 평가(rule 방식)의 안전(safety) 점수가 거친 평면 패널티(riskyPatterns 히트당 -35)에 의존하던 것을, 코드 검증 게이트(verifyCode)의 언어별 심각도 등급으로 정밀화. ruleScoreInto 변경: 텍스트 수준 위험 구문(프롬프트 인젝션·코드 밖 시크릿)은 히트당 -25로 유지하되, 펜스 코드블록 내부 위험은 게이트가 등급화 — high 발견 -30/건, medium -10/건 차감(0 하한). 평가 사유(reason_summary)에 "코드 위험 <none|low|medium|high>(high N/med M)", 테스트 가능 블록 수를 추가해 운영자가 왜 안전 점수가 깎였는지 코드 단위로 파악. 이제 위험 명령이 코드 펜스 안에 있어도(예: os.system·eval·rm 계열·DROP) 안전 점수에 정확히 반영됨 — 기존 riskyPatterns는 코드 밖 신호(인젝션 문구·평문 시크릿) 담당으로 역할 분리. 스키마·저장 구조 변경 없음(기존 safety 점수·reason 필드에 합산), judge 응답 note에 통합 사실 명시. 단위 테스트: 무해 코드블록 safety 100 유지·위험 코드블록(os.system+eval) safety 하락·reason에 "코드 위험" 포함. 기존 TestRuleScoreSafetyAndVerdict(코드 밖 패턴) 회귀 통과. 후속: /v1 응답 검증 메타 부착·verdict 영속 테이블·어드민 요청 상세 연계.

v0.64.1:- **코드 검증 게이트 2차 — 멀티런 모델별 코드 리더보드 (v0.64.1)**: v0.64.0 정적 분석 코어를 chat-test 멀티모델 비교에 연결. 신규 GET /admin/chat-test/multi-run/runs/{id}/code-verify(admin)가 저장된 각 모델 응답 preview에 verifyCode를 적용해 코드 위험도·발견 항목·테스트 가능성을 산출하고 모델을 순위화 — "텍스트가 그럴듯한 모델"이 아니라 "실제 코드가 안전·검증가능한 모델"을 가린다(로드맵 7번 코드 품질 리더보드의 첫 연결). 점수화 codeVerifyScore: 코드 없음=중립 50, 코드 있음=기본 100 − high*25 − medium*8 − syntax*5 + testable*5(0~100 상한/하한). 순위: 코드 포함 응답 우선 → 점수 내림 → 위험 낮은 순 → 모델명. 응답: leaderboard(rank·model·score·risk·block_count·languages·counts)·per_model·models_with_code·scoring_breakdown. stateless — 이미 저장된 preview에서 계산해 새 스키마·영속화 없음, verifyCode 제약대로 원문 코드 미반환(위험/발견 메타만). 어드민 멀티런 화면에 "코드 검증" 버튼 + 리더보드 카드(#·모델·점수·위험·블록·언어·high/med·시크릿/테스트, 토글) 추가. OpenAPI 반영. 단위 테스트: codeVerifyScore(무코드 중립·clean 100 cap·high 차감·0 floor)·안전 코드가 위험 코드보다 고득점. 주의: 멀티런 preview는 500자 절단 저장이라 매우 긴 응답은 일부만 평가됨. 후속: /v1 응답 검증 메타 부착, verdict 영속 테이블 + 어드민 요청 상세 연계, 자동 평가(rubric) safety 점수와 통합.

v0.64.0:- **AI 코드 출력 검증 게이트 — 1차(정적 분석 코어 + admin API) (v0.64.0)**: 로드맵 S순위 "AI 코드 출력 검증 게이트"의 첫 증분. 게이트웨이가 모델 응답의 코드블록을 추출해 언어별 정적 점검(위험 API·파괴적 명령·하드코딩 시크릿·구문 균형)을 수행하고 위험도·테스트 가능성을 산출. 신규 internal/proxy/code_verify.go — extractCodeBlocks(fence info-string 언어 인식 + 무지정 시 내용 휴리스틱 guessLang: go/python/java/sql/javascript/typescript/shell), 의존성 0·폐쇄망 규칙 기반 codeDangerRules(shell: 재귀 강제삭제·fork bomb·원격 스크립트 파이프 실행·과도한 권한·디스크 직접쓰기 / python: eval·exec·os.system·subprocess shell=True·pickle·yaml.load / js·ts: eval·child_process·innerHTML / go: exec.Command·unsafe·RemoveAll / java: Runtime.exec·ProcessBuilder·ObjectInputStream / sql: DROP·TRUNCATE·GRANT ALL·주입패턴·WHERE 없는 DELETE/UPDATE), 기존 secret firewall(detectSecretsInText) 재사용한 하드코딩 시크릿 탐지, bracketImbalance 괄호 균형 검사, looksTestable 실행 가능성 휴리스틱. verifyCode(text) → codeVerifyReport(has_code·block_count·languages·risk[none|low|medium|high]·counts·per-block{lang·lines·hash·findings·risk·testable}). 표준 제약 준수 — 원문 코드 미저장, 발견 항목은 규칙명·줄번호·한국어 detail만(매칭된 소스 텍스트 미포함), 블록은 sha256 prefix 해시로만 식별. 신규 admin API POST /admin/code-verify(RBAC, 무상태·미저장, 1MB 입력 제한)로 임의 텍스트 즉시 검증. OpenAPI 반영. 단위 테스트 7종: 블록 추출·언어 추측·무코드·위험 shell·시크릿·SQL DROP/no-where·안전+testable·괄호 불균형·발견 항목에 원문 미포함 검증. 후속: /v1·chat-test 멀티런 응답에 검증 메타 부착, 검증 verdict 영속 테이블 + 어드민 요청 상세·모델 리더보드 연계.

v0.63.1:- **Trace 전파 확장 — 워크플로·앱 실행을 요청 trace로 연결 + 전역 trace 조회 (v0.63.1)**: v0.63.0 Trace 타임라인의 후속. workflow_runs·ai_app_runs에 trace_id가 없어 한 사용자의 단일 액션이 /v1 요청에서 워크플로/앱 실행으로 이어질 때 끊겼던 것을 연결. 스키마: workflow_runs·ai_app_runs에 trace_id 컬럼 추가(CREATE + 레거시 ALTER, 기본값 ''), store 구조체/INSERT/SELECT/scan 반영, 신규 WorkflowRunsByTrace·AIAppRunsByTrace 조회. 실행 3개 생성 지점 모두에서 traceIDFromRequest(r)로 스탬프 — finishWorkflowRun, Gateway MCP gateway_run_workflow, handleUserAppRun(앱 실행). RecentRequests에 TraceID 필터 추가(RequestFilter.TraceID → r.trace_id = ?). 신규 전역 admin API GET /admin/traces/{trace_id}: 같은 trace_id로 묶인 게이트웨이 요청(경량 행 + 각 요청의 /admin/requests/{id}/trace waterfall 링크)·워크플로 실행·앱 실행을 한 번에 반환(counts 포함). 원문 prompt/SQL/args 미포함 유지(요청 행은 모델·provider·상태·지연·토큰·비용 메타만). OpenAPI 반영. 단위 테스트: WorkflowRunsByTrace·AIAppRunsByTrace 라운드트립(trace 스탬프 저장·조회). 후속: cache span 테이블, ClickHouse fact trace 그룹화, 전용 중첩 span 테이블, /admin/traces UI 노출.
v0.63.0:- **Trace 타임라인 — 요청 단위 통합 waterfall (v0.63.0)**: 한 요청이 모델 호출·MCP 도구·Text2SQL 단계로 어떻게 흘렀는지 단계별 latency·token·cost·error·cache를 한 화면 waterfall로 표시. 기존에 이미 공유되던 trace_id/request_id 링크(request_logs·tool_invocations·text2sql_spans)를 통합 — 신규 스키마/파이프라인 계측 없이 RequestDetail에서 조립. 신규 admin API GET /admin/requests/{id}/trace: root(요청: 모델·provider·지연·토큰·비용·cache_hit·error) + Text2SQL stage span(stage·latency·cost·status) + MCP/tool call span(server.tool·mcp/tool·error). span_id/parent_span_id 표준화(root 기준 부모), 시작 offset은 created_at 기준 상대시간, 원문 prompt/SQL/args 미포함. 어드민 요청 상세 모달 상단에 waterfall 바(종류별 색상·에러 강조·지연/토큰/비용 메타) 추가. OpenAPI 반영. 단위 테스트: span 조립·정렬·definition 제외·에러 root·total 계산. 후속: workflow_step_runs/ai_app_runs에 trace_id 전파, cache span 테이블, 전용 span 테이블·중첩.
v0.62.4:- **AI App Catalog 완성 — 앱별 명시 권한(ai_app_permissions) (v0.62.4)**: 로드맵 2단계의 마지막 미구현 항목. work_apps의 거친 allowed_teams/allowed_roles CSV 게이트에 더해, 특정 사용자/팀에게 앱을 직접 공유하는 명시 권한 테이블 추가. 신규 ai_app_permissions 테이블(app_id·subject_type[user|team]·subject_id, UNIQUE) + store(GrantAppPermission 멱등·RevokeAppPermission·ListAppPermissions·AppGrantsSubject). 신규 admin API: GET/POST/DELETE /admin/apps/{id}/permissions(RBAC). 가시성 통합: appVisibleToCaller = appVisibleTo(팀/역할) OR 명시 grant(가산적) — /v1/apps 목록·단건·실행에 적용, 단 비활성(archived/deprecated) 앱은 grant로도 비관리자에게 노출 안 됨. 어드민 AI 업무 앱 카드에 "권한" 버튼(부여/해제 모달). OpenAPI 반영. 단위 테스트: grant 멱등·user/team 매칭·revoke. 이로써 AI App Catalog(정의=work_apps·버전=ai_app_versions·실행이력=ai_app_runs·권한=ai_app_permissions) 구조 완성. 코드: store/ai_app_permissions.go·sqlstore.go·admin_work_apps.go·admin_openapi.go·admin_ui.go.
v0.62.3:- **대시보드 메뉴 그룹화 (v0.62.3)**: 상단 내비게이션에 "...대시보드" 명칭 메뉴가 흩어져 있던 것을 단일 "대시보드 ▾" 드롭다운으로 묶음. 하위: 종합 대시보드(#/dashboard)·팀 대시보드·보안 대시보드·비용 대시보드·DW 대시보드. 각 링크는 기존 data-tab·라우트·권한(security:read/admin:read/team:read)을 그대로 유지 — /me/navigation 권한 필터가 하위 항목을 개별 표시/숨김하고, 모든 하위가 숨겨지면 드롭다운 자체도 숨김. 하위 탭이 활성화되면 드롭다운 토글에 active 강조. CSS hover/focus-within로 펼침(폐쇄망, 외부 의존성 없음). 라우트·페이지·권한 로직 변경 없음(내비 IA만 정리). 코드: admin_ui.go(nav HTML·CSS·setActiveTab·applyNavPermissions).
v0.62.2:- **역할 변경 드롭다운에 전체 역할 노출 (v0.62.2)**: 로그인 계정(RBAC) 화면의 역할 생성/변경 드롭다운이 6개(developer·viewer·team_admin·admin·super_admin·service_account)로 하드코딩돼, 보안 대시보드 접근(security:read)을 가진 security_admin이나 readonly_admin·team_manager·ops_admin·ai_admin·billing_admin, 그리고 관리자 정의 custom 역할을 사용자에게 부여할 수 없던 문제. 해결: 드롭다운을 GET /admin/roles(내장+custom 역할 카탈로그)에서 동적으로 채우도록 변경(서버 role→scope 맵과 항상 동기화, 조회 실패 시 기존 6개 폴백). 서버측 역할 변경 핸들러(admin_users.go)의 검증을 roleScopes(내장 전용)에서 effectiveValidRole(내장+custom)로 변경해 사용자 생성 경로와 일관되게 custom 역할도 부여 가능. 이제 예: 사용자에게 security_admin을 부여하면 보안 대시보드 메뉴가 노출됨. 코드: admin_ui.go(역할 옵션 동적 로드)·admin_users.go(검증 일원화).
v0.62.1:- **런타임 설정 파드별 수렴 상태 관측 (v0.62.1)**: v0.62.0 멀티 파드 자동 반영의 운영 가시성 추가. 각 파드가 reloadRuntimeConfig 시 마지막 적용 시각(lastReloadNano)과 적용한 change token(lastReloadTok)을 기록. 신규 runtimeReloadStatus(hostname·last_reload_at·applied_token·current_token·up_to_date·reload_interval)를 GET /admin/settings 및 /admin/settings/effective 응답의 this_pod 필드로 노출. 런타임 설정 화면 상단에 배너 추가 — "이 파드(<hostname>) 런타임 설정 최신 ✓ / 동기화 대기…", 마지막 적용 시각·폴링 주기 표시. 운영자가 각 파드를 조회해 변경이 모두 전파됐는지 확인 가능(up_to_date = applied_token==current_token). 코드: server.go(필드)·admin_settings.go(기록·노출)·admin_ui.go(배너).
v0.62.0:- **런타임 설정 멀티 파드 자동 반영 (v0.62.0)**: 쿠버네티스 다중 파드 환경에서 한 파드가 런타임 설정을 변경해도 다른 파드는 재시작 전까지 메모리 캐시(atomic 스냅샷)가 stale했던 문제 해결. 원인: 각 파드가 자신의 PUT 시에만 reloadRuntimeConfig를 호출하고, 다른 파드의 DB 변경을 감지하지 못함. 해결: 신규 store AdminSettingsChangeToken(COUNT+SUM(version)+MAX(updated_at)로 upsert·update·delete 모두 감지하는 경량 토큰) + 백그라운드 runtimeReloadLoop가 SETTINGS_RELOAD_INTERVAL(기본 10s)마다 토큰을 폴링해 변경 시 reloadRuntimeConfig·reloadText2SQLFeatures·reloadKeycloakConfig를 재실행 → 모든 파드가 한 주기 내 수렴(재시작 불필요). 변경한 파드는 inline 적용 후 동일 토큰을 관찰만 함. config.RuntimeReloadInterval(env SETTINGS_RELOAD_INTERVAL, 0이면 폴링 비활성=단일 파드/로컬) 추가, 런타임 설정 env 카탈로그에 env.settings_reload_interval(read-only) 노출. 단위 테스트: 토큰이 upsert·재업데이트(version bump)·delete에서 변함. 폐쇄망(외부 의존성 0, DB 폴링만) 동작. 코드: store/admin_settings.go·config.go·proxy/admin_settings.go·server.go.
v0.61.6:- **관리자 API OpenAPI 전수 문서화 + Audit 게이트 강화 (v0.61.6)**: API Surface Audit의 잔여 미문서 50건(전부 /admin 운영 API + /v1 passthrough root)을 모두 OpenAPI 카탈로그에 추가해 openapi_missing 50→0 달성. 그룹: ops/capabilities(change-sets·change-impact·flow-map·system-errors·ops home/workers/preflight), chat-test multi-run 7종, DW/ClickHouse(metrics·events·fact-retry·lag), MCP(agentic-runs·trust-scores), model-tags, prompt-assets·prompt-lab(experiments·rubrics·contracts·test-cases), skills(fitness·adoption·skill-studio), RBAC roles, secrets/rotate, SSO config/test, text2sql(connections·registry export/import), xview(models·series·outliers). 각 핸들러의 실제 메서드 확인 기재(GET/POST/PUT/DELETE), by-id 라우트는 {id} 파라미터로. 이로써 README·CLI·SDK·OpenAPI·서버 route 불일치 0건 달성. cmd/api-surface-audit 게이트를 강화 — 이제 openapi_missing·undocumented_routes도 exit 1(기존 cli_only·sdk_only에 추가)로 처리해 신규 미문서 route가 CI에서 차단됨. 코드 변경 없이 카탈로그 확장 + 감사 도구 강화.
v0.61.5:- **사용자·SSO·팀 API OpenAPI 문서화 (v0.61.5)**: API Surface Audit이 가리킨 미문서 경로 중 클라이언트/사용자 대면 30개를 OpenAPI 카탈로그(apiEndpoints)에 추가 — openapi_missing 80→50 감소. 추가: /me/* 개인 surface(actions·snooze·navigation·notifications·recommended-models·report·reports/submit-to-team·sessions·sessions/{id} DELETE·sessions/revoke-others·skills·skills/{name}/request·access-denied), /v1/model-tags, Keycloak SSO 6종(/auth/sso/status·/auth/keycloak/login·callback·logout·backchannel-logout·frontchannel-logout, 공개 엔드포인트는 public 표시), /team/*(dashboard·onboarding·reports·risk·savings-challenge·skills/popular·templates/candidates)·/permissions/effective·/billing/dashboard·/security/dashboard. 각 항목의 실제 HTTP 메서드를 핸들러에서 확인해 정확히 기재(GET/POST/DELETE), 신규 태그 auth 도입. 코드 변경 없이 카탈로그만 확장 — /openapi.json·Swagger에 반영. 잔여 미문서 50건은 모두 /admin 내부 운영 API.
v0.61.4:- **Gateway MCP Contract Pack 정식화 (v0.61.4)**: 로드맵 4단계. /mcp/gateway를 임시 인터페이스가 아니라 공식 MCP 서버로 계약화. 신규 gatewayToolContracts() — 13개 gateway tool별 위험도(low/medium/high)·비용 정책·timeout·허용 역할·실행 여부·출력 스키마 계약 정의. handleGatewayMCPInfo(GET /admin/gateway-mcp/info)와 어드민 MCP 탭에 contracts 노출(Tool 계약 카드: 위험·비용·timeout·유형·출력). 단위 테스트: (1) 모든 광고 tool이 계약을 갖고 역도 성립(계약 없는 tool 추가 시 CI 실패), 계약 필드 유효성(risk_level·timeout·cost·output), (2) JSON-RPC 에지케이스 — 잘못된 본문 -32700, notification(id 없음) 무응답, 알 수 없는 tool 에러. 코드: mcp_gateway_contracts.go(신규)·mcp_gateway_local.go·mcp_gateway_local_test.go·admin_ui.go.
v0.61.3:- **워크플로 step별 실행 기록 + 영수증 (v0.61.3)**: 로드맵 6단계. 워크플로 실행 영수증이 집계(steps_total/ok)만 보여주던 것을 step별 상세로 확장. 신규 workflow_step_runs 테이블 + store(RecordWorkflowStepRuns 배치 저장 / ListWorkflowStepRuns step 순서 조회). executeWorkflowSteps 실행 후 finishWorkflowRun과 Gateway MCP gateway_run_workflow 양 경로에서 step별 안전 메타(step_index·name·type·ref·status·output_chars·error_class)를 저장 — 원문 출력/SQL/tool args는 저장하지 않고 출력 길이(chars)만 기록. `GET /v1/workflow-runs/{run_id}/receipt`가 steps 배열을 포함하도록 확장(소유자 검증·민감 원문 미노출 유지). 단위 테스트: step run 라운드트립·상태·output_chars. 코드: store/workflows.go·sqlstore.go·admin_workflows.go·mcp_gateway_local.go·me_run_receipts.go.
v0.61.2:- **Workflow 발행/버전 이력 (v0.61.2)**: 로드맵 3단계 보강. 워크플로에 publish/버전 스냅샷이 없던 부분을 앱과 동일 패턴으로 구현. 신규 workflow_versions 테이블 + store(PublishWorkflowVersion: 현재 정의를 다음 버전으로 불변 스냅샷 저장하고 enabled=1 활성화 / ListWorkflowVersions 최신순). 신규 admin API: POST /admin/workflows/{id}/publish(검증 실패 시 발행 거부, 버전 번호 반환), GET /admin/workflows/{id}/versions. 기존 /admin/workflows/ 핸들러를 action(dry-run|publish|versions) 디스패치로 일반화(dry-run 기본 동작 유지). 어드민 워크플로 화면에 발행·버전 버튼(버전 이력 모달). OpenAPI 반영. 단위 테스트: 버전 증가·step 스냅샷 보존·메타. 코드: store/workflows.go·sqlstore.go·admin_workflows.go·admin_openapi.go·admin_ui.go.
v0.61.1:- **AI App Catalog 발행/지원중단 + 버전 이력 (v0.61.1)**: 로드맵 2단계 보강. 기존 work_apps에 publish/deprecate 라이프사이클과 버전 스냅샷이 없던 부분을 구현. 신규 ai_app_versions 테이블 + store(PublishWorkAppVersion: 현재 앱 정의를 다음 버전으로 불변 스냅샷 저장하고 status=active로 활성화 / ListWorkAppVersions: 최신순 이력). 신규 admin API: POST /admin/apps/{id}/publish(메모 옵션, 버전 번호 반환), POST /admin/apps/{id}/deprecate(status=archived로 숨김), GET /admin/apps/{id}/versions(발행 이력). 기존 사용자 가시성(active만 노출)과 정합 — 발행=active 노출, 지원중단=archived 숨김. 어드민 AI 업무 앱 화면에 발행·지원중단·버전 버튼 추가(버전 이력 모달). OpenAPI에 /v1/apps·/admin/apps 전체 엔드포인트(미문서화 상태였음) 문서화 → API Surface Audit openapi_missing 84→80 감소. 단위 테스트: 버전 증가·스냅샷 보존·활성화. 코드: store/ai_app_versions.go·sqlstore.go·admin_work_apps.go·admin_openapi.go·admin_ui.go.
v0.61.0:- **API Surface Audit + SDK/CLI 계약 CI (v0.61.0)**: 로드맵 신규 1단계. 새 기능 추가가 아니라 README·CLI·SDK·OpenAPI가 기대하는 경로와 실제 서버 route의 정합성을 자동 점검. 신규 `cmd/api-surface-audit`(정적 분석: internal/proxy/*.go의 mux.HandleFunc 등록, admin_openapi.go의 apiEndpoints 카탈로그, cmd/vibe/main.go·sdk/typescript/vibe.ts의 경로 리터럴을 grep해 비교) — `server_routes`·`cli_paths`·`sdk_paths`·`openapi_paths`와 `cli_only_methods`·`sdk_only_methods`(서버에 없는 클라이언트 경로=계약 위반)·`openapi_missing`·`undocumented_routes` 산출. 접두 핸들러(`/v1/apps/`)·템플릿 파라미터(`${id}`) 인식. 클라이언트 경로가 서버 route와 불일치하면 exit 1. `--json` 출력 지원. 현재 결과: CLI/SDK 계약 위반 0건(/v1/chat/completions·/v1/models·/v1/apps/·/v1/workflows/·/mcp/gateway·/me/connection-doctor 모두 존재 확인). CI(ci.yml)에 api-surface-audit 실행 단계 + TypeScript SDK `tsc --noEmit --strict` 타입체크 잡 추가(폐쇄망 외 CI에서만; 의존성 0). 단위 테스트: pathCovered·extractors·계약 정합성. 코드: cmd/api-surface-audit/*, .github/workflows/ci.yml.
v0.60.6:- **vibe/auto 기본 모델을 업스트림 설정값으로 (v0.60.6)**: vibe/auto가 항상 내장 OpenAI 모델명(gpt-4.1-mini 등)으로 해석돼, 업스트림이 OpenAI가 아닌 배포에서는 제공되지 않는 모델을 호출하던 문제. 신규 환경변수 UPSTREAM_DEFAULT_MODEL(config UpstreamConfig.DefaultModel) 추가 — 설정 시 defaultAutoModelForPolicy가 모든 tier에서 이 모델을 최우선 후보로 사용(키 정책으로 허용될 때). 미설정 시 기존 gpt-4.1* 후보 그대로(동작 불변). 키 allow/deny로 거부되면 내장 후보로 폴백. Chat 테스트(단일/멀티)·MCP·라우팅 모두 동일 적용. 런타임 설정 env 카테고리에 env.upstream_default_model(read-only) 노출. 단위 테스트 추가. 코드: config.go·intelligent_routing.go·admin_settings.go.
v0.60.5:- **Chat 테스트 max_tokens 상한 확대 (v0.60.5)**: Chat 테스트 콘솔의 max_tokens가 서버측에서 4096으로 강제 제한되고 기본값도 1024라 최신 대용량 컨텍스트(약 128K) 모델을 충분히 시험하기 어려웠던 문제. 서버 prepareChatTestRequest의 상한을 4096→131072(128K)로 확대하고 targets 기본값을 1024→4096으로 상향. 단일/멀티모델 테스트 입력의 max 속성을 4096→131072, 빈 값 기본도 4096으로 변경. 전역 limits.max_output_tokens 가드레일(별도 거버넌스)은 그대로 유지. 코드: admin_chat_console.go·admin_ui.go.
v0.60.4:- **내 액션 큐 나머지 버튼 연결 (v0.60.4)**: 남은 액션 큐 버튼이 #/me 재렌더만 되던 것을 각자 맞는 대상으로 연결. meActionGo에 scroll:<id> 지원 추가(해당 섹션으로 부드럽게 스크롤 + 강조 표시, 필요 시 콘텐츠 로드). 매핑: 절감 추천 보기→scroll:me-report(주간 리포트), 실패 원인 보기→scroll:me-failures(최근 실패 카드, 신규 래퍼 id), 도구 보기→scroll:me-recmodels(추천 모델), 추천 보기→scroll:me-recs(추천 로드 후 스크롤), 리포트 만들기→modal:repeat_question(저장 리포트 생성 안내+내 저장 리포트 바로가기), 템플릿 만들기→modal:cache_improve(템플릿/시맨틱 캐시 재사용 안내). reportsCard/failCard에 스크롤용 래퍼 id(me-reports/me-failures) 추가. 코드: me_actions.go·admin_ui.go.
v0.60.3:- **내 액션 큐 "전환 가이드" 버튼 안내 모달 추가 (v0.60.3)**: model_switch 액션의 "전환 가이드" 버튼이 v0.60.2 이후 #/me 재렌더만 되어 실제 가이드가 없던 점 보완. ButtonHref를 modal:model_switch로 변경하고 meActionModal에 전환 가이드 모달 추가 — vibe/auto 자동 라우팅 사용, 간단 작업은 작은 모델 직접 지정, 주간 리포트 절감 추천으로 근거 확인, 품질 비교 후 반복 작업 기본값 고정 등 비용 절감 전환 방법을 한국어로 안내. 코드: me_actions.go·admin_ui.go.
v0.60.2:- **내 액션 큐 "안전 가이드" 버튼 무반응 수정 (v0.60.2)**: 내 홈의 개인 액션 큐 버튼이 `<a href="#/me">`로 렌더돼, 이미 #/me에 있는 상태에서 클릭하면 hashchange가 발생하지 않아 아무 반응이 없던 문제. 특히 "안전 가이드"는 대상 페이지가 없었음. 해결: 액션 버튼을 onclick `meActionGo(href)` 핸들러로 변경 — (1) `modal:<key>` href는 인라인 모달을 열고(신규 `meActionModal('safety')`로 민감정보 취급 안전 수칙 한국어 가이드 표시), (2) 현재 라우트와 같은 href는 renderMeHome()로 즉시 재렌더(클릭 피드백 보장), (3) 그 외는 정상 내비게이션. safety_warning 액션의 ButtonHref를 modal:safety로 변경. 코드: me_actions.go·admin_ui.go.
v0.60.1:- **Keycloak SSO 로그인 시 기존 팀이 "팀 없음"으로 비워지던 버그 수정 (v0.60.1)**: v0.60.0의 역할 강등 수정과 동일 계열 버그. IdP groups 클레임에 팀 정보가 없으면(예: /teams/<name> 그룹 미부여) provisionKeycloakUser가 빈 팀("")을 세션·JWT에 실어 발급해 로그인 직후 "팀 없음"으로 표시됐음(DB 멤버십은 유지되나 세션 team이 빈 값). 해결: 그룹 클레임에서 팀을 얻지 못하면 기존 사용자의 현재 주 팀(PrimaryTeamForUser)으로 폴백해 세션 team을 채움(effectiveTeam). 그룹 클레임에 팀이 있으면 종전대로 그 팀으로 자동 배정·생성. 신규 사용자는 종전 동작 유지. 코드: keycloak_handlers.go provisionKeycloakUser.
v0.60.0:- **Keycloak SSO 로그인 시 기존 역할 강등 버그 수정 (v0.60.0)**: super_admin 등 내부 역할을 가진 사용자가 Keycloak SSO로 로그인하면 IdP에 매핑된 역할이 없을 때 기본 역할(developer)로 강등되던 버그 수정. 원인: provisionKeycloakUser가 매 로그인마다 연결된 기존 사용자의 역할을 SSO 해석 역할로 덮어썼고, claim 매핑이 없으면 기본 역할로 폴백돼 강등. 해결: resolveKeycloakRoleExplicit(role, explicit) 도입 — claim→role 명시 매핑이 있을 때만 역할을 갱신하고, 기본 폴백인 경우 기존 사용자의 역할을 보존(상태는 active로 동기화). 명시 매핑 시에는 기존대로 승격/변경 허용. 신규 사용자는 종전대로 해석 역할 적용. 단위 테스트: 명시 매핑 vs 기본 폴백 explicit 플래그 검증. 주의: 이미 강등된 계정은 배포 후 관리자 화면에서 super_admin으로 복구 필요(이후 SSO 재로그인 시 보존됨).
v0.59.9:- **체크박스 디자인 축소 + SSO 환경변수 런타임 노출 + Keycloak SSO state(CSRF) 버그 수정 (v0.59.9)**: (1) 전역 CSS에서 `input{min-width:140px}`가 체크박스/라디오까지 키워 주변과 안 어울리던 문제를 `input[type=checkbox],input[type=radio]{height:auto;width:auto;min-width:0;margin:0 4px 0 0;accent-color:var(--accent)}` 규칙으로 자연 크기 복원(개별 인라인 override 불필요화). (2) 런타임 설정 카탈로그(settingRegistry)에 SSO_KEYCLOAK_* 환경변수를 env.sso 카테고리 read-only 항목으로 빠짐없이 노출(enabled·issuer_url·client_id·client_secret(secret)·redirect_uri·scopes·default_role·role_claim·group_claim·allow_local_login) — 편집은 기존 SSO 설정 화면(암호화 저장)에서. (3) Keycloak 로그인 콜백에서 "invalid or expired state (CSRF check failed)" 발생 버그 수정: OIDC 로그인 플로우 state(nonce+PKCE verifier)가 in-memory map에만 저장돼 프로세스 재시작/다중 인스턴스 시 콜백에서 유실됐음. 신규 oidc_flow_states 테이블 + Save/Take(단일 사용·10분 TTL)로 DB 영속화, Server.saveOIDCFlow/takeOIDCFlow는 DB 우선·in-memory fallback. 단위 테스트: 플로우 state 라운드트립·단일사용·만료. 코드: admin_ui.go(CSS)·admin_settings.go(env.sso)·store/oidc_flow_state.go·keycloak.go·keycloak_handlers.go.
v0.59.8:- **내 키 스코프 모달 편집 + 연결 진단 JWT 모드 (v0.59.8)**: (1) "내 키" 발급 폼의 큰 인라인 체크박스 그리드를 제거하고 admin 스코프 편집기와 동일한 모달 방식으로 변경 — 발급 시 "스코프 N개 선택됨/전체 상속" 버튼으로 모달을 열어 부여 가능한(역할) 스코프만 선택. (2) 기존 키의 스코프 수정 기능 추가: 각 활성 키 행에 "스코프" 버튼 → 모달에서 현재 스코프 프리체크 → 저장 시 신규 `PATCH /me/keys/{id}`(normalizeScopes·scopesWithin로 역할 범위 capping, 초과 시 403 scope_denied, 전부 해제 시 전체(미지정)). (3) 내 홈 "내 개발도구 연결하기(MCP)" 연결 진단이 키가 있어도 "유효한 Proxy API Key 없음"으로 표시되던 버그 수정 — 브라우저는 JWT로 호출하는데 doctor가 proxy key만 인증했던 것이 원인. 이제 proxy key(실클라이언트 테스트=full live 검사) 또는 JWT 세션(브라우저=발급된 활성 키 기준 진단, /v1·/mcp 실도달성은 plaintext 키 없으므로 skip 표기) 양쪽 지원. auth_mode 응답 필드 추가. 단위 테스트: PATCH 부분집합(200)·상승(403), grantable/role 노출. patchJSON 테스트 헬퍼 추가.
v0.59.7:- **내 키 관리 스코프 선택 UI (역할 기반 제한 유지) (v0.59.7)**: 셀프서비스 "내 키" 발급 폼의 자유 입력 스코프 텍스트박스를 역할에 맞는 체크박스 선택기로 교체. `GET /me/keys` 응답에 호출자의 `role`과 `grantable_scopes`(본인=역할 스코프, normalizeScopes로 정규화)를 추가해 UI가 부여 가능한 스코프만 체크박스로 노출. 아무것도 선택하지 않으면 기존처럼 내 권한 전체 상속, 선택 시 그 부분집합만 부여. 서버측 권한 상승 방지(scopesWithin 기반)는 그대로 유지 — 역할 범위를 벗어난 스코프 요청은 403(scope_denied). 한국어 스코프 라벨 맵(mkScopeLabel) 추가, 폼에 내 역할 표시. 단위 테스트: grantable_scopes/role 노출·유효 부분집합 발급(201)·상승 거부(403) 검증. 코드 변경: me_keys.go(GET 응답 확장)·admin_ui.go(체크박스 선택기·createMyKey).
v0.59.6:- **/mcp vs /mcp/gateway 문서화 (v0.59.6)**: 로드맵 F 부수 작업. 이름이 비슷한 두 MCP 엔드포인트의 차이를 README·USER_GUIDE·ADMIN_GUIDE에 명확히 구분 정리. `/mcp`(등록된 외부 업스트림 MCP 서버 집약, 도구명 `<업스트림ID>__<이름>`, 업스트림 등록 필요) vs `/mcp/gateway`(게이트웨이 자체 기능을 MCP tool로 노출 — `gateway_chat`·`gateway_route_preview`·`gateway_run_workflow` 등, 업스트림 등록 불필요, 실행형 tool은 /v1 파이프라인 재생으로 거버넌스·쿼터 동일 적용). README에 비교 표 + `/mcp/gateway` 클라이언트 설정 JSON, USER_GUIDE에 3.12 절(연결 진단·`vibe doctor`·`vibe mcp config` 안내 포함), ADMIN_GUIDE에 Gateway MCP(`/admin/gateway-mcp/info`·`/admin/mcp/gateway/test`) 및 MCP Tool Contract Registry(`/admin/mcp/contracts`·`/validate`, drift/missing 의미) 운영 절 추가. 코드 변경 없음(문서·버전 범프). 이로써 로드맵 F 및 부수 문서화까지 완료.
v0.59.5:- **MCP Tool Contract Registry — MCP tool 계약 레지스트리 + 드리프트 탐지 (v0.59.5)**: 로드맵 F 6단계(최종). MCP tool의 입력/출력 스키마·위험등급(low/medium/high)·타임아웃·허용 역할(CSV)·비용 정책·소유자를 계약으로 고정하는 레지스트리. 신규 `mcp_tool_contracts` 테이블 + store CRUD(UpsertMCPToolContract·ListMCPToolContracts(namespace·enabled 필터)·GetMCPToolContract·DeleteMCPToolContract). `GET/POST/DELETE /admin/mcp/contracts`(admin RBAC, upsert 시 스키마 JSON 유효성·risk_level 검증), `POST /admin/mcp/contracts/validate`로 등록 계약과 실제 게이트웨이 tool(gatewayToolDefs) 간 스키마 드리프트 탐지 — tool 소실(missing)·입력 스키마 속성 집합 차이(drift: declared_only/live_only) 보고(타입 심층 비교 아님, 속성 키 집합 비교). gateway 외 namespace는 not_checkable. MCP 탭 UI에 계약 목록·생성 폼·드리프트 검증 버튼 추가. OpenAPI 반영. 단위 테스트: store 라운드트립·schemaPropKeys·diffKeys·gatewayToolDefs 스키마 노출. 이로써 로드맵 F 6단계 전부 완료.
v0.59.4:- **Client Connection Doctor — 클라이언트 연결 진단 (v0.59.4)**: 로드맵 5단계. 사용자가 자신의 개발도구 연결 설정을 스스로 진단. `POST /me/connection-doctor`(body {client: openai-sdk|cursor|roo|cline|claude-desktop-mcp})가 인증(Proxy API Key)·scope(MCP는 mcp:use 권장)·모델 allowlist·쿼터(checkQuotas)·`/v1/models` 도달성(in-process 재생)·MCP 클라이언트는 `/mcp/gateway` initialize+tools/list까지 점검해 항목별 pass/warn/fail + 조치 힌트를 반환. 익명 호출은 401 대신 auth=fail 구조화 응답(진단 목적). 기존 authenticateProxyContext·dispatchGatewayMCP·requestOrigin 재사용, 원문 미저장. cmd/vibe에 `vibe doctor [--client C]` 추가(--table 사람용 출력). /me MCP 연결 카드에 클라이언트 선택 + "연결 진단" 버튼·결과표 추가. OpenAPI 반영. 단위 테스트: doctorRank 정렬·mcpClients 분류·method-not-allowed. 로드맵 5단계.
v0.59.3:- **템플릿 프롬프트 자산 평균 비용 SQL 에러 수정 (v0.59.3)**: ListPromptAssets 함수에서 request_logs 테이블에 존재하지 않는 cost_krw 컬럼을 직접 SELECT하여 SQL logic error (no such column: cost_krw)가 발생하던 문제를 token_usage 테이블과의 LEFT JOIN을 통해 estimated_cost를 집계하도록 변경하고 버전을 v0.59.3으로 범프.
v0.59.2:- **어드민 UI pctv 중복 선언 수정 (v0.59.2)**: renderTeamHome 함수 내 동일 스코프에서 자바스크립트 변수 `pctv`가 중복 선언되어 admin 페이지 렌더링 시 구문 에러(Uncaught SyntaxError: Identifier 'pctv' has already been declared)가 발생하는 버그를 수정하고 버전을 v0.59.2로 범프.
v0.59.1:- **App Run Receipt — 앱·워크플로 실행 영수증 (v0.59.1)**: 로드맵 4단계. 앱/워크플로 실행 1건을 사용자·운영자가 이해 가능한 영수증으로 제공. `GET /v1/app-runs/{run_id}/receipt`(앱: 앱명·상태·error_class·output_summary·input_hash·latency·cost·시각), `GET /v1/workflow-runs/{run_id}/receipt`(워크플로: 워크플로명·상태·steps_total/ok·error_class·latency·cost·시각). 원문 prompt/SQL/tool args 미노출(기존 ai_app_runs/workflow_runs 집계만 사용). 소유권 검증: run.user_id==caller(meUserID), 불일치 403·미존재 404. 신규 store GetAIAppRun·GetWorkflowRun(단건 조회). OpenAPI 반영. 단위 테스트: runReceiptID 경로 파싱·store 단건 getter. 로드맵 4단계.
v0.59.0:- **Gateway MCP 정식화 — run_workflow tool + admin 테스트 (v0.59.0)**: 로드맵 1단계 보강. `gateway_run_workflow`(workflow_id·input) MCP tool 추가 — 워크플로 실행 로직을 `executeWorkflowSteps`로 분리해 HTTP 핸들러와 공유, API 키 호출자(authCtx→accessClaims)로 팀 게이팅 후 서버측 순차 실행·run 기록. 신규 `POST /admin/mcp/gateway/test`(admin)로 외부 MCP 클라이언트 없이 게이트웨이 tool을 name+arguments로 직접 호출해 검증(읽기 진단, authCtx 없이 실행돼 사용자 범위 tool은 식별자 없음 보고). 이로써 Gateway MCP 실행형 tool 6종(chat·run_skill·text2sql_preview·run_saved_report·create_app_run·run_workflow). 단위 테스트: tools/list에 run_workflow 노출. README 문서화는 후속.
v0.58.8:- **TypeScript SDK (v0.58.8)**: step 5 SDK 산출물. 의존성 없는 단일 파일 `sdk/typescript/vibe.ts` — Node 18+/브라우저 global fetch 사용. VibeClient(baseURL·apiKey)로 chat(skill·maxTokens 옵션)·models·runApp·runWorkflow·quota·usage·routePreview·mcpTool·mcpConfig 제공. 읽기 조회는 Gateway MCP(/mcp/gateway), chat/app/workflow는 /v1. 모든 호출은 Bearer API Key로 호출자 권한 적용. README 포함. 주의: 본 SDK는 Go 테스트(go test ./...) 범위 밖이며(저장소에 JS/TS 툴체인 없음) 수기 작성·검토만 됨 — changelog·README에 명시. Go 빌드/테스트에는 영향 없음.
v0.58.7:- **Gateway MCP gateway_create_app_run (v0.58.7)**: 원래 Gateway MCP tool 목록의 마지막 항목. `gateway_create_app_run`(app_id)이 AI 업무 앱을 실행 — API 키 호출자의 authCtx를 accessClaims로 변환해 기존 appVisibleTo 가시성 검증·appComponentStep 플랜·validateAppComponent 권한 재검증·RecordAIAppRun 이력 기록을 재사용. 구성요소 실행 플랜(각 step endpoint/resolved)과 run_id 반환, 실행 이력은 안전 메타만 저장. 이로써 스펙의 Gateway MCP tools(읽기 7 + 실행 5)가 모두 구현됨. 단위 테스트: tools/list 노출.
v0.58.6:- **Gateway MCP gateway_run_saved_report (v0.58.6)**: 원래 Gateway MCP 스펙의 마지막 실행형 tool. `gateway_run_saved_report`(report_id)가 저장된 Text2SQL 리포트를 권한 검증 후 vibe/text2sql-preview로 실행. 접근 권한: 본인 작성(created_by==caller) 또는 팀 공유(visibility=team·approval=approved·team 일치)만 허용, 그 외 거부. 질문을 기존 /v1 파이프라인 in-process 재생으로 처리(거버넌스·검증 동일), preview 모드라 DB 미실행. 응답 텍스트만 반환. Gateway MCP 실행형 tool 4종(chat·run_skill·text2sql_preview·run_saved_report) 완비. 단위 테스트: tools/list 노출.
v0.58.5:- **워크플로 어드민 UI (v0.58.5)**: Workflow Chain 운영자 화면. 내비게이션 '워크플로'(#/workflows, admin:read) 추가, 기존 `/admin/workflows` 엔드포인트로 목록·생성(이름+허용팀+steps JSON)·dry-run(단계 해결 여부·이슈 모달)·삭제 제공. step 유형·한도 안내와 실행 호출 예시(`POST /v1/workflows/{id}/run` `{"execute":true,"input":...}`) 포함. UI 전용 증분(신규 엔드포인트/store 없음).
v0.58.4:- **Workflow 서버측 실행 엔진 (v0.58.4)**: Workflow Chain을 plan에서 실제 순차 실행으로 확장. `POST /v1/workflows/{id}/run` 본문에 `{"execute":true,"input":"..."}`를 주면 step을 순서대로 실행하며 각 step 출력을 다음 step 입력으로 연결. chat·skill·text2sql step은 기존 /v1 파이프라인 in-process 재생(runGatewayChat, 거버넌스·쿼터·정책·검증 동일 적용, step별 max_tokens·timeout 적용), approval은 일시정지(status=pending_approval + Mattermost 알림), condition은 입력 없으면 중단, transform은 통과, mcp_tool은 보류(skipped). 오류 시 중단(error_class=step_error). step 출력은 호출자에게만 반환, 저장은 집계 메타(workflow_runs: status·steps_ok·latency)만 — 원문 미저장. execute 미지정 시 기존 plan 동작 유지(하위호환). 단위 테스트: planWorkflow 제어 step 검증(chat/condition/approval/transform 해결·미지원 step 이슈·한도 echo). 남은 후속: mcp_tool 실행·run_saved_report·워크플로 빌더 UI.
v0.58.3:- **Gateway MCP 실행형 tool 확장 — skill·text2sql (v0.58.3)**: Gateway MCP Server에 실행형 tool 2종 추가. `gateway_run_skill`(skill·prompt)은 X-Skill 헤더로 등록 Skill을 적용해 chat 실행, `gateway_run_text2sql_preview`(question)는 vibe/text2sql-preview 가상 모델로 SQL 생성(미실행)을 수행 — 모두 기존 /v1 파이프라인 in-process 재생으로 인증·거버넌스·쿼터·Skill 정책·Text2SQL 검증이 동일 적용되고 호출자 권한으로 동작. runGatewayChat에 헤더 주입 지원 추가. 응답 텍스트만 반환. 단위 테스트는 v0.58.2 tools/list 커버. 남은 실행형 tool(run_saved_report)·서버측 워크플로 일괄 실행은 후속.
v0.58.2:- **Gateway MCP gateway_chat — 실행형 tool (v0.58.2)**: AI Gateway MCP Server에 첫 모델 실행형 tool 추가. `gateway_chat`(args: model·prompt 또는 messages)이 기존 `/v1/chat/completions` 파이프라인을 in-process(httptest recorder)로 재생해 실행 — 인증·거버넌스·쿼터·모델 allowlist·라우팅·로깅이 모두 동일하게 적용되고 호출자 권한으로 동작. assistant 응답 텍스트만 반환, stream=false. 별도 실행 경로/중복 거버넌스 없음(파이프라인 그대로 재사용). 기존 읽기 전용 tool과 함께 advertise. 단위 테스트: tools/list에 gateway_chat 노출 확인. 나머지 실행형 tool(run_skill·text2sql_preview·run_saved_report)은 후속.
v0.58.1:- **Gateway CLI — vibe (v0.58.1)**: 개발자/운영자가 터미널에서 Gateway를 쓰는 Go CLI(`cmd/vibe`). 명령: `vibe models`(/v1/models), `vibe chat -m MODEL PROMPT`(/v1/chat/completions), `vibe quota`·`vibe usage [WINDOW]`·`vibe route MODEL PROMPT`(Gateway MCP `/mcp/gateway` tools/call로 gateway_check_quota·gateway_get_usage_summary·gateway_route_preview), `vibe app run APP_ID`(/v1/apps/{id}/run), `vibe mcp config`(MCP 클라이언트 설정 JSON 생성). 설정은 플래그(--base·--key·--table) 또는 환경변수(VIBE_BASE_URL·VIBE_API_KEY), 출력은 JSON 기본·`--table` 사람용. stdlib만 사용(외부 의존성 없음), 읽기 전용 조회는 MCP 서버 재사용. 단위 테스트: 설정 해석(플래그>env>기본)·MCP 설정 생성·MCP 응답 텍스트 추출. 로드맵 5단계 — 사내 AI 업무 앱 플랫폼+MCP 제공 서버 로드맵 완료.
v0.58.0:- **Workflow Chain Orchestrator (v0.58.0)**: 단발 AI 호출이 아니라 업무 절차를 순차 자동화하는 워크플로 체인. 신규 `workflows`(steps JSON 임베드)·`workflow_runs` 테이블 + store CRUD/실행 이력. step 유형 chat·text2sql·mcp_tool·skill·condition·approval·transform, step별 안전 한도(timeout_ms·max_cost_krw·max_tokens·allowed_tools·allowed_tables). 관리자 `GET/POST/DELETE /admin/workflows`(생성 시 step 유형 검증), `POST /admin/workflows/{id}/dry-run`(단계 검증·해결 여부·한도 반환, 실행 안 함). 사용자 `POST /v1/workflows/{id}/run`(팀 게이팅 재검증 → run 기록 status=planned, 미해결 step은 error_class=step_unresolved → plan+run_id 반환). 실행 결과는 status·steps_total/ok·latency·cost·error_class만 저장(원문 prompt/SQL/tool args 미저장). 사용자 `GET /me/workflow-runs?workflow_id=&limit=`(사용자 격리). 서버측 step 일괄 실행은 후속(현재 검증·기록·플랜). work_apps/ai_app_runs 패턴 재사용. OpenAPI 반영. 단위 테스트: steps JSON roundtrip·한도 보존·run 기록·사용자 격리. 로드맵 2단계.
v0.57.3:- **AI 업무 앱 실행 이력 — ai_app_runs (v0.57.3)**: 1단계 AI 업무 앱 카탈로그 보강. 기존 사용자용 카탈로그(`/v1/apps` 조회·상세, `POST /v1/apps/{id}/run` 권한 재검증+실행 플랜)는 이미 제공되었고, 누락된 실행 이력 영속화를 추가. 신규 `ai_app_runs` 테이블 + store(RecordAIAppRun·ListAIAppRun) — 안전 메타데이터만(input_hash·output_summary·error_class·status·latency·cost, **원문 입력/출력 미저장**). `/v1/apps/{id}/run`이 실행 시 run을 기록(status=planned, 입력 해시, 미해결 컴포넌트는 error_class=component_unresolved)하고 run_id 반환. 신규 `GET /me/app-runs?app_id=&limit=`로 본인 실행 이력 조회(사용자 격리). OpenAPI 반영. 단위 테스트: 기록·앱 필터·사용자 격리. 로드맵 1단계.
v0.57.2:- **사용자 온보딩 팩 생성기 (v0.57.2)**: 개발자가 클라이언트별 연결 설정을 자동으로 받는 온보딩 팩. `GET /me/onboarding-pack?client=mcp|cursor|roo|cline|openai-sdk`이 base URL(요청 origin·X-Forwarded 반영), 사용 가능 모델(가격 카탈로그·키별 allow/deny 필터), 권장 모델(vibe/auto), scope, 예산, MCP 설정 JSON, OpenAI SDK snippet, 샘플 curl을 반환. 인증은 Proxy API Key 또는 JWT 재사용(authenticateProxyContext/currentAccessClaims), API Key secret은 재노출하지 않고 <YOUR_API_KEY> 자리표시자만 제공. 신규 store 없음. UI는 v0.57.1 '내 개발도구 연결하기(MCP)' 카드로 커버, 본 엔드포인트는 클라이언트·CLI(5단계)용 API 산출물. OpenAPI 반영. 단위 테스트: requestOrigin(X-Forwarded-Proto/Host 우선). 로드맵 3단계.
v0.57.1:- **Gateway MCP UI — 관리자 탭·사용자 연결 카드 (v0.57.1)**: AI Gateway MCP Server(v0.57.0)의 UI. 신규 `GET /admin/gateway-mcp/info`(admin)가 tool/resource/prompt 카탈로그와 엔드포인트·프로토콜 버전을 반환. 관리자 내비게이션 'Gateway MCP' 탭(연결 설정 + 복사 버튼·tools/resources/prompts 표). 사용자 '내 홈'에 '내 개발도구 연결하기(MCP)' 카드 추가 — Claude/Cursor/Roo/Cline용 mcpServers 설정 JSON(현재 origin + /mcp/gateway, Bearer 자리표시자)과 복사 버튼, 내 키 발급 안내. 신규 store/실행 로직 없음(기존 def 함수 재사용). OpenAPI 반영. 단위 테스트는 v0.57.0 dispatch 테스트로 카탈로그 커버. 로드맵 4단계 UI.
v0.57.0:- **AI Gateway MCP Server — Gateway 자체 MCP 노출 (v0.57.0)**: vibe-coders를 "MCP 중계 Gateway"를 넘어 "MCP로 호출 가능한 Gateway"로 확장. 신규 엔드포인트 `POST /mcp/gateway`(기존 `/mcp` 업스트림 집약과 별개·비충돌)가 MCP JSON-RPC 2.0(initialize·ping·tools/list·tools/call·resources/list·resources/read·prompts/list·prompts/get)로 vibe-coders 자체 기능을 노출. 1단계는 읽기 전용 안전 tool 7종: gateway_list_models(가격·권한 필터)·gateway_estimate_cost·gateway_check_quota·gateway_route_preview(planIntelligentRouting, 업스트림 미호출)·gateway_list_skills(팀 권한)·gateway_explain_request(본인 요청 영수증, 소유권 검증)·gateway_get_usage_summary. resources 5종(gateway://models·skills·usage/me·quota/me·onboarding), prompts 5종(use_gateway_safely·analyze_my_usage·choose_best_model·run_text2sql_report·create_ai_app_request). 인증은 기존 Proxy API Key 재사용(authenticateProxyContext), 모든 tool은 호출자 권한(AllowedModels·팀)으로 실행, 호출은 기존 logMCPCall로 request/audit 연결, 원문 args 미저장(hash만). 기존 JSON-RPC 타입·생성자 재사용, 신규 store 없음. Claude Desktop·Cursor·Roo·Cline 연결용 onboarding 가이드 리소스 포함. OpenAPI 반영. 단위 테스트: initialize·tools/list·resources/list·prompts/get·미지원 메서드(-32601). 모델 실행형 tool(gateway_chat·run_skill·text2sql_preview·run_saved_report·create_app_run)은 다음 증분으로 분리. 로드맵 4단계.
v0.56.1:- **Sensitive Workflow Sandbox — 민감 워크플로 샌드박스 (v0.56.1)**: 고위험 요청을 실제 실행 없이 안전하게 검증하는 격리 프리뷰. `POST /admin/sandbox/preview {kind,model,team,content,sql,server,tool}`이 업스트림 호출·도구 실행·Text2SQL 실행·저장 없이 기존 안전 게이트만 통과시켜 would_block 여부와 항목별 결과를 반환 — 프롬프트 인젝션(detectPromptInjection 심각도≥70 차단), secret 탐지(유형·건수, 원문 미저장), 정책 결정(evaluatePolicyRules 재생), Text2SQL 검증(ValidateSQL: SELECT-only/위험구문/LIMIT — SQL 미반향), MCP 도구 위험(ToolRiskProfile action=block 차단). 입력 원문 미저장, 결과만 반환. 신규 검출 로직 없이 기존 게이트 재사용. 내비게이션 '민감 샌드박스'(#/sandbox, security) + Admin UI(입력 폼·항목별 결과). OpenAPI 반영. 단위 테스트: 위험 SQL 차단·정상 SELECT 통과(서버 통합). 12개 개선 아이디어 #10 — **12개 전부 완료**.
v0.56.0:- **AI App Template Catalog — 업무 앱 템플릿 (v0.56.0)**: 자주 쓰는 업무 패턴을 시작 템플릿으로 제공해 "프롬프트 입력"이 아닌 "업무 앱 실행" 경험을 제공. 내장 템플릿 카탈로그(코드 리뷰·데이터 질의·문서 작성·장애 분류·PR 요약 — 코드 정의, Capability Registry 방식)를 `GET /admin/app-templates`로 조회하고, `POST /admin/app-templates/instantiate {key,title?,allowed_teams?}`로 편집 가능한 실제 WorkApp(active)을 생성(기존 CreateWorkApp 재사용). 각 템플릿은 추천 구성 요소(skill·model·prompt_product·text2sql_report·mcp_tool)를 포함하며, 생성 후 AI 업무 앱 화면에서 ref를 환경에 맞게 편집. 신규 테이블 없음. 내비게이션 '앱 템플릿'(#/app-templates, admin:read) + Admin UI(템플릿 카드·원클릭 생성). OpenAPI 반영. 단위 테스트: 카탈로그 무결성·조회. 12개 개선 아이디어 #2.
v0.55.9:- **Prompt Debt 관리 — 프롬프트 부채 (v0.55.9)**: 반복되는 저품질·고비용·모델낭비·고볼륨 프롬프트를 "부채"로 점수화해 개선 우선순위를 제시. `GET /admin/prompts/debt?window=30d&min_requests=&limit=`이 기존 PromptFingerprints 클러스터에 부채 점수(실패율 55%·상대비용 25%·볼륨 10%·모델낭비 +10 가중, fleet 중앙값 정규화)와 유형(failing/model_waste/expensive/high_volume)·권장 조치를 매겨 높은 순으로 반환. 신규 store 없이 PromptFingerprints 재사용, 원문은 마스킹된 샘플만. 순수 함수 promptDebtScore로 분류. 내비게이션 '프롬프트 부채'(#/prompt-debt, admin:read) + Admin UI(부채 우선순위 표·유형 배지·권장 조치). OpenAPI 반영. 단위 테스트: 유형 분류(실패>모델낭비>고비용>고볼륨>minor)·실패가 정상보다 높은 점수. 12개 개선 아이디어 #5.
v0.55.8:- **AI Cost Chargeback Pack — 월별 비용 배부 팩 (v0.55.8)**: 사내 비용 정산을 위해 팀·프로젝트·비용센터별 월별 비용 배부를 한 패키지로 산출. `GET /admin/cost/chargeback-pack?month=YYYY-MM&dimensions=cost_center,project,team`이 KST 월 경계로 차원별 요청·토큰·비용·오류를 집계해 반환(차원별 합계 포함). `?format=csv`로 전체 차원을 한 CSV(month,dimension,key,...)로 다운로드. 신규 store `CostAllocationWindow`(기존 CostAllocation 확장 — **team 차원 추가**(api_keys join) + **상한 시각 바운드**로 특정 월만 집계). 기존 CostAllocation/인보이스와 비충돌(별도 함수). 내비게이션 '비용 배부 팩'(#/chargeback, billing, admin:read) + Admin UI(월 선택·차원별 표·CSV). OpenAPI 반영. 단위 테스트: team 차원·월 윈도우 경계(이전/이후 제외)·미지원 차원 오류. 12개 개선 아이디어 #9.
v0.55.7:- **Skill Dependency Graph — Skill 의존성 그래프 (v0.55.7)**: Skill·모델·도구·팀·정책 간 의존성을 그래프로 보여 변경 시 영향 범위를 파악. `GET /admin/skills/dependency-graph[?skill=]`가 production Skill의 선언적 의존성(AllowedModels·AllowedTools·AllowedTeams)을 노드/엣지로 만들고, 각 모델·도구를 관할하는 활성 정책(rule conditions.model/mcp_tool, actions.deny_models/allow_models)을 transitive로 연결해 "이 모델/도구/정책을 바꾸면 어떤 Skill이 영향받는가"를 보여줌. 신규 store 없이 ListSkills·ListPolicies 재사용(listMatchesAny/containsFold로 glob 매칭). 응답: nodes[{id,type,label,risk}]·edges[{from,to,kind}]·skills[{models,tools,teams,governing_policies}]. 내비게이션 'Skill 의존성'(#/skill-graph, admin:read) + Admin UI(Skill별 의존성·관할 정책 카드). OpenAPI 반영. 단위 테스트: 의존성 파싱·노드 생성(서버 통합). 12개 개선 아이디어 #8.
v0.55.6:- **AI Usage Narrative Report — 월간 운영 보고서 (v0.55.6)**: 숫자 대시보드가 아니라 임원 보고용 월간 서술 보고서를 자동 생성. `GET /admin/reports/narrative?window=30d`가 기존 집계를 합성해 한국어 prose 섹션(요약·팀 활동·품질·안전 거버넌스·권고)을 생성 — 사용량/비용(직전 동일기간 대비 증감 %), 비용 상위 팀, 모델 평균 품질·최고/최저, 정책 차단·secret 탐지·비용 이상징후 건수, 수치 기반 자동 권고. `?format=md`로 마크다운 보고서 다운로드. 신규 store 쿼리 없이 UsageSince·ListTeams/TeamDashboardSince·ModelQualityScores·ListPolicyDecisionEventsFiltered·ListSecretEventsFiltered·CostAnomalies 재사용. 헬퍼 pctDelta(부호 보존 — round1이 음수를 0으로 clamp하는 문제 회피)·commaInt(천단위·음수 반올림 정확). 내비게이션 '운영 보고서'(#/narrative, admin:read) + Admin UI(서술 섹션·MD 다운로드). OpenAPI 반영. 단위 테스트: pctDelta·signedPct·commaInt. 12개 개선 아이디어 #12.
v0.55.5:- **Gateway Policy Advisor — 정책 추천 (v0.55.5)**: 운영자가 정책을 직접 발명하지 않도록 로그 근거로 거버넌스 규칙을 추천. `GET /admin/policy-advisor/suggestions`가 신호를 분석해 즉시 적용 가능한 규칙(정책 엔진과 동일한 conditions/actions 어휘)을 제안 — 모델 비용 급증(CostAnomalies, scope=model)→해당 모델 require_approval, secret detect-only 다발(ListSecretEventsFiltered)→contains_secret→secret_action=block, MCP 도구 오류 급증(ListMCPTools)→해당 mcp_tool require_approval. 각 추천에 근거(evidence)·심각도·rationale 포함, 이미 활성 규칙이 커버하는 항목은 제외(ruleCoversModel/ruleCoversMCPTool/ruleHasSecretBlock dedup). `POST /admin/policy-advisor/apply`는 추천을 **비활성(draft) 정책**으로 생성(UpsertPolicyWithRules, enabled=false)해 관리자가 정책 화면에서 검토 후 활성화 — 자동 시행 안 함, audit 기록. 내비게이션 '정책 어드바이저'(#/policy-advisor, admin:read) + Admin UI(추천 카드·draft 생성). OpenAPI 반영. 단위 테스트: dedup 헬퍼. 12개 개선 아이디어 #11.
v0.55.4:- **Model Contract Test — 모델 품질 계약 (v0.55.4)**: 운영 자동조치 5단계(마지막). 모델 교체·자동 라우팅 변경·MCP agentic 모델 변경 전에 업무별 최소 품질 계약을 검증해 안정성 확보. 신규 `model_contracts` 테이블 + CRUD: 업무유형별 임계값(최소 품질점수·골든 통과율·성공률, 최대 지연·평균비용; 0=미적용). `GET/POST/DELETE /admin/models/contracts`로 계약 관리, `POST /admin/models/contracts/run {model, contract_id?}`이 모델의 관측 지표(ModelQualityScores·ModelStats·신규 ModelAvgCost)를 계약 임계값과 비교해 차원별 pass/warn/fail(임계 5% 이내=warn)·계약별 verdict·교체 가능 여부를 반환. 실패 골든 샘플은 원문 없이 fingerprint(해시)+사유만(신규 store FailedGoldenForModel). 계약 없음/지표 부족 시 교체 안전 미보장. 내비게이션 '모델 계약'(#/model-contracts, admin:read) + Admin UI(계약 목록·임계값 폼·모델 검증 실행). OpenAPI 반영. 단위 테스트: evalMin/evalMax 경계·verdict 축약. 사용자 제품화/운영 자동조치 로드맵 5단계 완료.
v0.55.3:- **Team AI Scorecard — 팀 성숙도 점수 (v0.55.3)**: 운영 자동조치 4단계. 단순 사용량 집계를 넘어 팀별 AI 활용 성숙도를 경영 지표로 점수화. `GET /admin/teams/scorecard?window=30d`가 활성 팀마다 8개 차원(비용효율·성공률·캐시율·Skill 재사용률·MCP 성공률·Text2SQL 성공률·정책 준수율·만족도)을 0~100으로 산출하고 가용 차원 평균으로 종합 점수·등급(A≥85·B≥70·C≥55·D)을 매김. 비용효율은 요청당 비용의 fleet 중앙값 대비 상대 점수, 데이터 없는 차원은 -1로 표기하고 평균에서 제외. 기존 집계 재사용(TeamDashboardSince·TeamPopularSkills·TeamMCPTools·ListPolicyDecisionEventsFiltered·SkillFeedbackStats) + 신규 store `TeamQualityExtras`(팀 캐시율·Text2SQL valid 비율). `?format=csv`로 CSV 다운로드, 기본 JSON은 월간 보고서로 활용. 내비게이션 '팀 성숙도'(#/scorecard, admin:read) + Admin UI 표(정렬·CSV). OpenAPI 반영. 단위 테스트: 결측 차원 제외 평균·등급 경계·중앙값/clamp. 민감 원문 미저장.
v0.55.2:- **Auto Remediation Playbook — 운영 자동조치 (v0.55.2)**: 운영 자동조치 3단계. Incident Copilot이 보던 신호 위에 실행 가능한 조치 후보를 얹음. `GET /admin/remediation/playbooks`가 현재 상황(프로바이더 장애·비용 급증·MCP 오류 급증·Text2SQL 위험 급증·정책 위반 급증 + 비상 차단)을 같은 신호원(ProviderHealthScores·CostAnomalies·ToolMetricsSince/ListMCPTools·RiskyText2SQLLogs·PolicyDecisionEvents)에서 도출하고, 각 조치를 dry-run 설명·예상 영향·가역 여부와 함께 제시. `POST /admin/remediation/apply`가 승인된 조치를 실행(관리자 인증=승인 게이트) — 화이트리스트 가역 토글만: 게이트웨이 긴급정지/재개(SetFlag), Text2SQL 중지/재개(t2sKilled), 프로바이더 비활성/활성(UpsertProvider.Enabled), MCP 도구 차단/허용(UpsertToolRiskProfile). 적용 전 상태 캡처·audit 기록·rollback 디스크립터 반환, dry_run 지원. 예산/정책 등 금전·라우팅 변경은 권고(executable=false)로만 노출. 내비게이션 '자동 조치'(#/remediation, admin:read) + Admin UI(상황별 카드·Dry-run/적용/되돌리기). OpenAPI 반영. 단위 테스트: 실행 화이트리스트·worst MCP 도구 선정. 기존 안전 mutator만 재사용(신규 변경 메커니즘 없음).
v0.55.1:- **Request Receipt — 요청 영수증 (v0.55.1)**: 사용자 제품화 2단계. 사용자가 본인 요청 1건의 처리 내역을 안전하게 확인하는 영수증. `GET /me/requests`가 본인 최근 요청(모델·provider·상태·토큰·비용·캐시 — 안전 메타데이터만)을 나열하고, `GET /me/requests/{id}/receipt`가 모델·provider·토큰(입출력/캐시)·비용·캐시 적중·라우팅 이유 요약(요청→선택 모델·이유·폴백·위험/복잡도 tier)·정책 결과(차단 여부·규칙·사유)·Skill/MCP 사용 여부(라벨만)를 반환. 원문 prompt·SQL·tool 인자·관리자 trace는 제외. 소유권 검증: 요청의 api_key.user_id가 호출자와 일치해야 함(불일치 403, 미존재 404). 신규 store `RequestUserID`(api_key→user 해석)·`UserRecentRequests`(본인 요청 요약). 기존 RequestDetail/RoutingDecisionByID/PolicyDecisionEventsForRequest/ToolsForRequest 재사용. 내 홈에 '최근 요청 / 영수증' 카드 + 영수증 모달. OpenAPI self-service 반영. 단위 테스트: 소유권 해석·미존재 빈값·사용자 격리. 사용자 제품화/운영 자동조치 로드맵 2단계.
v0.55.0:- **User AI Portal 고도화 — 내 홈 카드 확장 (v0.55.0)**: 사용자 제품화 1단계. 일반 사용자가 어드민 도움 없이 본인 AI 사용을 파악하도록 `/me/dashboard`에 두 카드를 추가. `recent_blocks`: 본인 요청의 최근 정책 차단(규칙·사유·모델·시각 — 원문 prompt 미노출, ListPolicyDecisionEventsFiltered{UserID,Decision:block} 재사용)으로 "왜 막혔나"를 직접 확인. `my_saved_reports`: 본인이 저장한 Text2SQL 리포트(이름·스키마·유형·공개범위·승인상태 — 원문 SQL 미노출), 신규 store `ListText2SQLSavedReportsByCreatedBy(createdBy)`. 본인 데이터만(meUserID 기준). 내 홈(renderMeHome)에 '최근 차단 사유'·'내 저장 리포트' 카드 추가. 단위 테스트: created_by 필터 격리. 사용자 제품화/운영 자동조치 로드맵 1단계.
v0.54.8:- **Data Product Builder — 데이터 상품 빌더 (v0.54.8)**: 자주 쓰는 데이터 자산(저장 리포트·DW 메트릭·골든 쿼리)을 이름·설명·소유자·팀 범위·민감도를 갖춘 재사용 가능한 데이터 상품으로 큐레이션/발행하고, 팀이 탐색·접근 요청하는 카탈로그. 신규 `data_products`(product_key·source_type[saved_report|metric|golden_query|custom]·source_ref 참조만, **원문 SQL 미저장**·version 자동 증가)와 `data_product_access_requests` 테이블 + store CRUD/요청 처리. `GET/POST/DELETE /admin/data-products`(발행=status), `GET /admin/data-products/candidates`(반복 Text2SQL 질문→상품 후보 분류, 원문 SQL 미노출), `GET|POST /admin/data-products/requests`(접근 요청 승인/거부) — 모두 admin 인증. 사용자용 `GET /me/data-products`(본인 팀에 보이는 발행 상품+요청 상태), `POST /me/data-products/{key}/request-access`. 스킬 마켓플레이스 패턴 재사용(dataProductVisibleToTeam: 허용 팀 빈칸=전체). 내비게이션 '데이터 상품'(#/data-products, admin:read) + Admin UI 한국어 페이지(renderDataProducts: 카탈로그·후보·요청). OpenAPI 반영. 단위 테스트: roundtrip·published 필터·version 증가·요청 처리. 운영 플랫폼화 로드맵 8단계.
v0.54.7:- **Team Self-Service Portal — 팀 셀프서비스 포털 (v0.54.7)**: 팀 리드가 admin 권한 없이 본인 팀 현황을 한 화면에서 보는 통합 포털. `GET /team/portal[?window=]`이 team:read 스코프로 본인 팀 데이터만(admin은 ?team=로 임의 팀) 집계 — 사용량(요청·비용·성공률), 팀 예산 소진(월 예산·사용·소진율·예상 월말·소진 예정일), 팀 API 키(공개 projection만, 비밀값 절대 비노출), 사용 가능한 production Skill, 대기 중 Skill 접근 요청, 팀원 명단. 기존 resolveTeamScope/TeamDashboardSince/BudgetStatuses/ListAPIKeys/ListSkills/skillVisibleToTeam 재사용(신규 쿼리 없음). 내비게이션에 '팀 포털'(#/team-portal, team 그룹) 추가, Admin UI 한국어 페이지(renderTeamPortal). OpenAPI self-service 반영. 단위 테스트: 예산 집계·secret 미노출·팀 미지정 400. 운영 플랫폼화 로드맵 9단계.
v0.54.6:- **Policy Regression Test — 정책 회귀 테스트 (v0.54.6)**: 정책 변경이 의도치 않게 판단(allow/block/require_approval)을 뒤집는지 막기 위한 회귀 스위트. 고정 입력 시나리오(model·provider·team·role·complexity/risk·secret 여부·MCP 등 명시 속성만, 원문 prompt/SQL/tool args 미저장)와 기대 결과를 `policy_regression_cases` 테이블에 저장. `GET/POST/DELETE /admin/policies/regression/cases`로 케이스 CRUD, `POST /admin/policies/regression/run`이 현재 활성 정책(또는 inline rules·policy_id)에 순수 함수 `evaluatePolicyRules`로 재생해 케이스별 PASS/FAIL·실제 결과·사유와 통과/실패 집계를 반환. ExpectSecretAction 지정 시 secret_action(detect/mask/block)도 비교. 신규 store CRUD + dialect 호환. Secret Firewall 위 보안 화면에 '정책 회귀 테스트' 카드(케이스 추가 폼·전체 회귀 실행 버튼·결과 표) 추가. 단위 테스트: store roundtrip·decisionOutcome·evaluatePolicyRules 재생. 운영 플랫폼화 로드맵 6단계.
v0.54.5:- **Upgrade Preflight — 배포 안정성 점검 (v0.54.5)**: 폐쇄망 배포 전/후 안정성을 확인하는 읽기 전용 프리플라이트. `GET /admin/ops/preflight`가 DB 도달성(Ping), 핵심 테이블 존재(마이그레이션 적용 여부 — request_logs·api_keys·auth_sessions·admin_settings·skills·multi_model_test_runs·metric_catalog·change_sets·work_apps), OpenAPI 스펙 유효성(paths 존재), 기본 업스트림 설정, secret cipher 초기화, Auth 시크릿 구성을 점검해 항목별 status(ok/warn/fail)·상세와 종합 상태·버전을 반환. 신규 store `TableExists`(dialect-aware). 운영 홈 워커 표 하단에 '배포 프리플라이트 점검' 버튼·결과 표 추가. fail 시 배포 보류/롤백 검토 안내. 운영 플랫폼화 로드맵 10단계.
v0.54.4:- **MCP Tool Trust Score (v0.54.4)**: MCP 도구가 늘어날수록 신뢰도 관리가 필요해, 도구별 신뢰 점수를 산출. `GET /admin/mcp/trust-scores?days=`가 최근 N일 도구 통계(ListMCPTools: 호출·오류·오류율·고유 사용자)와 위험 등급(ToolRiskProfile)을 결합해 점수=100−오류율×50−위험도 패널티(critical −35/high −25/medium −10)와 등급(A≥85·B≥70·C≥50·D)을 계산, 신뢰도 낮은 순 정렬. 호출<10건은 confidence=low(표본 부족) 표기. MCP 운영 화면에 'MCP Tool 신뢰 점수' 표 추가(점수·위험도·호출·오류율·사용자). 운영 플랫폼화 로드맵 7단계.
v0.54.3:- **Skill Marketplace — 사용자 Skill 카탈로그 (v0.54.3)**: Skill을 관리자 기능이 아니라 사용자가 발견·요청·평가하는 자산으로 확장. `GET /me/skills`(내 팀이 쓸 수 있는 production Skill + 요청 가능한 Skill, 30일 실행수·성공률·사용자수·만족도 포함), `POST /me/skills/{name}/request-access`(접근 요청), `POST /me/skills/{name}/feedback`(1~5점·코멘트). 관리자 `GET /admin/skills/adoption`(스킬별 실행수·distinct 사용자·실패율·평균비용·만족도·대기 접근요청). 신규 테이블 `skill_access_requests`·`skill_feedback`. 내 홈(/me)에 '내가 사용 가능한 Skill' 카드 추가(평가·접근 요청 버튼, 다른 팀 전용 Skill은 요청 가능 목록으로 분리). 기존 SkillRunStats 재사용. 운영 플랫폼화 로드맵 5단계.
v0.54.2:- **Incident Copilot — 지금 확인할 이슈 (v0.54.2)**: 관측 데이터가 많아 "무엇을 먼저 봐야 하는지" 판단이 분산되던 문제를 해결. `GET /admin/incidents/candidates`가 provider health·cost anomaly·MCP 오류율·Text2SQL 차단·ClickHouse 적재 지연·이상탐지 이벤트를 종합해 **임계치를 넘은 신호만** 장애 후보로 추출하고 심각도(critical/warning/info)·근거 지표·추천 조치·관련 링크를 제공, 심각도순 정렬. (항상 모든 카드를 보여주는 운영 홈과 달리 실제 문제만 노출.) 운영 홈 상단에 '지금 확인할 이슈' 카드를 추가(없으면 ✅ 표시). 운영 플랫폼화 로드맵 4단계.
v0.54.1:- **Change Impact Simulator — 변경 영향도 사전 추정 (v0.54.1)**: 설정/정책/라우팅 변경을 적용하기 전에 과거 트래픽으로 영향을 추정. `POST /admin/change-impact/simulate {change_type, days, params}`가 모델 일별 집계(analytics_daily, 최근 N일)를 기준으로 dry-run 추정 — **block_model**(패턴 매칭 모델 호출 차단 시 차단 요청 수·제거 비용·차단율·매칭 모델), **model_price**(blended 단가 비율로 해당 모델 비용 델타), **route_remap**(원본→대상 모델 재라우팅 시 영향 요청·비용 델타). 현재 가격표(pricingMap)로 비용 비율 산출, 영향 모델의 최근 요청 id 샘플(원문 없이) 포함. 원문 prompt/SQL/tool args 미사용. 변경 세트 화면에 '변경 영향도 시뮬레이터' 패널 추가(유형·기간·파라미터 입력 후 기준 대비 영향 표시). 운영 플랫폼화 로드맵 2단계. (정책 phase 예외·토큰 입출력 분리는 근사 — 노트 표기. policy/quota/MCP allowlist 유형은 후속.)
v0.54.0:- **Capability Registry — 기능 맵 (v0.54.0)**: 비대해진 기능을 한눈에 이해하도록, 주요 기능을 capability_key 기준으로 카탈로그화. 각 capability는 이름·설명·그룹·관련 API·UI 탭·필요 권한·설정키·DB 테이블·워커를 가짐. `GET /admin/capabilities`(목록+그룹 카운트)·`GET /admin/capabilities/{key}`(상세) 추가. chat_proxy·routing·mcp_gateway·text2sql·governance·skill_studio·prompt_assets·prompt_lab·multimodel_console·clickhouse_dw·okf·personalization·work_apps·runtime_settings·sso·ops_visibility 16개 등록. 운영 메뉴에 '기능 맵' 탭 추가(그룹별 카드로 API·UI·권한·설정·테이블·워커 표시). 시스템 메타데이터이므로 코드 정의 레지스트리(capabilities.go)로 구현 — 기능 추가 시 함께 갱신. 운영 플랫폼화 로드맵 1단계.
v0.53.8:- **CI 코드 품질 게이트 강화 (v0.53.8)**: 기존 golden-regression 외에 종합 CI 워크플로(`.github/workflows/ci.yml`)를 추가 — PR/푸시마다 gofmt(클린 강제)·go vet·go build·go test·**go test -race**(레이스 검출 + 모든 SQL 마이그레이션을 store 테스트로 실행)·govulncheck(취약점 스캔, 현재는 advisory)·docker build·OpenAPI 스펙 검증(부팅 후 /openapi.json 유효 JSON·paths 존재 확인)을 게이트로 수행. go 버전은 go.mod(go-version-file)로 추적해 드리프트 방지. 운영 가시성 로드맵 9순위.
v0.53.7:- **폐쇄망 Swagger — 오프라인 API 탐색기 (v0.53.7)**: 기존 `/swagger`는 CDN(unpkg)에서 Swagger UI 자산을 받아 폐쇄망에서 렌더링이 실패할 수 있었음. `/swagger?offline=1`로 외부 CSS/JS/폰트 의존이 전혀 없는 자체 완결 API 탐색기를 제공 — `/openapi.json`을 fetch해 태그별로 오퍼레이션을 그룹화하고 method·경로·요약·파라미터(이름/위치/타입/필수)·응답 코드를 인라인 렌더링하며 경로/요약 필터를 지원. 온라인 Swagger 페이지 상단 안내에 '오프라인 API 탐색기' 링크 추가. 운영 가시성 로드맵 8순위(폐쇄망 Swagger 내장).
v0.53.6:- **운영 워커 상태판 (v0.53.6)**: 백그라운드 워커들의 런타임 상태를 한눈에. `GET /admin/ops/workers`가 async logger(큐 깊이·유실·written), 요청 fact 인입 큐(len/cap·dropped), ClickHouse rollup sink(설정/실행 여부), fact 재시도 백로그(대기 배치 수), retention(주기·last_run), Text2SQL 리포트 스케줄러의 상태(ok/warn/critical/idle)·running·queue_depth·dropped·last_run·detail를 반환하고 전체 상태를 종합. 운영 홈 하단에 '백그라운드 워커' 표를 추가(워커·상태·실행·큐·유실·상세). 워커별 장애 원인 분해·큐 적체 조기 감지 지원. 운영 가시성 로드맵 6순위.
v0.53.5:- **AI Gateway 운영 홈 — 오늘 봐야 할 것 (v0.53.5)**: 관리자가 장애·비용·보안·품질 우선순위를 한 화면에서 보는 운영 홈. `GET /admin/ops/home`이 최근 24h 신호를 6개 카드로 집계 — 비용 폭주(cost anomaly z≥3 상향), Provider 상태(health score<70 degraded), 위험 Skill(production 스킬 중 보안 high 발견), MCP 실패(tool 오류율), Text2SQL 차단(거부 쿼리 수), ClickHouse 적재(재시도 대기 배치). 각 카드는 status(ok/warn/critical/unknown)·값·상세·링크를 제공하고 전체 상태를 종합. 운영 메뉴 최상단에 '운영 홈' 탭 추가(상태별 색상 카드, 클릭 시 상세 화면 이동). 기존 store 집계(CostAnomalies·ProviderHealthScores·ListSkills+scanSkillSecurity·ToolMetricsSince·RiskyText2SQLLogs·CountClickHouseFactRetries) 재사용. 운영 가시성 로드맵 5순위.
v0.53.4:- **DW Metric Catalog — 표준 지표 사전 (v0.53.4)**: ClickHouse fact를 운영자가 이해 가능한 표준 지표로 관리. 신규 테이블 `metric_catalog`(metric_key·name_ko·description·query_template·dimensions·owner·sensitivity·enabled·version). `GET/POST /admin/dw/metrics`·`DELETE /admin/dw/metrics/{id}`·`POST /admin/dw/metrics/{id}/validate`. **정적 검증**: SELECT/WITH 전용·쓰기/DDL 차단·참조 테이블이 표준 fact(ai_*)인지 확인(아니면 경고)·민감 컬럼/원문(prompt_text·response_text·secret·api_key·client_ip 등) 참조 차단. 검증 실패한 쿼리는 enabled로 저장 불가, upsert 시 version 자동 증가. DW 대시보드에 '지표 사전' 서브탭 추가(지표 목록·민감도/활성/owner/버전·검증 결과·새 지표 정의 폼). 운영 가시성 로드맵 4순위. (ClickHouse 실제 dry-run·비용 추정은 후속.)
v0.53.3:- **Skill Studio 준비도 — 모델 적합성 게이트 반영 (v0.53.3)**: Skill Studio 마법사의 production 준비도 체크리스트(`GET /admin/skill-studio/readiness`)에 v0.52.3 모델 적합성 게이트를 추가. high 위험도(또는 require_model_fitness) 스킬은 멀티모델/Golden/테스트케이스 통과 근거 N건 이상을 readiness 항목으로 표시하고, 충족 전까지 `production_ready=false`로 '프로덕션 승격' 버튼을 비활성화. 응답에 `fitness_required`·`fitness_passing`·`fitness_threshold` 추가. 이전에는 readiness가 통과로 보여도 실제 promote가 게이트로 차단되던 불일치를 해소(운영 가시성 로드맵 3순위 Skill Studio 검증 환경 보강). 또한 시간대 경계(00:00–02:00 UTC)에서 깨지던 팀 대시보드 테스트의 cutoff 기준을 자정으로 고정.
v0.53.2:- **MCP Agentic Loop Timeline (v0.53.2)**: `vibe/grounded`·`vibe/research`·`vibe/all-mcp` 요청의 도구 탐색 과정을 사람이 검증할 수 있게 시각화. `GET /admin/mcp/agentic-runs?request_id=`가 저장된 domain routing 결정/시그널·tool invocations·MCP route 결정을 재구성해 후보 도구 점수(selector)·증거 점수(evidence)·evidence gate·라우팅 결정(route·신뢰도·증거 수·reason)·tool 호출(서버/도구/단계/오류/arg hash)·tool 정책·위험·조치를 반환. **tool arguments 원문은 미저장(arg hash만)**. 요청 상세 모달에 '🔧 MCP Agentic' 버튼을 추가해 후보/증거 점수 막대·tool 호출표·정책표로 표시. 운영 가시성 로드맵 2순위. (원시 selector relevance·stopping reason 등 일부 신호는 현재 미적재 — 후속 계측 대상.)
v0.53.1:- **Gateway Flow Map — 요청 처리 흐름 가시화 (v0.53.1)**: 한 요청이 인증→한도→Skill→거버넌스→라우팅→캐시→MCP→Text2SQL→업스트림→DW 적재까지 어떻게 흘렀는지 한 화면에서 설명. `GET /admin/flow-map?request_id=`가 기존 적재 데이터(RequestDetail·RoutingDecisionByID·PolicyDecisionEvents·Tools·MCPRouteDecisions·Text2SQLSpans·SecretEvents·Approvals)를 재구성해 단계별 `status`(ok/blocked/warn/fallback/skip/error)·`latency_ms`·`decision`·`reason`·`linked_artifacts`(ids/hash/count만)를 반환. **프롬프트·SQL·tool args 원문은 미포함**. 요청 상세 모달에 '🗺️ 처리 흐름' 버튼을 추가해 단계 카드(아이콘·상태 배지·근거·아티팩트)로 표시. 장애 분석·감사·문의 대응 시간 단축(운영 가시성 로드맵 1순위). Skill은 요청 단위 연결이 없어 skip, 캐시는 cached token 신호 기반.
v0.53.0:- **내 추천 모델 — 팀 우승 모델 결합 (v0.53.0)**: `/me/recommended-models`에 사용자 팀의 최근 90일 멀티모델 평가 우승 모델(team_winners: 우승 수·평균 점수 top 3)을 추가. `MultiModelJudgementRows`를 팀·기간으로 조회해 run별 최고 점수 모델을 집계(섹션 5의 team winners). 내 홈 '내 업무 추천 모델' 카드에 '팀 멀티모델 우승 모델 (90일)' 항목(🏆 모델·우승 횟수·평균 점수)을 추가해, 작업유형 추천·내 모델 태그와 함께 팀 검증 결과 기반 추천을 제공.
v0.52.9:- **멀티 모델 리더보드 (v0.52.9)**: 저장된 자동 평가(judge) 결과를 모델별로 집계해 "어떤 모델이 실제 테스트에서 계속 이기는지"를 보여줌(섹션 4 분석). `GET /admin/chat-test/multi-run/leaderboard?team=&days=` — judgement를 run과 조인(`MultiModelJudgementRows`, 팀·기간 필터)해 모델별 출전 수·평균 점수·통과율·**우승 수**(해당 run에서 최고 점수를 받은 횟수)를 산출하고 우승→평균순 정렬. Chat 테스트 화면에 '모델 리더보드 (최근 90일)' 표 추가(평가 데이터 없으면 숨김). 모델 용도 태그(v0.52.7)·추천(v0.52.8)과 함께 근거 기반 모델 선택을 지원.
v0.52.8:- **내 업무 추천 모델 (개인 홈) (v0.52.8)**: 사용자에게 "당신의 업무에는 이 모델이 더 적합합니다"를 안내. `GET /me/recommended-models`가 최근 30일 개인 프로필의 top 작업 유형(BuildPersonalProfile)과 관리자 모델 용도 태그(good_for/avoid_for)를 결합해 작업 유형별 추천/지양 모델을 산출하고, 사용자가 자주 쓰는 모델에 태그(good_for·위험 메모)를 주석으로 표시. 내 홈(/me)에 '내 업무 추천 모델' 카드 추가(작업 유형별 👍추천/⚠지양 + 내 모델 태그). 태그가 없으면 카드 자동 숨김. v0.52.7 모델 용도 태그를 개인화에 연결.
v0.52.7:- **모델 용도 태그 (TASK-003) (v0.52.7)**: "한 번 이긴 모델을 전체 기본값으로 착각"하는 것을 막기 위해 모델별 용도 태그(good_for·avoid_for·risk_note)를 부여. 신규 테이블 `model_usage_tags`. `GET/POST /admin/model-tags`·`DELETE /admin/model-tags/{model}`(관리자), `GET /v1/model-tags`(인증 사용자 읽기). 멀티 모델 비교 결과 카드에 모델별 용도 배지(👍 적합 / ⚠ 지양 / 위험 메모)를 표시하고, Chat 테스트 화면에 '모델 용도 태그' 관리 표(추가/삭제)를 추가. 용도별 비교를 유도해 업무 유형에 맞는 모델 선택을 지원.
v0.52.6:- **AI 업무 앱 — 실행 플랜 (run) (v0.52.6)**: `POST /v1/apps/{id}/run` 추가 — 권한 확인 후 앱의 각 컴포넌트를 해석해 실행 플랜(각 step의 action·endpoint·hint)을 반환. skill→`/v1/chat/completions`(X-Skill 헤더), prompt_product→채팅, text2sql_report→저장 리포트 실행 경로, mcp_tool→MCP 가상 모델, model→추천 모델로 매핑하고 컴포넌트별 해석 가능 여부를 표시. 실행은 `work_app_run` 감사 로그로 기록. AI 업무 앱 카드에 '실행(플랜)' 버튼 추가. 이종 컴포넌트의 서버측 일괄 실행 오케스트레이션은 후속(현재는 actionable 플랜 제공).
v0.52.5:- **운영 변경관리 센터 — Change Set (v0.52.5)**: 개별 설정 저장이 아니라 여러 변경을 하나의 릴리즈(change set)로 묶어 dry-run → 승인 → 적용 → 롤백하는 운영 변경관리. 신규 테이블 `change_sets`(items·prior·status·canary_scope·reviewer). `GET/POST /admin/change-sets`, `GET/DELETE /admin/change-sets/{id}`, 액션 `POST .../{dryrun|submit|approve|apply|rollback}`. **dry-run**: setting 항목별 현재 effective 값 vs 제안 값·변경 여부·유효성·재시작 필요 표시. **상태 흐름** draft→pending(submit)→approved(approve, 리뷰어 기록)→applied(apply)→rolled_back(rollback). **apply**: 모든 setting 항목을 사전 검증 후 적용 전 값을 prior로 캡처하고 일괄 저장·reloadRuntimeConfig 1회(부분 적용 방지), **rollback**: prior 값으로 복원. 기존 settings 인프라(settingDefByKey·persistSettingValue·effectiveSettingValue·런타임 reload) 재사용. 설정 탭에 '변경 세트' 서브탭 추가(목록·dry-run 비교표·생성·상태별 액션 버튼). 감사 로그 change_set.*. 이 버전은 setting 항목을 적용(policy/routing/skill은 참고 기록), canary_scope는 메모(설정은 전역). 멀티 모델 콘솔 고도화 로드맵 8단계(마지막).
v0.52.4:- **AI 업무 앱 (레지스트리·검증·노출) (v0.52.4)**: Skill·프롬프트 상품·Text2SQL 저장 리포트·MCP 도구·추천 모델을 하나의 'AI 업무 앱'으로 묶어 권한 있는 사용자에게 노출. 신규 테이블 `work_apps`(컴포넌트 JSON·allowed_teams·allowed_roles·status). 관리자 API `GET/POST /admin/apps`·`GET/PATCH/DELETE /admin/apps/{id}`·`POST /admin/apps/{id}/validate`(각 컴포넌트 참조 해석: skill/리포트/상품 존재 확인, 허용 모델 합집합 수집, 경고). 사용자 API `GET /v1/apps`(팀·역할 기반 필터, admin은 전체)·`GET /v1/apps/{id}`(권한 확인 후 상세). 운영 메뉴에 'AI 업무 앱' 탭 추가 — 앱 목록·컴포넌트 배지·검증 결과·새 앱 생성(컴포넌트 JSON). 멀티 모델 콘솔 고도화 로드맵 7단계. 후속: `POST /v1/apps/{id}/run` 실행 오케스트레이션.
v0.52.3:- **Skill 승격 전 모델 적합성 게이트 (v0.52.3)**: Skill을 production으로 승격하기 전 멀티 모델 테스트/Golden Workflow/Prompt Lab 테스트케이스로 모델 적합성을 검증했다는 근거를 요구하는 게이트. high 위험도 Skill은 항상, 그 외에는 metadata `require_model_fitness=true` opt-in 시 적용. 통과 근거가 최소 2건 미만이면 production 전환을 422로 차단(기존 필수 정책·보안 high 0건 게이트에 더해짐). 신규 테이블 `skill_fitness_evidence`(skill·kind(multimodel/golden/testcase)·ref_id·passed·score·note) + `POST/GET /admin/skills/fitness`(근거 기록/조회·통과 수 반환). Skill Studio 프로덕션 승격 단계에 '모델 적합성 근거' 패널 추가(근거 목록·통과 카운트 배지·근거 추가 폼). 멀티 모델 콘솔 고도화 로드맵 6단계.
v0.52.2:- **멀티 모델 비교 — ClickHouse Fact 적재 (v0.52.2)**: 멀티 모델 비교 결과를 ClickHouse DW에 장기 적재해 "어떤 모델이 실제 테스트에서 계속 이기는지"를 분석 가능하게 함. 신규 환경변수 `CLICKHOUSE_MULTIMODEL_FACT_TABLE`(예: ai_multimodel_fact) — 비어 있으면 비활성. 단일 테이블에 `row_type` 디스크리미네이터로 run(실행 1행)·result(모델별 1행)·choice(사람/자동 선택 1행)을 적재(프롬프트 원문 없이 식별자·지연·비용·토큰·judge 점수·verdict·선택 모델·승격 여부만). 멀티 실행(handleChatTestMultiRun)·Prompt Lab 테스트케이스 실행 시 run+result fact 적재(Prompt Lab은 judge 점수 포함), 라우팅 후보 승격·Golden 승격 시 choice fact 적재. 적재는 요청 경로 밖 best-effort goroutine(실패 시 retry 큐), ClickHouse 미설정 시 no-op. DW 부트스트랩(`/admin/dw/clickhouse/bootstrap`)에 테이블 DDL 자동 생성 추가. 멀티 모델 콘솔 고도화 로드맵 5단계.
v0.52.1:- **멀티 모델 비교 — Golden Workflow 승격 (MMT-103) (v0.52.1)**: 멀티 모델 테스트에서 선택한 모델 결과를 Golden Workflow step으로 저장해 모델 변경 회귀 테스트에 활용. `POST /admin/chat-test/multi-run/runs/{id}/golden {selected_model, workflow_id|workflow_name, task_type?, contract_id?, rubric_id?, prompt?, expected?}` 추가 — 기존 워크플로에 step 추가 또는 신규 생성. `GoldenWorkflowStep`에 선택 모델·baseline 점수(저장된 judgement 총점)·task_type·contract/rubric·source_run_id 메타데이터를 함께 저장(JSON 컬럼이라 하위호환). 원문 미저장 run은 prompt를 함께 전달하면 step 생성 가능. 멀티 비교 모델 카드에 'Golden 저장' 버튼 추가(워크플로 이름·task_type·프롬프트 입력). 이후 `/admin/golden-workflows`에서 모델 교체 회귀에 사용. 감사 로그 `chat_test.multi_run_golden`. 멀티 모델 콘솔 고도화 로드맵 4단계.
v0.52.0:- **Prompt Lab — 실험·테스트케이스·출력계약·회귀 (P0/P1) (v0.52.0)**: 단발 Chat 테스트를 재사용 가능한 테스트 자산으로 전환하는 프롬프트 실험실. Chat 테스트 옆에 'Prompt Lab' 서브탭 신설(admin). 신규 테이블 `prompt_experiments`·`prompt_test_cases`·`prompt_rubrics`·`prompt_contracts`·`prompt_test_case_runs`. **실험(experiment)**으로 테스트를 프로젝트/팀 단위로 묶고, 자주 검증하는 프롬프트를 **테스트케이스**(messages+모델목록+출력계약+rubric)로 저장. `POST /admin/prompt-lab/test-cases/{id}/run`이 저장된 테스트케이스를 여러 모델로 병렬 실행→멀티모델 run으로 저장(Diff/Judge 재사용)→rule rubric 자동 채점→**출력계약 검증**→회귀 이력(`prompt_test_case_runs`: 평균점수·best 모델·계약통과·평균비용/지연) 기록. **출력계약 엔진**: json/json_schema(필수키+스칼라 타입 lite)·markdown_table·sql(SELECT-only, 쓰기/DDL 차단)·regex 지원, strict 계약 위반 시 verdict 강등. API: experiments(목록/생성/상세/상태/삭제)·rubrics·contracts·test-cases CRUD. UI: 실험 목록·생성, 출력계약/rubric 관리, 테스트케이스 저장·실행(모델별 점수·계약 통과·지연·비용 + 회귀 이력 표시). 멀티 모델 콘솔 고도화 로드맵 3단계. 후속: 입력 데이터셋·Golden Workflow 연동(MMT-103)·production template 승격.
v0.51.9:- **멀티 모델 비교 — Rubric 기반 자동 평가 (MMT-101) (v0.51.9)**: 멀티 모델 테스트 결과를 정확성·완성도·형식 준수·안전(보안 위험 역수)·비용 효율 5개 기준으로 0~100 자동 채점. `POST /admin/chat-test/multi-run/judge {run_id, method, judge_model?}` 추가. **rule 방식**: 위험 패턴(rm -rf·DROP/DELETE·secret 노출·프롬프트 인젝션·private key) 스캔으로 안전 점수, 최장 응답 대비 길이로 완성도, 구조화 출력(코드·목록·표)으로 형식, 모델 간 상대 비용으로 비용효율을 휴리스틱 산출(정확성은 안전·완성도 기반 근사). **model 방식**: 지정 judge model에 모든 응답을 한 번에 보내 JSON 채점을 받아 rule 점수 위에 덮어씀(파싱 실패 시 rule로 폴백). 가중 총점(정확성0.3·완성도0.25·형식0.15·안전0.2·비용0.1)과 verdict(pass≥75·warn≥50·fail) 계산, best model 선정. `multi_model_test_judgements` 테이블 신설 — **점수·요약·response_hash만 저장(원문 미저장)**, run별 최신 1세트 교체 저장. run 상세 응답에 judgements 포함. 멀티 비교 결과 헤더에 '자동 평가' 버튼(규칙/모델 평가) + 점수표 표시. 멀티 모델 콘솔 고도화 로드맵 2단계.
v0.51.8:- **멀티 모델 비교 — 모델별 응답 Diff (MMT-105) (v0.51.8)**: 동일 프롬프트에 대한 모델별 응답을 문단·목록·코드블록 단위로 분해해 비교. `GET /admin/chat-test/multi-run/runs/{id}/diff`가 각 모델 응답을 구조 블록으로 나누고(코드펜스는 통째로, 리스트 라인은 개별 항목, 나머지는 문단), 정규화(소문자·공백 축약) 후 해시로 매칭해 **공통 블록(모든 응답 모델 공통)·모델별 누락 블록(다른 모델엔 있으나 이 모델엔 없음)·고유 블록(이 모델에만 있음)**과 형식 통계(문단/목록/코드블록 수·표 포함 여부·길이·줄 수)를 반환. 멀티 비교 결과 헤더에 'Diff 보기' 토글 버튼을 추가해 형식 차이 표 + 공통/모델별 블록을 표시. 저장된 응답 preview 기준(save_prompt 비활성 시 일부 잘릴 수 있음). 멀티 모델 비교 콘솔 고도화 로드맵의 첫 단계(추후 Rubric 자동 평가·Prompt Lab·Golden Workflow 연동).
v0.51.7:- **로그인 세션 관리 (다중 세션) (v0.51.7)**: 사용자가 본인의 활성 로그인 세션을 직접 보고 종료할 수 있는 셀프서비스 기능. `GET /me/sessions`(활성 세션 목록: 기기/브라우저·IP·로그인 시각·SSO 연결 여부·현재 세션 표시), `DELETE /me/sessions/{id}`(본인 소유 세션만 종료, 소유권 검증), `POST /me/sessions/revoke-others`(현재 세션 제외 전체 로그아웃) 추가. 세션 종료 시 연결된 refresh token도 함께 폐기. 내 홈(/me)에 '로그인 세션' 카드를 추가해 User-Agent를 기기/브라우저로 파싱 표시하고 개별 '종료'·'다른 모든 세션 로그아웃' 버튼 제공. 감사 로그 `session_revoked`·`session_revoked_others`. 스토어에 `ListActiveAuthSessionsForUser`·`RevokeAuthSessionOwned`(소유권 검증)·`RevokeOtherAuthSessionsForUser` 추가(v0.51.6의 `kc_sid` 연결로 SSO 세션 식별). 후속(3순위): SCIM·다중 Realm·SAML.
v0.51.6:- **Keycloak — Front-Channel Logout + sid 기반 정밀 세션 종료 (v0.51.6)**: OIDC Front-Channel Logout(`GET /auth/keycloak/frontchannel-logout?iss=&sid=`)을 추가. Keycloak이 로그아웃 시 iframe으로 이 URL을 로드하면 issuer를 검증하고 sid에 연결된 내부 세션만 폐기(no-store, 최소 HTML 응답). 이를 위해 로그인 콜백에서 ID token의 `sid` claim을 내부 세션에 연결(`auth_sessions.kc_sid` 컬럼 신설, `LinkAuthSessionKeycloakSID`), `RevokeAuthSessionsByKeycloakSID`로 특정 브라우저 세션만 정밀 종료(영향 사용자 refresh token도 함께 폐기). Back-Channel Logout도 logout_token에 `sid`가 있으면 해당 세션만, 없으면 기존대로 sub 기준 전체 세션을 폐기하도록 개선(sub/sid 중 하나만 있어도 처리). 감사 로그 `sso_frontchannel_logout`. `issueTokenPairWithSession`로 세션 id를 반환받아 연결. 후속(3순위): 다중 세션 관리 UI, SCIM·다중 Realm·SAML.
v0.51.5:- **Keycloak — Role 매핑 편집 UI (v0.51.5)**: 그동안 코드에 하드코딩돼 있던 Keycloak Role→내부 Role 매핑(vibe-admin→admin 등)을 관리자 화면에서 편집·DB 저장할 수 있게 함. `sso_provider_config`에 `role_map`(JSON) 컬럼을 추가하고, `s.effectiveKeycloakRoleMap()`가 DB에 커스텀 매핑이 있으면 그것을, 없으면 기존 기본 매핑을 반환. 로그인/access token 검증의 role 결정(`resolveKeycloakRoleWith`)이 이 effective 매핑을 사용(가장 높은 권한의 매핑 우선, 미매핑 시 기본 Role 폴백·기본 Role 없으면 차단은 동일). `GET /admin/sso/keycloak/config`는 effective `role_map`·`role_map_default`·`role_map_custom`(커스텀 여부)을 반환하고, `PUT`은 `role_map`을 받아 대상이 유효한 내부 role인지 검증 후 저장(omitted=기존 유지, `{}`=기본값으로 초기화). 저장 시 reloadKeycloakConfig로 즉시 반영. 관리자 SSO 화면의 Role 매핑표를 읽기 전용에서 행 추가/삭제·매핑 저장·기본값 초기화가 가능한 편집 표로 확장(커스텀/기본값 배지 표시). `/teams/<name>`→team 매핑은 고정. 후속(3순위): front-channel logout, 다중 세션 관리 UI, SCIM·다중 Realm·SAML.
v0.51.4:- **Keycloak — DB 기반 Provider 설정 + Client Secret 암호화 저장 (v0.51.4)**: Keycloak SSO 설정을 환경변수(SSO_KEYCLOAK_*)뿐 아니라 관리자 화면에서 편집·DB 저장할 수 있게 함. 신설 `sso_provider_config` 테이블에 enabled·issuer·client_id·redirect·scopes·default_role·role/group claim·allow_local_login을 저장하고, **client secret은 기존 AES-GCM Cipher(secret.New)로 암호화해 `client_secret_enc`에만 보관(평문 미저장·미응답)**. 런타임은 `s.keycloakConfig()`가 DB row(있으면)를 환경변수 위에 오버레이해 반환하며 secret은 복호화해 사용(기동 시·저장 시 reloadKeycloakConfig로 atomic 갱신, 모든 Keycloak 핸들러/검증 경로가 이 effective config 사용). `GET /admin/sso/keycloak/config`는 source(db/env)·db_backed·최종 수정자/시각을 함께 반환하고, `PUT /admin/sso/keycloak/config`로 저장(활성화 시 issuer 절대 URL·client_id 필수·redirect URL 검증, client_secret 미입력 시 기존 암호문 유지·빈 문자열 명시 시 클리어=public client). 저장 감사 로그 `sso_config_updated`(secret/code 미기록). 관리자 SSO 화면을 읽기 전용에서 편집 폼(저장 버튼·secret 비우기 옵션·source 배지)으로 확장. 후속(3순위): front-channel logout, Role Mapping UI 편집, 다중 세션 관리 UI, SCIM·다중 Realm·SAML.
v0.51.3:- **Keycloak — Back-Channel Logout + Group 기반 팀 자동 생성 (v0.51.3)**: Keycloak가 보내는 OIDC Back-Channel Logout을 처리하는 `POST /auth/keycloak/backchannel-logout` 추가. `logout_token`(form/JSON)을 공유 검증 코어(keycloakVerifyJWT)로 서명·issuer·exp 검증하고, audience(client id)와 back-channel-logout event claim(`http://schemas.openid.net/event/backchannel-logout`)을 확인한 뒤 `sub`을 `auth_identities`로 매핑해 해당 사용자의 모든 내부 세션을 즉시 폐기(RevokeAuthSessionsForUser). Keycloak에서 로그아웃하면 게이트웨이 세션도 함께 무효화됨. 감사 로그 `sso_backchannel_logout` 기록. 또한 SSO 로그인/링크 시 `/teams/<name>` group으로 매핑된 팀이 없으면 자동 생성(UpsertAuthTeam)한 뒤 사용자에 연결. 후속(3순위): front-channel logout, DB 기반 provider 설정·secret 암호화 저장, Role Mapping UI, 다중 세션 관리 UI.
v0.51.2:- **Keycloak — Admin SSO 설정 화면 (v0.51.2)**: 설정 탭에 'SSO' 서브탭을 추가해 Keycloak 설정(활성화·issuer·client id·secret 설정 여부·redirect·scopes·기본 role·role/group claim·로컬 로그인 허용)을 조회하고, Role 매핑표(Keycloak→내부 + /teams/<name>→team, 폴백 정책)를 표시. '연결 테스트' 버튼으로 `/admin/sso/keycloak/test`를 호출해 discovery·endpoints·JWKS RSA 키 수를 진단. 설정값은 환경변수 기반(읽기 전용)이며 DB 기반 편집·secret 암호화 저장은 후속.
v0.51.1:- **Keycloak — API Bearer 인증 + SSO 진단 (v0.51.1)**: Keycloak 발급 RS256 access token을 API/admin 인증에 허용. `currentAccessClaims`가 내부 HS256 세션 토큰을 먼저 시도하고, 실패 시 Keycloak access token을 검증(서명·issuer·만료)해 role 매핑으로 내부 scope를 합성하여 기존 RBAC·scope 인가에 그대로 연결(배치·CI/CD machine client 지원, 내부 세션 불필요). 공유 검증 코어(keycloakVerifyJWT)로 ID/Access 토큰 검증을 통합. 운영 진단용 `GET /admin/sso/keycloak/config`(비밀 제외 설정 조회)·`POST /admin/sso/keycloak/test`(discovery·JWKS 도달성·RSA 키 수 점검) 추가. HS256 내부 토큰은 Keycloak 검증기가 무시(alg 사전 체크).
v0.51.0:- **Keycloak OIDC SSO 로그인 (MVP) (v0.51.0)**: 관리자 UI에 Keycloak SSO 로그인을 추가. OIDC Authorization Code + PKCE(S256) 흐름으로 `/auth/keycloak/login`→Keycloak→`/auth/keycloak/callback`을 처리하고, OIDC discovery(.well-known) 자동 조회·JWKS 캐싱·키 로테이션(kid 변경 시 재조회)·RS256 ID Token 검증(issuer 고정·audience·exp(±60s)·nonce·서명)을 구현. 검증된 subject/email 기준으로 내부 사용자 자동 생성·연결(`auth_identities` 테이블), Keycloak realm/client role을 내부 RBAC(vibe-admin→admin, vibe-team-admin→team_admin, vibe-developer→developer, vibe-auditor→readonly_admin, 기본 role fallback)로 매핑하고 `/teams/<name>` group을 팀으로 매핑. 기존 JWT 세션 발급 체계로 내부 세션 생성(state CSRF·nonce replay 방지, 토큰은 URL fragment로 전달 후 즉시 제거). Local logout + Keycloak end_session 연동(`/auth/keycloak/logout`), 로그인 성공/실패·로그아웃 감사 로그, 환경변수 설정(SSO_KEYCLOAK_*), Local Login fallback 유지. 로그인 화면에 'SSO 로그인' 버튼(활성화 시 표시). 후속(2/3순위): Admin SSO 설정/Role Mapping UI, DB 기반 provider 설정·secret 암호화 저장, back/front-channel logout, API Bearer로 Keycloak access token 허용, Group 기반 팀 자동 생성, SCIM·다중 Realm·SAML.
v0.50.45:- **멀티 모델 비교 — 스트리밍 비교 (v0.50.45)**: 선택 모델들의 응답을 모델별 카드에 실시간 스트리밍으로 동시 렌더링(AC-008). 백엔드 변경 없이 기존 단일 모델 스트리밍 엔드포인트(`/admin/chat-test/stream`)를 모델별로 병렬 호출하고 SSE 델타를 각 카드에 누적 표시(모델별 지연 측정). 멀티 비교 패널에 '스트리밍 비교' 버튼 추가. 이로써 멀티 모델 응답 비교 콘솔의 핵심 수용 기준(병렬 실행·비교·이력·평가·승격·내보내기·사전 비용·스트리밍)을 모두 충족.
v0.50.44:- **멀티 모델 비교 — 사전 비용 예측 (v0.50.44)**: 실행 전 예상 비용을 보여줘 실비용 인지를 강화(AC-004). `POST /admin/chat-test/multi-run/predict`가 선택 모델별 입력 토큰 추정(메시지 길이 기반)·출력 토큰(모델 이력 평균 또는 max_tokens)·KRW 비용을 기존 predictCost/pricingMap으로 산출하고 합계를 반환. 멀티 비교 UI에 '예상 비용' 버튼을 추가해 모델별·합계 예상비용을 실행 전에 표시.
v0.50.43:- **멀티 모델 비교 — 라우팅 후보 승격·내보내기 (v0.50.43)**: 멀티 모델 비교 마무리. `POST /admin/chat-test/multi-run/runs/{id}/promote`로 우수 모델을 task_type 라우팅 룰 **draft 후보**로 저장(자동 적용 없음, 검토 후 반영). `multi_model_test_promotions` 테이블 신설, run 상세 응답에 promotions 포함. `GET .../runs/{id}/export?format=md|csv|json`으로 비교 결과를 마크다운·CSV·JSON으로 다운로드. 멀티 비교 UI에 모델 카드별 '라우팅 후보' 버튼과 결과 헤더의 MD/CSV/JSON 내보내기 버튼 추가. 이로써 멀티 모델 응답 비교 콘솔(병렬 실행·비교·이력·평가·승격·내보내기)을 완성.
v0.50.42:- **멀티 모델 비교 — 이력 저장·상세·사람 평가 (v0.50.42)**: 멀티 모델 비교 2차. 실행 결과를 `multi_model_test_runs`/`results`/`feedback` 테이블에 영속화(프롬프트 원문은 save_prompt opt-in 시에만, 그 외 해시+응답 preview/hash만 저장). `GET /admin/chat-test/multi-run/runs`(이력 목록)·`GET .../runs/{id}`(상세: 모델별 결과+평가)·`POST .../runs/{id}/feedback`(모델별 1~5점·코멘트) 추가. multi-run 응답에 run_id 포함. Chat 테스트 멀티 비교 UI에 모델 카드별 평가(점수·코멘트) 입력과 '이력' 조회를 추가. 관리자 인가.
v0.50.41:- **멀티 모델 응답 비교 콘솔 (v0.50.41)**: 동일 프롬프트를 여러 모델에 병렬 호출해 응답을 나란히 비교. `POST /admin/chat-test/multi-run`이 모델 목록(최대 5개)·messages·temperature·max_tokens를 받아 기존 Chat Completion 파이프라인(prepareChatTestRequest+handleOpenAI)을 모델별 goroutine으로 병렬 실행하고, 모델별 status·지연·입력/출력 토큰·예상비용·선택 provider·응답 본문을 수집(한 모델 실패가 전체를 실패시키지 않음). 최저 지연/최저 비용 모델을 요약으로 반환. Chat 테스트 탭에 '멀티 모델 응답 비교' 섹션(모델 입력·system/user 프롬프트·실행) 추가, 결과를 비교 테이블+응답 카드로 표시. 관리자 인가, 모델 수 상한으로 실비용 보호. 이력 저장·사람 평가·promote·export는 후속.
v0.50.40:- **개인 액션 큐 스누즈 (v0.50.40)**: 처리했거나 당장 급하지 않은 액션 카드를 사용자가 '나중에'로 미룰 수 있도록 개선. `POST /me/actions/snooze {type, days}`로 카드 유형을 일정 기간(기본 7일) 보류하면 `me_action_snoozes` 테이블에 사용자별 1행 저장(재보류 시 갱신)되고, `/me/actions`는 아직 유효한 보류 유형을 결과에서 제외. 내 홈 액션 카드에 '나중에' 버튼 추가.
v0.50.39:- **개인 액션 큐·알림 센터·사용 리포트 (v0.50.39)**: 개인 홈에서 바로 행동할 수 있도록 세 가지 개인화 API 추가. `GET /me/actions`는 키 만료·비용 급증(전주 대비 30%+)·실패 급증(24h)·모델 전환·반복 질문·MCP affinity·위험 상승·캐시 개선·미확인 추천을 조건부 액션 카드(메시지+버튼)로 반환하고 심각도순 정렬. `GET /me/notifications`는 추천·키 상태·실패·정책/Secret 이벤트(사용자 범위)를 단일 피드로 통합(긴급 카운트 포함). `GET /me/report?window=weekly|monthly`는 요청·비용·성공률·지연·캐시율·위험점수·상위 모델/작업유형과 전기간 대비 비용 증감을 담은 개인 사용 리포트 생성. 내 홈에 액션 큐·주간 리포트·알림 센터 카드 노출(월간 토글). 모든 데이터는 호출자 본인 범위로 한정.
v0.50.38:- **팀 리포트 승인 (v0.50.38)**: 개인 저장 Text2SQL 리포트를 팀 공용으로 승격하는 경량 승인 워크플로우. 소유자가 `POST /me/reports/submit-to-team`으로 제출하면 pending 상태로 팀 태깅(Mattermost 승인 알림), team:read 보유자가 `GET/POST /team/reports`로 목록 조회 및 승인/반려(승인 시 visibility=team, 반려 시 private 복귀). `text2sql_saved_reports`에 team·visibility·approval_status·approved_by·approved_at 컬럼 추가, 자기 팀 리포트만 결정 가능하며 타 팀 비노출. 팀 대시보드에 '팀 공용 리포트' 카드(승인/반려 버튼) 추가. 이로써 팀 공유/조직 확산 8개 아이디어를 모두 반영.
v0.50.37:- **팀 비용 절감 챌린지 (v0.50.37)**: 팀 비용 규율을 게임화. `GET /team/savings-challenge`가 이번 달 누적 비용, 경과일 기반 선형 월말 예상 비용, 전월 총액, 전월 대비 예상 절감액을 산출하고 목표 달성/초과 추세를 신호. 실제 팀 비용만 사용(가공 추정 없음), 신규 스토어 없이 TeamDashboardSince 재사용. 팀 대시보드에 챌린지 카드 노출. team:read 인가(admin은 ?team= 가능).
v0.50.36:- **팀 온보딩 팩 (v0.50.36)**: 신규 팀원을 위한 시작 키트를 팀 실제 사용 데이터에서 자동 구성. `GET /team/onboarding`이 팀이 가장 많이 쓰는 모델(사용량·성공률·비용), 팀이 채택한 Skill(성공률 가중), 팀이 실제 사용하는 MCP 도구(호출수·성공률)를 묶어 추천. 신규 스토어 메서드 TeamMCPTools(tool_invocations→request_logs→팀 귀속) 추가, 모델·Skill은 기존 TeamDashboardSince·TeamPopularSkills 재사용. 팀 대시보드에 '팀 온보딩 팩' 카드 노출. team:read 인가(admin은 ?team= 가능).
v0.50.35:- **팀 위험 신호 (v0.50.35)**: 팀 대시보드에 위험 포스처 카드를 추가. `GET /team/risk`가 팀 범위 정책 위반(차단/경고 수 + 최근 위반)·Secret Firewall 탐지(유형별)·승인 대기 건수를 집계하고, 직전 동일 길이 구간 대비 차단 건수 증감(차단 추세)으로 위험 증가를 신호. governance 이벤트의 team_id가 정규 팀 ID(claims.TeamID와 일치)임을 활용해 기존 필터를 재사용하며 team:read로 인가(admin은 ?team= 가능).
v0.50.34:- **팀 공유 — 팀 인기 Skill + 팀 추천 템플릿 후보 (v0.50.34)**: 팀 단위 지식 확산을 위해 팀 인기 Skill(`GET /team/skills/popular`: 팀 구성원의 skill_runs를 actor→사용자→팀으로 묶어 사용량·성공률·비용·지연 랭킹)과 팀 추천 템플릿 후보(`GET /team/templates/candidates`: 팀 범위 반복 프롬프트 클러스터를 빈도순으로 추천하고 이미 프롬프트 상품화된 항목 표시)를 추가. 두 API 모두 team:read scope로 인가하고 admin은 `?team=`으로 임의 팀 조회 가능하며 타 팀 데이터 누출 없음. 팀 대시보드에 두 카드를 병렬 로드로 노출. 스토어에 TeamPopularSkills·TeamTemplateCandidates 추가(기존 팀 귀속 패턴 재사용).
v0.50.33:- **역할별 홈 템플릿 — 보안·비용 대시보드 + 역할 맞춤 랜딩 (v0.50.33)**: 역할별로 적합한 홈을 제공하도록 보안 담당자용 `GET /security/dashboard`(정책 위반·Secret Firewall·위험 MCP·승인 대기)와 비용 담당자용 `GET /billing/dashboard`(비용센터별 비용·예산 소진율·모델 전환 후보·예상 절감)를 신설. `billing_admin` 역할 추가, `security_admin`·`billing_admin`·`team_manager`는 scope만으로 구분되지 않으므로 역할 맞춤 기본 홈(roleHomeOverride)으로 각각 `#/security`·`#/billing`·`#/team`에 랜딩. 두 대시보드는 기존 스토어 집계(정책결정·Secret·도구위험·승인큐·비용배분·예산상태·모델전환)를 재사용하고 scope로 서버 인가(보안=security:read, 비용=admin:read). 쓰기 권한(admin:write) 없는 운영자에게는 헤더에 '읽기 전용' 표시(쓰기는 서버에서 차단). 보안/비용 담당자에게는 프롬프트 원문이 데이터 스코프 마스킹으로 비노출.
v0.50.32:- **역할 기반 홈·내비게이션 + RBAC 확장 (v0.50.28~v0.50.32)**: 비관리자 사용자의 기본 홈을 `/admin` 운영 대시보드가 아닌 개인화 홈(#/me)으로 전환하고, 역할·scope·feature 기준 서버 단일 메뉴 레지스트리(`GET /me/navigation`)로 접근 가능한 메뉴만 렌더링하며 동일 정책을 SPA 라우트 가드에 적용(직접 URL 차단 시 권한 안내 화면 + `/me/access-denied` 감사 기록). `/auth/me`에 roles·features·default_home·menu_version 추가. RBAC 가시성을 위해 역할 카탈로그(`GET /admin/roles`)와 실효 권한 디버그(`GET /permissions/effective`, 메뉴별 allow/deny 사유)를 제공. `team_manager` 역할과 팀 대시보드(`GET /team/dashboard`: 팀 사용량·비용·실패·팀원 Top, 소속 팀 한정) 추가. 데이터 스코프 마스킹으로 하위 권한 관리자(viewer·readonly_admin·ops_admin 등)에게는 요청 상세·비교의 프롬프트 원문을 리덕션 처리(원문 열람은 admin·security_admin만). 코드 변경 없이 역할을 정의하는 동적 커스텀 역할(`custom_roles` 테이블 + `/admin/roles` POST/DELETE) 오버레이를 도입하여 토큰 발급 시 실효 scope에 반영. `/admin/*` API는 기존 scope 기반 인가가 그대로 적용되어 메뉴 숨김과 무관하게 직접 호출도 차단.
v0.50.27:- **Skill Studio — 후보 추천 + 승격 마법사 (v0.50.27)**: 기존 데이터(프롬프트 클러스터·프롬프트 상품·Text2SQL 반복질문·교차 사용자 개인화 추천)를 마이닝해 draft 스킬 후보를 랭킹 추천하는 API(`GET /admin/skill-studio/candidates`)를 추가. 후보를 origin 메타데이터와 함께 draft 스킬로 채택하고(`POST /admin/skill-studio/adopt`, 멱등·중복 시 409), 평가→Chat 테스트→스테이징→프로덕션 전환을 하나의 스텝퍼 마법사(안전 > Skill Studio 탭)로 제공. 실시간 준비도 체크리스트(`GET /admin/skill-studio/readiness`)로 진행 상황을 표시. 프로덕션 전환 게이트를 강화하여 allowed_models·allowed_tools·allowed_teams·daily_limit(>0)를 instructions·보안 스캔·고위험 사유와 함께 필수 검증하도록 의무화(미충족 시 staging→production 차단). 교차 사용자 추천 집계용 `AggregateRecommendations` 스토어 메서드 신설.
v0.50.26:- **프롬프트 자산 관리소 — 버전 관리·성과 귀속·A/B·골든 연계 (v0.50.26)**: 어드민에 프롬프트 자산 관리소 탭을 신설하여 템플릿을 태그 체계(Java·Go·SQL·보안·테스트·사규·법규 등)와 승인 워크플로우(초안→검토→승인→조직 표준)로 중앙 관리. `prompt_template_history` 통합 이력 테이블로 본문 변경마다 전체 스냅샷(버전 번호 부여)을 남기고 상세 모달에서 버전 타임라인·라인 diff·특정 버전 복원을 제공. `X-Prompt-Asset-Id` 헤더로 `request_logs`에 자산 ID를 정확 귀속(이름 매칭 폴백)하여 성공률·평균비용·평균지연을 90일 윈도우로 집계하고, 두 자산을 나란히 비교하는 A/B 뷰와 팀별 사용처 역추적을 추가. 조직 표준 승격 시 Golden Prompt 회귀 케이스를 자동 등록하고, 검토 제출 시 Mattermost 승인 채널로 알림하며 승인·반려 전체 변경 이력을 감사 타임라인으로 노출. 신규 엔드포인트 `GET /admin/prompt-assets`, `GET/POST /admin/templates/{id}/{history,rollback,usage}`. 더불어 UTC 자정 경계에서 깨지던 `TestMyHomeQueries`를 결정적으로 수정.
v0.50.25:- **Chat 테스트 MCP Discovery 운영 가시성 강화 (v0.50.25)**: Chat Completion 테스트 카탈로그가 `vibe/grounded`·`vibe/research`·`vibe/all-mcp`의 MCP Discovery 정책(`mode`, 후보 수, selector/evidence gate)과 agentic loop 설정(`mcp.agentic_model`, steps/tokens/tools/force first)을 함께 노출. 선택 대상 상세 패널과 표의 세부 컬럼을 추가하고, 디버그 레일이 브라우저의 소문자 `x-mcp-*` 헤더도 인식해 후보/관련성 gate/백킹 모델/도구 호출 통계를 안정적으로 표시.
v0.50.24:- **MCP Discovery 에이전트 루프 실시간 스트리밍·관측성·신뢰성 강화 (v0.50.24)**: 에이전트 루프의 LLM 턴 수(`mcp.max_agent_steps`/기본 8)·턴당 토큰(`mcp.max_tokens`/기본 2048)·첫 턴 도구 강제(`mcp.force_tool_first`)·노출 도구 상한(`mcp.max_tools`/기본 32)을 설정화하고 하드코딩 6턴 상한을 제거. 최종 답변을 비스트리밍 후 재청크 대신 업스트림을 실제 스트리밍 호출해 토큰 단위로 실시간 전달(조각난 tool_calls는 index별 누적). 일시적 업스트림 오류 1회 재시도와 `tool_choice=required` 미지원 provider의 `auto` 폴백, 동일 인자 도구 호출 캐시로 신뢰성·비용 개선. 턴/도구호출/근거/백킹모델 통계를 `X-MCP-*` 헤더 및 스트리밍 `x_mcp` 청크로 노출해 Chat 테스트 디버그 레일에 표시하고, 각 도구 호출의 실제 인자를 evidence·감사에 기록.
v0.50.23:- **MCP Discovery selector 보조화 및 백킹 모델 설정 (v0.50.23)**: `vibe/grounded`·`vibe/research`·`vibe/all-mcp` agentic 경로에서 MCP selector 점수를 hard gate가 아니라 후보 정렬 가중치로 사용하도록 변경. 백킹 LLM 모델을 `MCP_AGENTIC_MODEL` 또는 런타임 설정 `mcp.agentic_model`로 지정 가능하게 하고, `vibe/all_mcp` 별칭을 `vibe/all-mcp`와 동일하게 지원. 백킹 모델 부재 시 정적 fallback은 기존 관련성 gate를 유지.
v0.50.22:- **에이전트식 MCP Discovery 도구 호출 루프 탑재 (v0.50.22)**: vibe/grounded 및 vibe/research 가상 모델 요청 시 백업 LLM이 후보 MCP 도구들을 직접 선별·실행해 최종 답변을 합성하는 에이전트 루프(mcp_agentic.go) 구현. SSE 스트리밍 시 reasoning_content 델타 전송 및 백업 모델 부재 시 정적 폴백 지원.
v0.50.21:- **MCP Discovery 자가 진단 피드백 및 정책 정밀화 (v0.50.21)**: MCP 디스커버리 후보가 비어있을 때 누락 사유(도구 미확인·점수 미달·정책 필터 등)를 자연어 응답에 구조화해 반환하는 mcpSelectionDiag 진단 모듈 도입. vibe/grounded 및 vibe/research의 라우팅 기준 정책 세부 조율.
v0.50.20:- **Chat Completion 스트리밍 테스트 지원 (v0.50.20)**: 테스트 콘솔에 /admin/chat-test/stream 엔드포인트를 추가하고 실시간 SSE 응답 스트리밍 렌더링 기능을 구현하여, 스트리밍 모드에서도 응답 헤더 및 본문의 동작 무결성을 한 화면에서 완벽 검증 가능.
v0.50.19:- **Chat Completion 테스트 콘솔 (v0.50.19)**: `GET /admin/chat-test/targets`, `POST /admin/chat-test/run`과 어드민 `Chat 테스트` 탭을 추가. `vibe/auto`·`vibe/text2sql-*`·provider model pattern·MCP route 후보를 선택해 실제 `/v1/chat/completions` 파이프라인 호출, 라우팅 preview, 응답 헤더/본문을 한 화면에서 검증 가능.
v0.50.18:- **추천 채택률 개인화 탭 연동 (v0.50.18)**: 기존 `GET /admin/recommendations/adoption`을 개인화 탭에 연결해 추천 종류별 채택/거절/채택자/채택률을 같은 운영 화면에서 확인 가능. 추천형 개인화의 품질 루프를 관리자 UI에서 바로 점검.
v0.50.17:- **Text2SQL 개인 힌트 운영 표 (v0.50.17)**: `GET /admin/personalization/text2sql-hints`와 어드민 개인화 탭 표를 추가. 사용자별 반복 Text2SQL 질문을 원문 없이 fingerprint·schema·성공률·비용·절감 추정으로 노출해 저장 리포트/대시보드/데이터마트 후보를 운영자가 검토 가능.
v0.50.16:- **개인화 Affinity 운영 표 (v0.50.16)**: `GET /admin/personalization/model-affinity`, `GET /admin/personalization/mcp-affinity`와 어드민 개인화 탭 표를 추가. 사용자별 모델 적합도와 MCP 도구 적합도를 성공률·사용량·비용/지연 기반 점수로 정렬해 추천·코칭의 운영 근거를 강화.
v0.50.15:- **릴리즈 자동화 스크립트 리팩토링 (v0.50.15)**: gh_release.ps1 내의 모든 과거 변경 이력 하드코딩 리스트를 scripts/changelog.txt 외부 텍스트 파일로 분리하고, 지정 버전의 문서가 존재하는지 사전 검사하는 자동 검증 로직을 도입하여 릴리즈 관리 정합성 및 유지보수성을 극대화.
v0.50.14:- **개인화 코칭 후보 (v0.50.14)**: `GET /admin/personalization/coaching`과 어드민 개인화 탭의 코칭 후보 표를 추가. Personal AI Profile 지표만 사용해 보안·품질·재사용·비용·Text2SQL·MCP 코칭 후보를 read-only로 산출하며 원문 프롬프트/SQL/응답은 참조하지 않음.
v0.50.13:- **추천 피드백 API 호환성 (v0.50.13)**: 기존 `POST /me/recommendations/feedback`을 유지하면서 `POST /me/recommendations/{id}/feedback` 별칭을 추가. `accepted/rejected/dismissed/later` 액션 별칭과 선택 `reason` 저장을 지원해 추천 채택률 학습 데이터를 더 유연하게 수집.
v0.50.12:- **개인화 추천 후보 확장 (v0.50.12)**: `/me/recommendations`가 반복 Text2SQL 질문 기반 저장 리포트 후보와 사용자별 MCP 도구 affinity 추천을 생성. Text2SQL 추천은 원문 질문/SQL 대신 fingerprint·집계 지표만 저장하고, 차단/승인필요 MCP risk profile 도구는 추천에서 제외. 후보 산출 store 회귀 테스트 추가.
v0.50.11:- **Personal AI Profile 운영 지표 확장 (v0.50.11)**: 개인 프로필에 평균 지연, 캐시율, Text2SQL 사용률, MCP 사용률, 개인 위험 점수, 자주 쓰는 MCP 도구 Top5를 추가. `/me/dashboard`와 개인화 어드민 탭이 같은 프로필 기준을 표시하도록 연결하고 store 회귀 테스트를 보강.
v0.50.10:- **Text2SQL span timeline 안정화 (v0.50.10)**: preview/cache/검증 실패/실행 실패 경로에서도 `explain_guard`, `execute`, `mask_result`, `summarize` 후속 단계를 `skipped` span으로 기록해 XView·Text2SQL Timeline의 단계 비교를 안정화. preview flow 회귀 테스트가 요구 stage 전체와 skipped 상태를 검증하도록 강화.
v0.50.9:- **MCP Wizard 선택 상태 안정화 (v0.50.9)**: MCP 연결 상태 Wizard가 `#/mcp?server=...` 필터와 직전 선택 upstream을 기본 선택으로 유지하고, Wizard에서 업스트림 등록 직후 생성된 upstream을 바로 선택하도록 개선. 다중 upstream 운영에서 연결 테스트→Flow→정책 확인→로그 확인 흐름이 첫 upstream으로 되돌아가는 UX 문제를 제거.
v0.50.8:- **Routing/Governance 운영 문서 동기화 (v0.50.8)**: README, Admin Guide, Safety Guide에 Provider Health 화면(`#/routing/health`), `route_reason`, Trace Links, Governance default audit/effective count(`policy_decision_count`/`policy_decision_total`) 의미를 반영. 안정화 릴리즈 운영 문서와 현재 API/UI 동작을 일치시킴.
v0.50.7:- **Governance effective count 정리 (v0.50.7)**: default 감사 이벤트 도입 후 요청 상세/Trace Links가 정상 요청을 거버넌스 이상으로 보지 않도록 `policy_decision_count`는 `decision=default`를 제외한 실질 판단 수로 계산. 원시 감사 이벤트 수는 `policy_decision_total`로 별도 노출. explain/links/scatter 회귀 테스트 보강.
v0.50.6:- **Governance DEFAULT 감사 이벤트 (v0.50.6)**: 정책 규칙이 매칭되지 않은 허용 요청도 `policy_decision_events`에 `decision=default`로 기록해 모든 정책 판단 경로를 감사 가능하게 보강. XView/scatter의 거버넌스 이상 카운트에서는 `default`를 제외해 정상 요청 전체가 경고로 표시되는 노이즈를 방지. default 감사와 XView 제외 회귀 테스트 추가.
v0.50.5:- **Routing preview reason code (v0.50.5)**: `POST /admin/routing/preview` 응답에 사람이 읽는 `decision_reason`과 별도로 코드형 `route_reason`을 추가. `auto_router`, `auto_router_learned`, `client_model`, routing rule reason 등을 자동화·UI 필터에서 안정적으로 사용할 수 있으며 preview 회귀 테스트로 고정.
v0.50.4:- **Provider Health 관리자 화면 (v0.50.4)**: 라우팅 탭에 `Provider Health` 하위 화면(`#/routing/health`)을 추가. `GET /admin/routing/health`의 ranking, degraded, alerts, trend 응답을 그대로 사용해 provider 수, 평균 health, 최상위/최하위 provider, degradation alert, bucket별 health trend를 표시하고 window/threshold 필터를 제공.
v0.50.3:- **Provider Health 운영 응답 확장 (v0.50.3)**: `GET /admin/routing/health` 응답에 기존 provider health score 원본을 유지하면서 ranking, degraded provider, alert, bounded trend bucket을 추가. 실시간 provider ranking, degradation alert, health trend UI가 별도 재계산 없이 같은 API를 사용할 수 있도록 하고, trend 계산은 `ProviderHealthScoresBetween` bounded 조회로 정확히 분리. store/API 회귀 테스트 추가.
v0.50.2:- **요청 Trace Links (v0.50.2)**: `GET /admin/requests/{id}/links`로 요청 상세·XView·Waterfall·MCP Waterfall·Text2SQL Timeline·라우팅 결정 연결 정보를 한 번에 반환. 요청 상세 모달 상단에 관련 아티팩트 버튼과 governance/MCP/Text2SQL 카운트 배지를 표시해 요청 1건의 인과관계를 빠르게 추적 가능.
v0.50.1:- **Settings Registry effective view (v0.50.1)**: `GET /admin/settings/effective`를 추가해 `bootstrap_env`·`db_setting`·`runtime_flag`·`request_override` 계층과 최종 적용 출처를 노출. 비밀값은 기존과 동일하게 마스킹하며, 런타임 설정 UI에서도 active/set/off 배지로 계층 상태를 표시.
v0.49.9:- **MCP 운영 콘솔 1차 (v0.49.9)**: MCP 탭을 단순 목록 관리에서 운영 콘솔로 확장. 신규 API `GET /admin/mcp/overview`, `GET /admin/mcp/routes`, `POST /admin/mcp/route/explain`, `POST /admin/mcp/test`를 추가해 업스트림 상태, 노출 route map, 서버 정책·tool risk profile 합산 판단, 실제 upstream test call 결과를 확인 가능. 어드민 UI에는 MCP Overview KPI, Route Map, Route Explain/Test Console을 추가하고 `github__create_issue -> github/create_issue`처럼 클라이언트 노출명과 업스트림 원본 대상을 나란히 표시. disabled upstream 제외, namespaced route, approval/block explain, test call 회귀 테스트 추가.
v0.49.8:- **Intelligent Routing 학습 루프 관리자 UI (v0.49.8)**: 운영 데이터 기반 라우터 개선 흐름을 어드민 화면에 통합. 상단 `라우팅` 탭을 추가하고, MCP discovery 라우팅의 decision log·signal·confidence·evidence score·자동 승격 예시·검토 큐를 한 화면에서 확인 가능. 낮은 신뢰도/후보 충돌/증거 부족 항목은 review queue에서 승인·거절 처리할 수 있으며, 기존 모델 추천 학습 결과도 같은 화면에 노출해 관리자가 규칙을 계속 손으로 고치는 부담을 줄임.
v0.49.7:- **시스템 오류 로그 UI 패딩 여백 조정 및 캡처된 응답 gzip 디코딩 깨짐 수정 (v0.49.7)**: (1) **UI 여백 보완** - 시스템 오류 로그 페이지의 설명 문구 왼쪽에 4px 패딩을 부여하고 상단 8px 마진을 추가하여 제목 및 레이아웃과의 시각적 조화를 개선. (2) **gzip 응답 자동 디컴프레션** - AI API 응답 헤더 중 `Content-Encoding: gzip`이 전달될 경우 프록시 단에서 이를 자동 압축 해제(`gzip.Reader`)한 뒤 저장소와 클라이언트에 전달하도록 보완하여, 요청 상세 모달 창의 '캡처된 응답' 영역 인코딩 깨짐 현상과 PostgreSQL의 sqlstate 22021(invalid byte sequence) 저장 장애를 해결.
v0.49.6:- **PostgreSQL 호환성 및 시스템 오류 로깅 체계 구축 (v0.49.6)**: (1) **인코딩 오류 예외 방어 보강** - `cleanArgs` 헬퍼 함수를 보강해 문자열 필드 유입 시 `strings.ToValidUTF8`를 적용하여 PostgreSQL에서 sqlstate 22021(invalid byte sequence for encoding UTF8: 0x8b)로 인서트를 차단하고 트랜잭션을 롤백시키던 현상을 원천 방지. (2) **일별 롤업 쿼리 PostgreSQL 표준 호환 수정** - `analytics_rollup.go`에서 `substr` 함수를 표준 `substring`으로 개정하고, `GROUP BY` 문을 컬럼 번호 인덱스(`GROUP BY 3`)로 단순화하여 PostgreSQL 환경에서 `retention rollup failed` 에러가 발생하던 쿼리 문법 호환성 문제 해결. (3) **시스템 오류 모니터링 체계 도입** - 시스템 오류 로그 테이블(`system_error_logs`) 및 DB API를 신설하고, 비동기 로깅 실패 혹은 retention rollup 실패 등의 주요 백그라운드 예외 발생 시 시스템 로그에 자동 적재. 어드민 UI 설정 메뉴 하위에 '시스템 오류' 탭 및 조회/삭제 기능 연동.
v0.49.5:- **PostgreSQL 환경에서의 호출 이력 누락 해결 (v0.49.5)**: PostgreSQL 환경에서 문자열 필드에 NULL 바이트(`\x00`)가 입력되거나 실수형 필드에 `NaN`/`Infinity`가 입력될 때 인코딩 예외 및 타입 오류로 인해 데이터베이스 저장이 실패하던 현상을 해결. `cleanArgs` 전처리 단계를 통해 NULL 바이트를 스트립하고 유효하지 않은 실수 값을 `0.0`으로 자동 정제하여 유실 없이 100% 로깅을 보장.
v0.49.4:- **마크다운 렌더러 표(Table) 가독성 개선 (v0.49.4)**: 어드민 UI의 '캡처된 응답' 등에서 API 응답에 표(Table) 문법이 포함되어 있을 때, 테이블 형식을 깔끔하게 HTML `<table>`로 변환하여 렌더링하도록 개선. 열 정렬 방식(`:---`, `---:`, `:---:`)에 따라 셀 정렬 스타일(`text-align: left/right/center`)을 인라인으로 자동 적용하고, 테마 색상(`var(--line)`, `var(--panel-alt)`)을 사용해 가독성을 대폭 향상.
v0.49.3:- **SQLite Lock Contention(database is locked) 완화 및 로깅 누락 해결 (v0.49.3)**: (1) **SQLite 최적화 DSN 파라미터 자동 적용** - SQLite DB 연결 DSN에 `busy_timeout(10000)` (10초 대기), `journal_mode(WAL)` (Write-Ahead Logging), `synchronous(NORMAL)` 옵션을 자동으로 보간하도록 개선. 이를 통해 다중 에이전트 및 백그라운드 태스크의 동시 쓰기 경쟁 상황에서도 SQLite `database is locked` 오류를 발생시키지 않고 트랜잭션을 순차 대기 처리하여 `fallback.ndjson`으로의 로그 유실을 원천 차단. (2) **로깅 유실 무결성 검증** - 기존 `fallback.ndjson`에 유실 및 누적되어 있던 미복원 로그 데이터를 메인 DB에 성공적으로 복원(Replay)하고, XView 및 호출 이력 정상 표출 검증 완료.
v0.49.2:- **요청 이력 누락 완전 해소 및 마크다운 뷰어 가독성 개선 (v0.49.2)**: (1) **CORS/Method/글로벌 킬스위치 및 budget/cost guard 누락 해소** - `defer` 기반의 견고한 로깅 복원 기법을 도입하여, HTTP Method 체크 실패(405), 글로벌 킬스위치 차단(503), 비용 예측 한도 초과(budget_denied) 및 비용 가드 차단 등 파이프라인의 모든 초기/중간 실패 이력을 호출 이력에 유실 없이 100% 로깅 보장. 슬래시가 누락된 `/v1` 경로도 리다이렉트 없이 다이렉트 처리. (2) **요청 상세 마크다운 가독성 대폭 향상** - 어드민 UI의 요청/응답 상세 뷰어에 마크다운 렌더러를 개선(코드 블록 줄바꿈 깨짐 수정, blockquote 및 이탤릭 지원 등)하고, 전체 텍스트에 모노스페이스 대신 깔끔한 system-ui 샌스세리프 폰트를 적용하고 줄간격과 크기를 조절해 가독성 개선.
v0.49.1:- **미등록 API 경로 허용 및 초기 파이프라인 차단 로깅 보장 (v0.49.1)**: (1) **미등록 OpenAI API 경로 지원** - `/v1/responses`, `/v1/completions` 등 미등록 `/v1/*` 경로 유입 시 404 차단 없이 정상 패스스루하여 인증/할당량을 적용하고 업스트림 중계 및 요청 이력에 반드시 기록. (2) **초기 단계 실패 로깅 보장** - `auth`, `quota`, `routing`, `skill`, `deprecation`, `limits` 등 파이프라인 초기 단계에서 실패하여 반환되는 요청도 DB `request_logs` 및 XView/요청 목록에 status code 및 에러 내역(`pipeline_blocked:<step>`)을 누락 없이 기록하도록 수정.
v0.44.0:- **모델별 호출 XView (v0.44.0)**: XView 탭을 다중 모델 비교 분석 뷰로 확장. (1) **다중 모델 필터** — `?models=gpt-4.1,gpt-4.1-mini`(콤마 구분) 지원, 기존 단일 `?model=` 호환. (2) **지표 전환** — 응답시간·첫청크·토큰·비용·위험·헬스 중 선택. (3) **색상 모드** — 카테고리별(오류/거버넌스/캐시/폴백/복잡) ↔ 모델별 색상. (4) **이상 항목 링 마커** — 오류/폴백/거버넌스 점에 외곽 원형 표시. (5) **모델별 요약 테이블** — P50/P95/P99·오류율·폴백·거버넌스·위험P95·헬스 평균(열 클릭 정렬). (6) 신규 API 3종: `GET /admin/xview/models`(top-N 모델 집계), `GET /admin/xview/model-series`(모델별 시계열), `GET /admin/xview/model-outliers`(이상 항목 태그). 스펙 AC-006(원문 프롬프트 미노출)·AC-007(admin 권한 그대로) 준수. 백엔드 `ScatterFilter.Models []string` 추가(멀티 IN 쿼리), 집계는 Go 레이어 in-memory(P50/P95/P99 포함)
v0.43.0:- **어드민 UI: 네비 IA 정리 마무리 (v0.43.0)**: 관련 메뉴를 상위 탭의 하위 탭으로 추가 묶음 — (1) **사용자** ▸ 팀·IP·사용 한도, (2) **안전** ▸ Skills·모델 일몰, (3) **MCP** ▸ 에이전트·VCS. 상단 네비 15→9개(`대시보드 / MCP / 호출 이력 / 프롬프트 검색 / 사용자 / 안전 / Text2SQL / DW 대시보드 / 설정`). 모든 하위 항목은 기존 최상위 라우트라 해시(`#/teams`·`#/skills`·`#/agents` 등) 불변 — 북마크·딥링크·상세 페이지(`#/users/<id>` 등) 100% 호환. 하위 진입 시 보조 탭 바 + 상위 탭 활성 유지. v0.41–0.42에 이은 IA 정리 완료, UI 라우팅 전용
v0.42.0:- **어드민 UI: 관측 탭 묶음 (v0.42.0)**: `XView`·`Waterfall`·`LLM 관측`을 **대시보드** 하위 탭으로 이동 — 트래픽/트레이스 관측 4종을 한 그룹으로. 상단 네비에서 3개 항목 제거(18→15개), 대시보드/XView/Waterfall/LLM에 진입 시 보조 탭 바 표시 + 상위 `대시보드` 탭 활성 유지. 해시(`#/xview`·`#/waterfall`·`#/llm`)는 그대로라 북마크·내부 링크 100% 호환. v0.41.0(ClickHouse→DW, 런타임설정→설정)에 이은 IA 정리. UI 라우팅 전용
v0.41.0:- **어드민 UI: 탭 구조 정리 (v0.41.0)**: 상단 네비를 단순화하고 **하위 탭(sub-tab)** 도입. (1) `ClickHouse`를 **DW 대시보드** 하위 탭으로 이동(`#/dwdashboard` ↔ `#/dwdashboard/clickhouse`), (2) `런타임 설정`을 **설정** 하위 탭으로 이동(`#/settings` ↔ `#/settings/runtime`). 본문 상단에 보조 탭 바를 표시하고, 하위 뷰에서도 상위 네비 탭이 활성 표시되도록 처리. 기존 `#/clickhouse`·`#/runtimesettings` 해시는 레거시 별칭으로 계속 동작(딥링크 호환), 내부 링크는 신규 경로로 갱신. 백엔드/API 변경 없음(UI 라우팅 전용)
v0.40.0:- **보안: ClickHouse 식별자 SQL 인젝션 차단 (v0.40.0)**: `handleClickHouseOverview`가 admin이 설정 가능한 `clickhouse.table`/`database`를 `system.tables` 조회에 이스케이프 없이 보간하던 인젝션을 수정 — 문자열 리터럴은 `chEscape`로 이스케이프. 더해 신규 `validCHIdentifier`로 식별자(letters/digits/underscore, 선택적 `db.table`)를 검증하고 (1) 설정 저장 시(`clickhouse.database`·`table`·모든 `*_fact_table`) 유효성 검사로 잘못된 식별자 거부, (2) bootstrap DDL 실행 전 방어적 재검증(env 경유 값까지 차단). DDL 식별자는 리터럴 이스케이프가 불가하므로 화이트리스트 검증으로 방어. 단위·부트스트랩 거부·설정 거부 테스트 추가
v0.39.0:- **DW 대시보드 — 마감 폴리시: 관측 메트릭 + 주별 bucket (v0.39.0)**: 스펙 마무리. (1) **관측 메트릭** — `/metrics`에 `proxy_dw_dashboard_cache_hits_total`·`proxy_dw_dashboard_cache_misses_total` 추가(스펙의 'metrics' 백엔드 요구). 캐시 적중률을 대시보드/알람으로 모니터링. (2) **시계열 bucket** — `GET /admin/dw/dashboard/timeseries?bucket=day|week` 지원(week는 `toMonday(day)`로 ISO 주 집계, 알 수 없는 값은 day로 폴백; 일별 rollup 특성상 hour는 미지원). 어드민 비용 추이 패널에 일별/주별 토글 추가. 장기 추세를 주 단위로 평활화해 조망
v0.38.0:- **DW 대시보드 — 쿼리 캐시 + 새로고침 (v0.38.0)**: 스펙의 백엔드 요구사항 ``30-60s 캐시``·``refresh`` 구현. 모든 DW 대시보드 패널이 거치는 `dwQueryJSON`에 **45초 TTL 인메모리 캐시**(ClickHouse URL+쿼리 SQL 키, 만료분 자동 정리, 256개 초과 시 정리)를 추가해 여러 관리자가 동일 기간을 폴링해도 ClickHouse 풀 rollup 스캔이 반복되지 않도록 보호. 신규 API `POST /admin/dw/dashboard/refresh` — 캐시를 비워 다음 조회를 최신값으로 강제(비운 건수 반환, admin audit 기록). 대시보드에 ``새로고침`` 버튼 추가. OpenAPI(`dw`) 반영
v0.37.0:- **DW 대시보드 — 데이터 상품 후보 패널 (v0.37.0)**: DW 대시보드 9차(마지막 스펙 슬라이스, '데이터 상품'). 기존 report-candidate miner(`/admin/text2sql/miners`)를 대시보드에 표면화 — 반복되는 자연어 질의를 **추천 상품 유형**(대시보드/데이터마트/API)으로 분류하고, 후보 수·유형별 분포 요약 카드와 상위 후보 표(질의 요약·반복 횟수·추천 상품·마지막 발생)를 표시. 스펙의 ``원문 prompt·SQL 노출 금지``를 준수해 **SQL 원문은 미노출**, 질의는 60자 요약만 표시. 정형 리포트 승격·골든 등록은 Text2SQL 탭 deep link로 안내. UI 전용 슬라이스(백엔드 재사용) — 이로써 스펙의 핵심+부가 화면(Overview/비용/사용량/성능/품질/라우팅/Text2SQL/적재상태/정합성/비용절감/데이터상품) 전체 완료
v0.36.0:- **DW 대시보드 — 비용 절감/모델 전환 패널 (v0.36.0)**: DW 대시보드 8차 슬라이스(스펙의 '비용 절감·모델 전환·청구서 연결'). 기존 운영 API `/admin/savings`(라우팅 다운시프트+캐시 절감, 정확한 모델별 단가 기준)와 `/admin/model-migration`(프롬프트 클러스터별 현재→추천 모델 전환 advisor)을 대시보드에 표면화 — **총 절감액·다운시프트/캐시 절감·모델 전환 추천 건수·전환 예상 절감** 요약 카드, 차원별(project/model/cost_center) **절감 Top** 표, **모델 전환 추천**(현재→추천·성공률·예상 절감) 표, 비용 상세 deep link. 대시보드 기간/차원 필터 연동, 절감/전환은 운영 DB 실시간 계산(ClickHouse rollup은 단가 baseline 재현 불가). UI 전용 슬라이스(백엔드 재사용)
v0.35.0:- **DW 대시보드 — 품질 분석 패널 (v0.35.0)**: DW 대시보드 7차 슬라이스(스펙의 '품질' 화면). 신규 API `GET /admin/dw/dashboard/quality?window=` — 두 fact를 합산: (1) **자동 평가** `ai_eval_fact`(CLICKHOUSE_EVAL_FACT_TABLE)에서 평가 수·평균 점수·**통과율**과 **카테고리별**(injection/security 등) 평균 점수·통과율, (2) **사용자 피드백** `ai_feedback_fact`(CLICKHOUSE_FEEDBACK_FACT_TABLE)에서 피드백 수·평균 평점·긍정/부정·**긍정 비율**과 label별 분해. 두 소스는 각각 선택적 — 하나라도 설정되면 `configured:true`, 미설정 소스는 안내 문구. 대시보드에 품질 패널(평가/피드백 카드·카테고리/label 표) 추가. OpenAPI(`dw`) 반영
v0.34.0:- **DW 대시보드 — 성능(지연) 분석 패널 (v0.34.0)**: DW 대시보드 6차 슬라이스. 신규 API `GET /admin/dw/dashboard/latency?window=&model=&provider=&project=&cost_center=&team=` — `ai_request_fact`(CLICKHOUSE_REQUEST_FACT_TABLE)에서 지연 **P50/P95/P99**·평균·최대, **TTFB P95**(스트리밍 첫 청크)·스트리밍 비율·오류율을 집계하고 **모델별 P95 지연 Top-N**을 반환(ReplacingMergeTree FINAL). rollup의 단일 차원과 달리 request fact는 차원별 실제 컬럼이라 model/provider/project/cost_center/team 필터를 **조합** 가능. 대시보드에 성능 패널(지연 카드·모델별 P95 표) 추가, fact 미설정 시 `configured:false`. 실(real)-ClickHouse 통합 테스트(`CH_IT_URL` 게이트) 추가 — bootstrap·전체 fact sink·정합성 검증. OpenAPI(`dw`) 반영
v0.33.0:- **DW 대시보드 — 라우팅 분석 패널 (v0.33.0)**: DW 대시보드 5차 슬라이스. 신규 API `GET /admin/dw/dashboard/routing?window=` — `ai_routing_fact`(CLICKHOUSE_ROUTING_FACT_TABLE)에서 총 라우팅·**자동 재작성 수/율**·fallback 수·평균 complexity/risk/health를 집계하고, **모델 재작성 Top(요청→선택)**·**결정 근거(decision_reason) Top**을 반환(ReplacingMergeTree FINAL dedupe). 대시보드에 라우팅 패널(요약 카드·재작성 표·결정 근거 표) 추가. fact 미설정 시 `configured:false`. ``자동 라우팅이 어떤 모델로/왜 다운시프트했는지``를 한눈에 분석. OpenAPI(`dw`) 반영
v0.32.0:- **DW 대시보드 — 정합성(Consistency) 패널 (v0.32.0)**: DW 대시보드 4차 슬라이스, DW 품질 진단. 기존 `/admin/dw/consistency`(차원별 운영 DB rollup ↔ ClickHouse 적재량 비교, 최근 30일)를 대시보드에 **정합성 패널**로 화면화 — 전체 일치/불일치 배지와 차원별(all/model/provider/project/cost_center) 요청·토큰(DB→CH)·요청 차이·비용 차이를 표로 표시하고 불일치 차원은 경고 강조. 불일치 시 ClickHouse 탭(재적재·table-info)으로 안내. UI 전용 슬라이스(백엔드 재사용)
v0.31.0:- **DW 대시보드 — Text2SQL 분석 패널 (v0.31.0)**: DW 대시보드 3차 슬라이스. 신규 API `GET /admin/dw/dashboard/text2sql?window=` — `text2sql_fact`(CLICKHOUSE_TEXT2SQL_FACT_TABLE)에서 질의 수·유효(valid)·실행(executed)·**차단·차단율**·평균 EXPLAIN risk·비용을 집계하고, **모드별(preview/execute)** 분해와 **실패 원인(failure_category) Top 10**을 반환(질문·SQL 원문은 미노출, 마스킹된 fact만). 대시보드에 Text2SQL 패널(요약 카드·모드별 표·실패 원인 표·Text2SQL 탭 deep link) 추가. fact 테이블 미설정 시 `configured:false`. OpenAPI(`dw` 태그) 반영
v0.30.0:- **DW 대시보드 — CSV Export + DW Health 패널 (v0.30.0)**: v0.29.0 DW 대시보드 2차 슬라이스. (1) **CSV 내보내기** `GET /admin/dw/dashboard/export.csv?window=&dimension=&order_by=&limit=` — 현재 필터 기준 Top-N(차원 지정 시) 또는 일별 시계열을 **UTF-8 BOM CSV**로 다운로드(Excel 한글 정상), export는 admin audit 기록. (2) 대시보드에 **DW 적재 상태(Health) 패널** — 기존 sink-status 재사용으로 dimension별 마지막 적재일·행수·갱신과 **실패 재처리 큐**(사유·시도수)를 표시하고 ``실패분 재처리`` 버튼(POST /admin/dw/sink-retry)·ClickHouse 상세 링크 제공(스펙의 ``적재 상태·지연·실패 큐·재처리 버튼 반드시 포함`` 충족). OpenAPI(`dw` 태그) 반영
v0.29.0:- **ClickHouse DW 대시보드 — MVP (v0.29.0)**: 어드민에 **``DW 대시보드`` 탭** 신설 — ClickHouse 일별 rollup을 장기 추세·비용 분석 전용으로 시각화. 신규 read 전용 API `GET /admin/dw/dashboard/overview`(KPI 카드: 요청·토큰·비용·오류율·요청당 비용·1K토큰당 비용)·`/timeseries`(일별 요청·토큰·비용·오류 추이)·`/dimensions?dimension=model|provider|project|cost_center&order_by=cost|requests|tokens|errors&limit=`(Top-N). 공통 필터 `window`(1d/7d/30d/90d)와 단일 차원 스코프(model/provider/project/cost_center → 해당 rollup dimension 선택). ReplacingMergeTree `FINAL`로 재전송 dedupe, ClickHouse 조회는 `FORMAT JSON`, SQL 리터럴 이스케이프·차원 화이트리스트로 인젝션 방지, ClickHouse 미설정 시 ``DW 비활성`` 응답·배너. UI는 KPI 카드·일별 비용 막대·Top-N 표(차원/정렬 선택). 운영 DB는 실시간 상세, ClickHouse는 장기 집계로 역할 분리 — Text2SQL/품질/라우팅/DW Health 패널은 후속 슬라이스
v0.28.0:- **거버넌스 스텝 관측 메트릭 (v0.28.0)**: v0.12~0.27에서 추가한 파이프라인 거버넌스 스텝(Skill·모델 일몰·Limits)의 동작을 `/metrics`(Prometheus)로 노출. 신규 카운터 `proxy_skill_blocked_total`(Skill 정책 차단)·`proxy_model_sunset_rewrites_total`/`proxy_model_sunset_blocked_total`(모델 일몰 자동 재작성/차단)·`proxy_limits_clamped_total`(출력 토큰 클램프·주입)·`proxy_limits_rejected_total`(요청 크기·메시지 수 거부). 각 스텝의 차단/재작성 경로에서 증가. 운영자가 ``가드가 실제로 얼마나 발동하는지``를 대시보드·알람으로 모니터링 가능
v0.27.0:- **메시지 개수 가드 (v0.27.0)**: limits 가드 패밀리(출력 토큰·입력 바이트·메시지 수) 완성. 신규 런타임 설정 `limits.max_messages`(0=비활성). >0이면 `limits` 스텝이 chat 요청 `messages` 배열 개수가 상한을 초과할 경우 **400 `too_many_messages`**로 거부하고 `X-Message-Count` 헤더 표기. 바이트 크기와 무관하게 컨텍스트 스터핑·과도한 멀티턴 누적(작은 메시지 수천 개 등)을 차단. env `LIMITS_MAX_MESSAGES`, 권한 그룹 `ops`, 런타임 즉시 반영
v0.26.0:- **요청 본문 크기 가드 (v0.26.0)**: v0.25.0 출력 토큰 상한과 짝을 이루는 **입력 측 가드**. 신규 런타임 설정 `limits.max_request_bytes`(0=비활성). >0이면 파이프라인 `limits` 스텝이 chat 요청 본문이 상한을 초과할 경우 **413 `payload_too_large`**로 거부(업스트림 호출 전 차단)하고 `X-Request-Bytes` 헤더로 실제 크기 표기. 비정상적으로 큰 프롬프트·남용·우발적 대용량 페이로드를 비용 발생 전에 차단. env `LIMITS_MAX_REQUEST_BYTES`, 권한 그룹 `ops`, 런타임 즉시 반영
v0.25.0:- **출력 토큰 상한 가드 (v0.25.0)**: 런어웨이 생성·비용 폭주를 막는 출력 토큰 클램프. 신규 런타임 설정 `limits.max_output_tokens`(0=비활성). >0이면 요청 파이프라인 `limits` 스텝(deprecation 직후·governance 직전)이 chat 요청의 `max_tokens`/`max_completion_tokens`를 상한으로 **클램프**(초과 시 축소, 미지정 시 상한 주입)하고 `X-Max-Tokens-Clamped`(예: `100000->512` 또는 `injected:512`) 헤더로 표기. 재배포 없이 즉시 반영(런타임 오버레이), 권한 그룹 `ops`(ops_admin 관할). env `LIMITS_MAX_OUTPUT_TOKENS`로 기본값 지정 가능
v0.24.0:- **모델 일몰 정책 어드민 UI (v0.24.0)**: v0.23.0에서 API/Swagger로만 관리하던 모델 폐기 정책을 어드민 대시보드 **``모델 일몰`` 탭**으로 노출. 정책 목록(model_glob·대체 모델·일몰일·**현재 단계** 배지(경고만/일몰 예정/자동 재작성/차단)·메시지), 추가 폼(glob·대체 모델·일몰일 date picker·안내 메시지), 삭제 버튼 제공. 현재 단계는 일몰일과 대체 모델 유무로 자동 판정해 표시. 운영자가 curl 없이 구모델 은퇴 정책을 관리·현황 점검 가능
v0.23.0:- **모델 일몰(Sunset)·폐기 정책 (v0.23.0)**: 구형 모델의 생애주기를 요청 경로에서 관리. `model_deprecations`(model_glob·replacement·sunset_date·message) 테이블 + 관리 API `GET/POST /admin/model-deprecations`·`DELETE …/{id}`. 요청 파이프라인에 `deprecation` 스텝 신설(라우팅·skill 직후) — 요청 모델이 폐기 glob에 매칭되면 응답에 `X-Model-Deprecated`/`X-Model-Replacement`/`X-Model-Sunset`/`X-Model-Deprecation-Message` 헤더를 부여하고, **일몰 전에는 경고만**, **일몰일(UTC) 도달 후에는** 대체 모델이 있으면 **자동 재작성**(`X-Model-Sunset-Rewritten`, provider 선택에 반영) · 없으면 **400 `model_sunset`** 차단. 30초 캐시 스냅샷(변경 시 무효화), glob 매칭은 기존 라우팅과 동일. 이제 ``구모델을 점진 경고→자동 이전→차단``으로 안전하게 은퇴시킬 수 있음(현재 API/Swagger로 관리)
v0.22.0:- **예산 경보 (Budget Alerts) (v0.22.0)**: 월 예산 소진 예측(v0.6.x)을 **경보**로 승격. 신규 API `GET /admin/budgets/alerts?warn=&critical=&notify=&all=` — 설정된 모든 예산을 burn ratio·projected ratio로 **ok/warn/critical** 분류(임계값 기본 0.8/1.0, 쿼리로 조정; ``예상 소진율>100%``면 burn이 낮아도 warn). 기본은 경보 대상(warn+critical)만 반환, `all=1`이면 전체. `notify=1`이면 경보 요약을 **Mattermost로 푸시**(기존 알림 연계 재사용). 어드민 ``월 예산 소진 예측`` 카드에 ``예산 경보 확인``·``경보+Mattermost 알림`` 버튼. read-only 분석(예산 자체는 미변경), OpenAPI(`cost` 태그) 반영. 운영자가 예산 초과 추세 팀/키를 한 번에 점검·통지 가능
v0.21.0:- **Skill 일일 호출 한도 (v0.21.0)**: 비싸거나 위험한 Skill의 ``폭발 반경``을 제한하는 일일 실행 캡 추가. Skill에 **`daily_limit`**(UTC 일 기준 최대 실행 수, 0=무제한). 요청이 `X-Vibe-Skill`로 Skill을 지정하면 오늘의 **실제 실행 수(ok+error, blocked는 미차감)**를 `skill_runs`에서 집계해 한도 도달 시 enforce 모드는 **429 `skill_rate_limited`**(Retry-After 헤더)로 차단, warn은 경고만. 응답에 `X-Vibe-Skill-Daily-Used`/`-Limit` 헤더. `skills` 테이블 컬럼 추가(기존 DB 멱등 ALTER)·신규 store `CountSkillRunsSince`, 업서트/조회/공개 상세/편집기에 반영. 모델·도구·팀 정책에 ``사용량`` 차원 추가
v0.20.0:- **Skill 팀 접근 제어 (v0.20.0)**: Skill에 **`allowed_teams`**(콤마 구분 팀 glob, 비우면 전체 허용) 정책 힌트 추가 — `allowed_models`/`allowed_tools`와 대칭. 요청이 `X-Vibe-Skill`로 Skill을 지정하면 **호출자 팀(API 키의 team)**을 allowed_teams와 대조해 enforce 모드에서 미허용 팀은 차단(warn은 경고). `skills` 테이블에 컬럼 추가(기존 DB는 멱등 ALTER), 업서트/조회/Export·Import·평가(dry-run에 `team` 입력)·공개 상세(`GET /v1/skills/{name}`)에 반영. 어드민 Skills 편집기에 allowed_teams 입력. 이제 ``특정 Skill을 특정 팀만 사용``하도록 거버넌스 가능 — 모델·도구·팀 3차원 정책 완성
v0.19.0:- **Skill GitOps — Export/Import (v0.19.0)**: 큐레이션한 Skill을 버전 관리하고 게이트웨이 배포 간(dev→prod) 이동할 수 있는 portable 번들. 신규 API `GET /admin/skills/export?status=`(매칭 Skill 전체 레코드를 `{version, skills:[…]}` 번들로)·`POST /admin/skills/import`(번들 업서트, name 기준 멱등). Import는 status/risk/metadata 검증 + **보안 게이트 보존**(production으로 가져오는 Skill이 v0.15.0 스캐너에서 high 발견 시 silently 게시하지 않고 **건너뜀·사유 보고**) — 가져온/건너뛴 목록을 응답. 어드민 Skills 탭 ``내보내기``(JSON 다운로드)·``가져오기``(파일 업로드) 버튼. OKF(v0.9.4)·설정(v0.8.6) export/import와 동일 패턴. OpenAPI(`skills` 태그) 반영
v0.18.0:- **ClickHouse Skill fact 적재 (v0.18.0)**: v0.13.0 `skill_runs`(로컬)를 ClickHouse 행동 로그 DW로 확장 — `ai_skill_fact` 테이블 추가, Skill 실행 1건당 1행(skill/version/actor/model/status(ok/error/blocked)/tools_used/cost_krw/latency_ms)을 **best-effort 직접 적재**(저빈도라 배치 큐 대신 즉시 전송, 실패 시 retry 큐 보존; 프롬프트 원문 미전송). `skill` 파이프라인 스텝의 run 기록 경로에서 함께 emit. 런타임 설정 `clickhouse.skill_fact_table`(미설정 시 비활성)·env `CLICKHOUSE_SKILL_FACT_TABLE`, bootstrap DDL·lag/overview 상태에 포함. 이제 ``어떤 Skill이 비용/지연이 큰가·정책 위반으로 자주 막히는가``를 ai_request_fact·ai_tool_fact 등과 조인해 분석 가능 — Skills 도메인이 통합 행동 로그 DW에 합류
v0.17.0:- **Skill 지침 주입 — 거버넌스에서 실행으로 (v0.17.0)**: 그동안 Skill은 정책·승격·관측 ``대상``이었을 뿐 호출에 실제 적용되지 않았는데, 이제 요청이 `X-Vibe-Skill` 헤더로 **production Skill**을 지정하면 그 **instructions를 system 메시지로 프롬프트 맨 앞에 주입**(X-Vibe-Knowledge 주입과 동일 방식)해 요청이 해당 매뉴얼 하에 실행됨. 파이프라인 `skill` 스텝에서 정책 검사 통과 후 `rc.body`를 재작성(messages 배열이 있는 chat 요청만, 파싱 실패 시 안전 no-op), 적용 시 `X-Vibe-Skill-Applied: 1` 헤더. enforce 모드 차단 시에는 주입 안 함; warn 모드 위반은 헤더 경고 후 주입. 이로써 Skill이 ``governance 객체``에서 ``실행 가능한 능력``으로 완성 — 추천→draft→승격→스캔→**적용**→enforce→관측 루프 완결
v0.16.0:- **OKF 기반 Skill 추천 (v0.16.0)**: 반복되는 Text2SQL 질문 패턴(report-candidate 마이너 재사용)에서 **표준화 가능한 Skill 초안을 자동 제안**하는 self-improvement 루프의 Skill판. 신규 API `POST /admin/skills/recommend?window=&min_count=&apply=` — 정규화 질문 빈도가 임계 이상인 클러스터마다 `answer-<slug>` Skill 후보(검증된 SQL 패턴을 instructions에 포함, metadata에 origin/count/데이터상품)를 만들고, **기본은 dry-run(미생성)**·`apply=1`이면 **draft로 멱등 생성**(이미 있는 이름은 재제안 안 함). 추천 Skill은 항상 **draft** — 사람이 검토 후 v0.14.0 승격 게이트·v0.15.0 보안 스캔을 거쳐 production으로 올림(human-in-the-loop, 자동 노출 없음). 어드민 Skills 탭 ``Skill 추천`` 버튼(제안 미리보기 → ``draft로 적용``). 이로써 Skills 에픽이 추천→draft→승격(게이트)→스캔→enforce→관측의 완결 루프를 형성. OpenAPI(`skills` 태그) 반영
v0.15.0:- **Skill 보안 스캐너 + production 보안 게이트 (v0.15.0)**: Skill 정의(instructions·metadata·정책 힌트)를 정적 점검하는 보안 스캐너 추가. 탐지: **임베딩 시크릿**(기존 secret firewall 패턴 재사용, 타입만 보고·값 미노출, high)·**프롬프트 인젝션/탈옥 문구**(기존 injection 시그니처 재사용, high)·**파괴적 명령**(rm -rf·DROP TABLE·`| bash`·sudo 등 instructions에 그대로 존재, medium)·**정책 위생**(high-risk인데 allowed_tools 무제한 medium·allowed_models 무제한 low·production인데 instructions 공백 medium). 신규 API `GET /admin/skills/scan?name=`(단건 상세)·`GET /admin/skills/scan`(전체 요약). **승격 게이트 강화**: `to=production` 승격 시 스캔에서 **high 발견이 1건이라도 있으면 422 `security_gate`로 차단**(v0.14.0 전이/instructions/사유 게이트에 보안 게이트 추가). 어드민 Skills 탭 ``보안 스캔`` 버튼(전체 스킬 심각도·발견 요약). OpenAPI(`skills` 태그) 반영
v0.14.0:- **Skill 버저닝·승격 게이트 워크플로 (v0.14.0)**: Skill 생애주기 전이를 **검증·기록**하는 거버넌스 경로 추가. 신규 API `POST /admin/skills/promote`{name,to_status,version,note} — **허용 전이만**(draft→staging→production, production/staging→deprecated, deprecated/production→staging 롤백 등; **draft→production 직행 차단** = staging 게이트 필수) 통과시키고, **production 승격 게이트**(instructions 필수, **high-risk는 사유(note) 필수**) 위반 시 422 `promotion_gate`. 전이는 `skill_promotions`(from/to status·version·actor·note) 테이블에 이력으로 적재, 버전 동시 변경 가능. 조회 `GET /admin/skills/promotions?skill=&limit=`. 어드민 Skills 탭 행에 ``승격``·``이력`` 버튼(대상 상태/사유 입력). 기존 `POST /admin/skills` 업서트는 로우레벨 편집용으로 유지하되, **승격은 게이트 경로 권장**. OpenAPI(`skills` 태그) 반영
v0.13.0:- **Skill 실행·비용 관측 대시보드 (v0.13.0)**: v0.12.0이 적재하는 `skill_runs`를 Skill별로 집계해 **운영 가시성**을 제공. 신규 API `GET /admin/skills/stats?window=`(기본 30일) — Skill 1개당 실행수·성공(ok)·오류(error)·**차단(blocked)·차단율**·총비용(₩)·평균 지연(ms)·distinct 사용자수·마지막 실행 시각을 busiest 순으로 반환(시간창은 1h/24h/7d/30d 또는 Go duration). 어드민 **Skills 탭 상단에 ``실행·비용 요약`` 표** 추가(차단율은 경고 배지). 집계는 sqlite/PostgreSQL 공통 SQL(GROUP BY + 조건부 SUM)로 수행, OpenAPI(`skills` 태그) 반영. 이제 ``어떤 Skill이 비용을 많이 쓰는지·정책 위반으로 자주 막히는지·실제로 쓰이는지``를 한눈에 점검 가능
v0.12.0:- **Skill 정책 enforce 엔진 (v0.12.0)**: v0.11.0 Skill 레지스트리의 `allowed_models`/`allowed_tools` 정책 힌트를 **요청 경로에서 실제로 적용**. 요청이 `X-Vibe-Skill` 헤더로 Skill을 지정하면 게이트웨이가 **선택된 모델·선언된 도구**를 해당 Skill 정책(콤마 구분 glob)과 대조 — 요청 파이프라인에 `skill` 스텝 신설(라우팅 직후·거버넌스 직전 → 모델 재작성 반영). 적용 모드는 런타임 설정 `skills.enforcement`(**off**=비활성·**warn**=위반 시 `X-Vibe-Skill-Policy: warn` 헤더만·**enforce**=위반/비-production Skill 지정 시 403 `skill_policy_violation` 차단, 기본 **warn**으로 안전 롤아웃). 모든 판정은 `skill_runs`에 비동기 기록(차단=blocked·완료=ok/error, 모델·비용·지연·도구 포함; ClickHouse/프록시 요청은 절대 실패시키지 않음). 운영자용 **드라이런** `POST /admin/skills/evaluate`{name,model,tools}(정책 시뮬레이터의 Skill판 — 차단 여부 미리보기)와 **추천 Skill 시드** `POST /admin/skills/seed-recommended`(text2sql-safety-test-generator·prompt-regression-reviewer·mcp-tool-risk-classifier를 draft로 멱등 생성) 추가. 어드민 Skills 탭에 ``추천 Skill 시드`` 버튼·enforce 모드 안내. 설정 권한 그룹은 `security`(security_admin 관할)
v0.11.0:- **Skill 레지스트리 기반 (v0.11.0)**: 재사용 가능한 AI 작업 매뉴얼을 거버넌스 대상으로 등록·승격·노출하는 **Skill 레지스트리** 도입(첫 슬라이스). `skills`(name PK·description·version·owner·**status**(draft/staging/production/deprecated)·**risk_level**(low/medium/high)·**allowed_models/allowed_tools** 정책 힌트·instructions·metadata(JSON)·수정 이력)와 `skill_runs`(Skill 실행 로그 — skill·version·actor·input_hash·tools_used·model·status·cost_krw·latency_ms) 테이블 추가. **호출자용 공개 API** `GET /v1/skills`(=production 상태만, 지침 제외 목록)·`GET /v1/skills/{name}`(지침+정책 힌트 포함) — 프록시 키 인증. **관리 API** `GET/POST /admin/skills`(전체 상태 목록·생성/업서트, status·risk·metadata 검증)·`GET|DELETE /admin/skills/by-name/{name}`·`GET /admin/skills/runs?skill=&limit=`(실행 로그). 어드민 **``Skills`` 탭** 신설(상태 필터·등록/편집·삭제·실행 로그 조회), OpenAPI(`skills` 태그)·Swagger 반영. ``production만 외부 노출``로 미검증 Skill 유출 차단, allowed_models/tools는 후속 정책 enforce 엔진의 토대
v0.10.8:- **가격 폴백 모델·환율 런타임 설정화 (v0.10.8)**: v0.10.7의 미매칭 모델 폴백을 **런타임에서 조정** 가능하게 함 — 신규 설정 `pricing.fallback_model`(미매칭 모델 과금 기준 모델, 기본 qwen-plus)·`pricing.usd_krw`(카탈로그 시드 환율, 기본 1380). `pricing.fallback_model` 변경은 **재배포 없이 즉시** 비용 계산에 반영(`audit.SetFallbackPriceModel` 원자 적용), `pricing.usd_krw`는 변경 후 `/admin/pricing/seed?overwrite=1` 재시드 시 적용. env(`PRICING_FALLBACK_MODEL`·`PRICING_USD_KRW`)로도 기본값 지정 가능. 어드민 ``런타임 설정``의 `pricing` 카테고리에서 조회·변경
v0.10.7:- **모델 가격 미매칭 시 qwen-plus 단가로 폴백 (v0.10.7)**: 가격표에 **정확/프리픽스 매칭이 없는 모델**의 비용을 0으로 처리하던 동작을 변경 — 이제 `qwen-plus` 단가(`audit.FallbackPriceModel`)로 계산. 비용 계산 핵심 경로(`EstimateCostKRW`/`ModelPriced`)와 explain 절감 계산(`lookupModelPrice`) 양쪽에 적용돼 신규/미등록 모델도 합리적 기본가로 과금·집계됨(가격표에 qwen-plus 항목이 있을 때, 없으면 종전대로 0). 특정 모델 단가는 `/admin/pricing` 등록이 항상 우선
v0.10.6:- **ClickHouse 정책 결정 fact 적재 — DW 에픽 완료 (v0.10.6)**: `ai_policy_fact` 테이블 추가 — 거버넌스 정책 결정 1건당 1행(request_id·team·endpoint·phase·policy/rule·decision(allow/block/approval)·reason·risk_score·complexity·cost)을 정책 판정 경로에서 **best-effort 적재**(실패 시 retry 큐 보존). bootstrap DDL·런타임 설정 `clickhouse.policy_fact_table`·lag/events 상태에 포함. 이로써 **요청·도구·라우팅·평가·피드백·정책·Text2SQL** 전 영역이 ClickHouse 행동 로그 DW로 적재돼 비용·성능·품질·보안·거버넌스를 요청 단위로 교차 분석 가능 — 일별 rollup 중심에서 ``완전한 행동 로그 DW``로 전환 완료
v0.10.5:- **ClickHouse 사람 피드백 fact 적재 (v0.10.5)**: `ai_feedback_fact` 테이블 추가 — `/admin/llm/feedback` 기록 시 피드백 1건당 1행(request_id·trace_id·rating·label·source·created_by)을 **best-effort 직접 적재**(저빈도라 배치 큐 대신 즉시 전송, 실패 시 retry 큐에 보존). bootstrap DDL·런타임 설정 `clickhouse.feedback_fact_table`(미설정 시 비활성)·lag/events 상태에 포함. 이제 ``어떤 모델/프롬프트가 사람 피드백이 나쁜가``를 ai_request_fact·ai_eval_fact와 조인해 분석 가능
v0.10.4:- **ClickHouse Materialized View — 대시보드용 자동 집계 (v0.10.4)**: bootstrap이 `ai_request_fact` 위에 **일별·시간별 집계 Materialized View**를 생성 — `<fact>_daily`·`<fact>_hourly`(SummingMergeTree, team·provider·model별 requests·total_tokens·cost_krw·errors)와 이를 채우는 MV(`toStartOfHour(event_time)` 기반 시간 집계 포함). 이제 비용/지연/오류 대시보드 쿼리가 원본 fact 전체 스캔 없이 작은 집계 테이블만 읽어 가볍고 빠름. 요청 fact가 설정된 경우 bootstrap에서 함께 생성
v0.10.3:- **ClickHouse fact 적재 상태 UI + lag/events API (v0.10.3)**: 어드민 **ClickHouse 탭에 ``Fact 적재 상태`` 섹션** 추가 — 비동기 큐 깊이/용량/드롭, request_fact 적재 행수와 로컬 요청 수 대비 lag, 재처리 배치 수, 그리고 설정된 fact/rollup 테이블별 **ClickHouse 행수**를 한눈에 표시하고 테이블별 ``최근 보기``로 실제 적재 행을 조회. 신규 API `GET /admin/dw/clickhouse/lag`(테이블별 행수·큐 lag·드롭·retry 집계)·`GET /admin/dw/clickhouse/events?table=&limit=`(설정된 fact 테이블의 최근 행 read-only 조회, 화이트리스트 검증). 운영자가 ``정말 적재되고 있는지``를 즉시 검증 가능
v0.10.2:- **ClickHouse Text2SQL fact 확장 (v0.10.2)**: 기존 Text2SQL fact 적재(`CLICKHOUSE_TEXT2SQL_FACT_TABLE`)에 **`reject_reason`(검증 거부 사유)·`sql_hash`(생성 SQL 해시, 원문 미전송)** 컬럼 추가 — ``Text2SQL 실패 원인``(reject_reason·failure_category·schema·mode별)과 동일 SQL 형태 클러스터링 분석 강화. bootstrap DDL·fact sink 동시 확장(질문/SQL 원문은 여전히 미전송, question_chars·sql_hash만)
v0.10.1:- **ClickHouse 세부 이벤트 fact 분리 적재 — tool·routing·eval (v0.10.1)**: v0.10.0의 비동기 fact 파이프라인을 확장해 **`ai_tool_fact`(MCP/도구 호출 1건당 1행: server·tool·source·is_mcp·is_error·arg_hash)·`ai_routing_fact`(요청당 1행: 요청/선택 모델·provider·complexity·risk·health·fallback_path·decision_reason)·`ai_eval_fact`(LLM 평가 1건당 1행: name·category·score·label·passed)** 를 **같은 요청 큐에서 한 번에 fan-out** 적재(요청당 추가 비용 없음, 각 테이블 독립 INSERT·독립 retry). routing/eval은 `ReplacingMergeTree`로 멱등, tool은 append. bootstrap이 세 DDL 자동 생성, 런타임 설정 `clickhouse.tool_fact_table`·`routing_fact_table`·`eval_fact_table`로 개별 on/off(미설정 시 비활성). 이제 ``어떤 MCP 도구가 위험한가``·``자동 라우팅이 비용을 줄였나``·``어떤 프롬프트 버전 품질이 낮은가``를 ClickHouse에서 직접 분석 가능
v0.10.0:- **ClickHouse 요청 단위 fact 적재 (행동 로그 DW) (v0.10.0)**: 기존 일별 rollup sink는 그대로 유지하면서, **요청 1건당 1행**을 ClickHouse `ai_request_fact`로 보내는 **비동기 배치 파이프라인** 추가 — 팀·모델·provider·프로젝트·프롬프트·라우팅·토큰·비용·지연·오류를 요청 단위로 분석 가능. 요청 경로에서 직접 호출하지 않고 내부 **bounded 큐 → 배치 flush**(`clickhouse.batch_size`·`clickhouse.flush_interval`, 큐 가득 시 드롭 카운트), `ReplacingMergeTree(ingested_at)` + `ORDER BY (event_date,team,provider,model,request_id)`로 **재전송 멱등**, `JSONEachRow`에 `best_effort`+`skip_unknown_fields` 적용. **개인정보 보호**: 프롬프트/응답 원문 미전송, client IP는 해시. ClickHouse 장애 시 요청은 실패하지 않고 실패 배치는 `clickhouse_fact_retry`에 저장→`POST /admin/dw/clickhouse/fact-retry`로 재처리. `clickhouse.request_fact_table` 설정 시 활성화(미설정 시 완전 비활성), bootstrap이 `ai_request_fact` DDL 생성, overview에 큐 깊이·드롭·retry 노출
v0.9.9:- **ClickHouse 수동 적재 — 집계 후 전송 (v0.9.9)**: ``지금 적재``(`POST /admin/dw/clickhouse`)가 로컬 일별 rollup을 **읽기만** 하고 집계는 안 해서, 보존 워커(기본 1h)가 아직 안 돌았거나 당일 rollup이 비어 있으면 트래픽이 있어도 ``적재 완료 0행``이 뜨던 문제 수정. 이제 적재 전에 해당 기간을 **`RollupRange`로 먼저 집계**한 뒤 전송하므로 버튼만으로 현재 데이터가 반영됨. 참고: DW로 보내는 데이터는 일별 집계 행(`day·dimension(all/model/provider/project/cost_center)·dim_value·requests·tokens·cost_krw·errors`)이며, fact 테이블 설정 시 Text2SQL 질의 단위 fact도 전송
v0.9.8:- **OpenAPI 전체 API 망라 (v0.9.8)**: `/openapi.json`을 큐레이션 subset에서 **전체 HTTP 서피스 망라**로 확장 — 라우트 등록 목록과 동기화된 엔드포인트 카탈로그(`apiEndpoints`)에서 스펙을 코드로 생성(135+ 경로). inference·auth·self-service·admin·settings·cost·routing·governance·text2sql·okf·dw·llm·mcp·vcs·notifications 태그로 그룹화, 경로 파라미터(`{id}`/`{key}`/`{name}`/`{server}`/`{team}`/`{user_id}`)·메서드(GET/POST/PUT/PATCH/DELETE)·보안(공개 vs bearerAuth) 표기. Swagger UI(`/swagger`)에서 전 API 탐색 가능
v0.9.7:- **어드민 UX — 마지막 사용자 메시지·앱 버전·세션 만료·API 문서 (v0.9.7)**: (1) **요청 상세 모달** 최상단(요청 ID 위)에 **마지막 사용자 메시지**를 가독성 높은 강조 카드로 표시(마크다운 렌더). (2) 개인 메뉴의 **단축키 도움말 아래에 앱 버전**(`__APP_VERSION__`, 서버 주입)을 노출. (3) **로그아웃 버튼 아래에 세션 만료 예정 시각**(access token exp, `/auth/me`의 `expires_at` 기반, 남은 분 표시). (4) 개인 메뉴에 **Swagger API 페이지(`/swagger`)와 `openapi.json` 링크** 추가 — 공개 GET `/openapi.json`(주요 엔드포인트 큐레이션 스펙, info.version=앱 버전)·`/swagger`(Swagger UI, 폐쇄망에서는 openapi.json 직접 사용 안내)
v0.9.6:- **OKF 게이트웨이 지식그래프 + 에이전트 자기개선 루프 (v0.9.6)**: (1) **지식그래프 sync**(`POST /admin/okf/graph/sync`) — API Key→소유자(user)·소속팀(team), 모델 glob→서빙 업스트림(`served_by`) 관계를 OKF 링크로 구성해 ``왜 이 요청이 이 모델/업스트림으로 갔는지``를 설명 가능하게 함(엔티티 문서 + 엣지 자동 생성, 멱등). (2) **자기개선 루프**(`POST /admin/okf/propose?window=&min_count=`) — 반복되는 Text2SQL 질문(report candidate)을 **status=proposed** OKF sample_sql 문서로 자동 제안하고, 사람이 검토 후 status=active로 재저장하면 활성 지식이 됨(자동 반영 없음 — human-in-the-loop). 이로써 프록시가 조직 AI 컨텍스트 허브로서 스스로 지식을 축적
v0.9.5:- **Text2SQL OKF 메타지식 주입 (v0.9.5)**: OKF 지식을 Text2SQL 프롬프트에 연결해 정확도↑·hallucination↓. SQL 생성 프롬프트에 **요청 허용 테이블로 스코프된** OKF 메타지식 블록(테이블 설명·조인 경로·금지/주의 쿼리 패턴·샘플 SQL·추가 지침)을 스키마 컨텍스트 바로 뒤에 주입(`WithOKFKnowledge`). 스키마 레지스트리·골든쿼리로부터 OKF 문서를 자동 생성하는 시드 API `POST /admin/okf/text2sql/sync?schema=`(테이블/컬럼 설명→table/column 문서, 골든→sample_sql 문서, 멱등) 추가. **OKF 문서가 없으면 완전 no-op**이라 무설정 안전, 큐레이션할수록 생성 품질이 올라감
v0.9.4:- **OKF 지식 기반 — 문서·링크·Export/Import (v0.9.4)**: 조직 AI 컨텍스트를 **OKF(Open Knowledge Format)** 로 관리하는 기반 도입. `okf_documents`(kind=table/column/join_path/forbidden_query/sample_sql/model_policy/entity/glossary 등 + subject·title·body·attributes(JSON)·tags·status)와 `okf_links`(from–relation–to 지식그래프 엣지) 테이블 + CRUD API(`GET/POST /admin/okf/documents`, `GET/DELETE …/by-id/{id}`, `GET/POST /admin/okf/links`). **Export**(`GET /admin/okf/export?kind=&subject=&tag=&status=` — 매칭 문서 + 연결 링크를 portable 번들로)와 **Import**(`POST /admin/okf/import` — 외부 DB 문서·업무 규칙·모델 정책 번들 가져오기, (kind,subject)·(from,relation,to) 키로 멱등). 이후 Text2SQL 메타지식 주입·게이트웨이 지식그래프·자기개선 루프의 공통 저장소
v0.9.3:- **모델 가격표 현재가 자동 시드 (v0.9.3)**: 내장 가격 카탈로그를 **현행 모델 ID**(Claude Opus 4.8/4.7/4.6·Sonnet 4.6/4.5·Haiku 4.5·Fable 5, GPT-5.x·4o·o3·o4-mini, Gemini 3, Qwen/Kimi 등)로 갱신하고, **첫 기동 시 가격 테이블이 비어 있으면 카탈로그를 자동 적용**(`seedPricingIfEmpty`). 이제 별도 시드 호출 없이도 합리적인 현재가가 미리 들어가 비용 계산이 바로 동작하며, 운영자는 `POST /admin/pricing`(모델별 새 버전)·`/admin/pricing/seed?overwrite=1`(카탈로그 재적용)로 언제든 덮어쓸 수 있음(버전 이력 유지). 기존 관리 가격이 있으면 부팅 시 절대 건드리지 않음
v0.9.2:- **시맨틱 캐시 — 멀티턴 자동 스킵 게이팅 (v0.9.2)**: 시맨틱 chat 캐시가 **단발성(single-turn) 요청에만** 동작하도록 게이팅. 시맨틱 캐시는 messages 배열 **전체**를 임베딩해 코사인 유사도로 비교하는데, 멀티턴 에이전트 대화는 맥락이 계속 누적돼 (1) 이전 엔트리와 거의 적중하지 않아 임베딩 호출만 낭비되고 (2) 우연히 적중하면 다른 맥락의 답을 줄 위험이 있음. 이제 assistant/tool 턴이 있거나 tools가 선언된 요청은 임베딩을 **건너뜀**(저장도 안 함) — 같은 질문 재요청 같은 안전·고확률 케이스에만 캐시가 동작. 멀티턴까지 강제로 켜려면 신규 런타임 설정 `cache.chat_semantic_multiturn=true`(기본 off). 임베딩 비용 낭비 제거 + 오적중 방지
v0.9.1:- **ClickHouse 설정·모니터링 콘솔 (v0.9.1)**: 어드민에 **ClickHouse 탭** 신설 — 한 화면에서 연결 상태(ping·지연), rollup 테이블 존재/엔진/dedupe 키 점검, 자동 적재 on/off·주기, dimension별 적재 워터마크, 실패 재처리 대기열을 보여주고 **연결 테스트·테이블 생성·지금 적재·실패분 재처리·정합성 확인** 버튼을 제공. 신규 API `POST /admin/dw/clickhouse/bootstrap`(rollup·fact 테이블을 ``IF NOT EXISTS``로 자동 생성 — ReplacingMergeTree + (day,dimension,dim_value) 정렬키 보장)·`GET /admin/dw/clickhouse/overview`(단일 호출 상태 집계). 또한 Text2SQL fact 적재 시 RFC3339 타임스탬프가 DateTime 컬럼에 들어가도록 ``date_time_input_format=best_effort`` 적용. 이제 운영자가 DDL을 수기로 작성하거나 curl 없이 설정→생성→적재→검증을 클릭으로 수행
v0.9.0:- **시맨틱 캐시 — 임베딩 전용 프로바이더/URL (v0.9.0)**: 시맨틱 chat 캐시의 임베딩 호출 대상을 분리할 수 있게 함. 기존에는 임베딩이 일반 chat과 동일한 프로바이더 선택(모델 glob→기본 업스트림)을 그대로 타서 별도 URL 지정이 불가능했음. 신규 설정 `cache.embedding_base_url`(설정 시 ``{base_url}/v1/embeddings``로 **직접 호출** — 예: 사내 로컬 임베딩 서버)·`cache.embedding_provider`(임베딩에 강제할 등록 프로바이더)·`cache.embedding_api_key`(전용 키, 암호화 저장·마스킹) 추가. **세 값이 모두 비어 있으면 기존 동작 그대로**(프로바이더 라우팅) 유지하고, base_url만 주고 key를 비우면 기본 업스트림 키로 폴백. 런타임 설정이라 재배포 없이 즉시 반영
v0.8.9:- **프록시 API 키 — 팀 ID를 팀명으로 표기 (v0.8.9)**: 사용자(Proxy API 키) 목록·상세에서 팀이 **팀 ID**(예: `team_security`)로 보이던 문제 수정. 셀프서비스(``내 키``)로 발급한 키는 사용자 정체성의 팀 ID를 저장하는데, 어드민 화면이 그 값을 그대로 출력해 ID가 노출됐음. 이제 `/admin/users`(목록·상세) 응답에 `auth_teams` 기반 `team_names`(ID→표시명) 매핑을 포함하고, UI는 **팀 표시명을 크게·ID를 작은 글씨**로 렌더(`#/teams/{id}` 링크·필터는 ID 유지). 매칭되는 auth team이 없는 자유 입력 팀은 기존처럼 입력값 그대로 표기
v0.8.8:- **런타임 관리자 설정 — 키별 상세 설명 (v0.8.8)**: 런타임 설정 화면의 각 설정 키에 **무엇을 바꾸는지 한 줄 도움말**을 작은 글씨로 표기 — ClickHouse·Text2SQL·Carbon·Insurance·Cache·Retention 전 키(50여 개)에 대해 용도·단위·예시·재연결 영향(예: ``자동 적재 주기(예: 1h). 변경 시 sink 워커 재시작``)을 키 코드 아래 회색 캡션으로 노출. 설명은 API 응답에도 `description` 필드로 포함되어 외부 도구·문서화에서 재사용 가능. 운영자가 키 이름만 보고 추측하지 않도록 함
v0.8.7:- **런타임 관리자 설정 — 역할별 권한 (v0.8.7)**: 런타임 설정 쓰기를 역할 범위로 제한(스펙 §11) — 설정 전용 서브 관리자 역할 `ops_admin`(ClickHouse·Retention·Cache)·`ai_admin`(Text2SQL 모델·dialect·shadow·clarify 등)·`security_admin`(마스킹·일일 위험 한도/경고·Replay 보존·모든 **secret** 키)을 추가하고, 각 설정 키를 **권한 그룹**(secret 및 mask_results·daily_risk_limit/warn·replay_bundles→`security`, ClickHouse/Retention/Cache→`ops`, 그 외 Text2SQL→`ai`, Carbon/Insurance→`admin`)으로 분류. 서브 관리자는 `admin:read`만 보유해 다른 어드민 엔드포인트는 못 쓰고, `/admin/settings` 하위에서 **자기 그룹만** 쓰기 가능(PUT·DELETE·bulk·import·rollback 모두 키 단위로 검증해 위반 시 403 `settings_role_denied`/422). `admin`·`super_admin`(및 토큰 모드)은 전체 쓰기 유지, `readonly_admin`은 읽기·이력만. 설정 목록 응답에 키별 `permission_group`·`can_write` 노출
v0.8.6:- **런타임 관리자 설정 — 일괄 적용·내보내기/가져오기 (v0.8.6)**: GitOps 친화 일괄 API 추가 — `PUT /admin/settings/bulk`(여러 설정 원자 적용: 전체 검증 후 일괄 쓰기·1회 reload, 하나라도 실패하면 전부 미적용으로 부분 적용 장애 방지), `GET /admin/settings/export`(admin 오버레이 중 **비밀값 제외** 키·값을 import 가능한 형식으로), `POST /admin/settings/import`(export 형식 가져오기, **secret 키는 거부**하고 개별 설정 요구). default_limit<=max_limit 교차 검증 포함
v0.8.5:- **런타임 관리자 설정 — Cache·Retention 확장 (v0.8.5)**: 레지스트리에 **Cache**(embedding/chat/semantic on·off·TTL·임계값 등 9키)와 **Retention**(request/prompt/response/text2sql_replay days·interval 5키) 추가 — Cache는 `s.cacheConf()` 오버레이로 다음 요청부터 즉시 반영, Retention은 워커를 **런타임 reconfigure** 가능하게 리팩터(일수는 다음 실행부터, interval 변경 시 ticker 재생성, atomic 보호)해 재배포 없이 보관정책 변경. 변경 이력·롤백·연결 무관 검증 그대로
v0.8.4:- **런타임 관리자 설정 — Carbon·Insurance 확장 (v0.8.4)**: 런타임 설정 레지스트리를 **Carbon(탄소 점수 계수)·Insurance(SLA 기준)** 카테고리로 확장 — `carbon.wh_per_1k_tokens`·`carbon.pue`·`carbon.grid_intensity_g`, `insurance.sla_target`·`insurance.fast_burn`·`insurance.slow_burn`를 어드민 화면에서 조회·변경하면 atomic 스냅샷(`s.carbonConf()`/`s.insuranceConf()`)으로 즉시 반영되어 Prompt Carbon Score·AI 요청 보험/번레이트 리포트의 계산 기준이 재배포 없이 갱신(검증: rate 0~1, 양수/음수 제약). 변경 이력·롤백은 기존 설정 백엔드 그대로 적용
v0.8.3:- **런타임 관리자 설정 — 어드민 UI (v0.8.3)**: 어드민 대시보드에 **런타임 설정** 탭 추가 — ClickHouse·Text2SQL 설정을 카테고리별 표로 조회·편집(값 입력/불리언 select)·저장(즉시 반영), 출처(env/admin)·재연결 필요 배지 표시, 민감값은 마스킹 입력(빈 값이면 변경 안 함), 기본값 되돌리기·롤백·키별 변경 이력 보기, ClickHouse/실행 DB/Twin DB **연결 테스트 버튼** 내장. 운영자가 curl 없이 v0.8.0~0.8.2 설정 백엔드를 다룰 수 있음
v0.8.2:- **런타임 관리자 설정 — 커넥션 스왑·워커 재시작·연결 테스트 (v0.8.2)**: 설정 변경의 런타임 적용을 커넥션/워커까지 확장 — (1) Text2SQL **Exec/Twin DSN·드라이버 변경 시 캐시된 `sql.DB`를 close하고 다음 요청에서 새 대상으로 재오픈**(swap), (2) ClickHouse **sink worker를 관리형으로 전환**해 URL·interval 변경 시 안전하게 stop/start/restart(빈 URL·0 interval이면 중지), (3) 적용 전 **연결 테스트 API**(`POST /admin/settings/test/clickhouse`·`/text2sql-exec`·`/text2sql-twin`) — ClickHouse SELECT 1 ping + 테이블 존재 확인, 실행/트윈 DB open+SELECT 1, twin이 exec와 동일 DSN이면 경고. 본문에 후보값(url·dsn 등)을 주면 저장 전에 검증 가능
v0.8.1:- **런타임 관리자 설정 — Text2SQL/ClickHouse 즉시 반영 (v0.8.1)**: v0.8.0 설정 저장소를 **런타임 스냅샷**에 연결 — 서버는 부팅 시와 설정 변경 시마다 ``env 기본값 + DB 관리자 오버레이``를 atomic 스냅샷(`s.t2sConf()`/`s.chConf()`)으로 재구성하고, Text2SQL 핸들러·스케줄러·파이프라인과 ClickHouse sink가 이를 참조하도록 변경. 이제 모델·limit·mask·dialect·clarify·shadow·risk 등 Text2SQL 설정과 ClickHouse DB/table/계정/fact table 등은 **재배포 없이 다음 요청·다음 적재부터** 적용(민감값은 스냅샷 구성 시 복호화해 메모리에서만 사용). ※ Exec/Twin DSN 핫스왑과 sink worker interval·URL 변경 시 재시작은 v0.8.2
v0.8.0:- **런타임 관리자 설정 — 기반 (v0.8.0)**: 그동안 환경변수로만 지정하던 ClickHouse·Text2SQL 설정을 **관리자 화면에서 조회·변경·검증·이력·롤백**할 수 있는 기반 도입(`env 기본값 + DB 관리자 오버레이` 구조의 1단계) — 설정 레지스트리(키별 카테고리·타입·secret 여부·검증기), `admin_settings`/`admin_setting_history` 테이블, API(`GET /admin/settings`·`/{category}`, `PUT/DELETE /admin/settings/by-key/{key}`, `POST /admin/settings/validate`·`/rollback`, `GET /admin/settings/history`). **민감값(DSN·password)은 GATEWAY_SECRET으로 암호화 저장하고 응답에서 ``********`` 마스킹**, 변경 이력(이전/이후)과 롤백, default_limit<=max_limit 등 교차 검증 포함. ※ 본 단계는 저장·검증·이력 기반이며 런타임 즉시 반영(워커 재시작·DSN swap)은 후속 릴리스에서 연결
v0.7.5:- **엔터프라이즈 종합 기술 보고서 개정 및 공식 릴리즈 배포 (v0.7.5)**: 40여 종의 신규 시스템 환경변수(시맨틱 캐시, 셀프서비스 키, Text2SQL 파이프라인, Carbon ESG, SLA 보험, ClickHouse 싱크 등)를 표에 완벽히 추가 수록하고, 18대 서브시스템 아키텍처 수립 및 v0.7.5 최종 릴리즈 패키징 완료
v0.7.4:- **개인 메뉴 정리·콘텐츠 여백 (v0.7.4)**: 로그아웃 버튼을 개인 메뉴 내 ``내 키`` 아래로 이동(헤더 정리), 그리고 ``내 키``·``개인화``·``사용 한도`` 패널의 콘텐츠 영역에 들여쓰기 여백(`card-body` 패딩) 적용으로 가독성 향상
v0.7.3:- **개인 메뉴(auth-user 드롭다운) (v0.7.3)**: 상단 nav를 정리하고 **사용자 칩(`auth-user`) 클릭 시 드롭다운**으로 개인 메뉴 노출 — 개인화·내 키 이동, 자동 새로고침 주기·테마 전환·단축키 도움말을 한 곳에. 토큰 모드에서도 칩이 ``☰ 메뉴`` 트리거로 항상 표시되어 접근 가능, 외부 클릭/Esc로 닫힘
v0.7.2:- **만료·미사용 키 알림 (v0.7.2)**: API Key 위생 점검 — 활성 키 중 **만료 임박/만료됨/미사용/장기 유휴**를 탐지(`GET /admin/keys/health?stale_days=&expiring_days=`, 기본 30·7일). 키별 flags(expired/expiring_soon/never_used/stale_unused)·severity(high/medium)·유휴일수, request_logs로 마지막 사용 시점 산출, 위험 큰 순 정렬. My AI Home 대시보드(`/me/dashboard`)에 **본인 키 알림** 섹션 추가. read-only
v0.7.1:- **내 키 패널 UI (v0.7.1)**: 어드민 대시보드에 **``내 키``** 탭 추가 — 로그인 주체 본인의 API Key를 화면에서 발급/회전/폐기(`/me/keys` 연동). 발급/회전 시 평문 시크릿을 1회 배너로 표시(복사용), 스코프는 비우면 본인 권한 상속·입력 시 본인 범위 내로 제한. 셀프서비스 비활성(`SELF_SERVICE_KEYS_ENABLED=false`) 또는 사용자 식별 불가 시 안내 메시지
v0.7.0:- **셀프서비스 키 관리 (v0.7.0)**: 사용자가 **본인 소유 API Key를 직접 관리**하는 opt-in 경로(`SELF_SERVICE_KEYS_ENABLED`, 기본 off) — `GET/POST /me/keys`(본인 키 목록·발급), `POST /me/keys/{id}/rotate`(회전), `DELETE /me/keys/{id}`(폐기). 호출자는 JWT(sub) 또는 API Key(user_id)로 식별, 발급 키는 **본인 role·스코프 이내로만**(권한 상승 차단, 미지정 시 본인 스코프 상속), 타인 키는 404로 은닉. 평문 시크릿은 발급/회전 시 1회만 반환, 모든 동작 감사 로그 기록
v0.6.9:- **프로필 스냅샷 추세 (drift) (v0.6.9)**: Personal AI Profile 스냅샷(v0.6.5)을 시점 비교해 사용 패턴 변화를 드러냄 — 최근 2개 스냅샷 간 요청수·총비용·요청당 평균비용·성공률 델타와 대표 모델/주요 작업 전환을 계산하고 `cost_up`/`cost_down`/`success_down`/`model_shift`/`task_shift` 신호 부여. 프로필 상세 응답에 `drift` 포함 + 전용 조회(`GET /admin/personalization/profiles/{user_id}/drift`), 어드민 ``개인화`` 탭 상세에 추세 카드 표시. 스냅샷 2개 미만이면 baseline 없음
v0.6.8:- **추천 채택률 추적 (v0.6.8)**: My AI Home 추천(v0.6.6)에 대한 사용자 피드백을 기록·집계 — 사용자가 추천을 **채택/거절**(`POST /me/recommendations/feedback {id, action}`, 추천의 kind·ref에 키잉)하면 `recommendation_feedback`에 저장, 관리자는 종류별 채택/거절·distinct 채택자·채택률(adopted/(adopted+dismissed))을 조회(`GET /admin/recommendations/adoption?window=`). 추천이 실제로 행동으로 이어지는지 측정. 추천 레코드에 `ref`(추천 모델/템플릿 id) 추가
v0.6.7:- **개인화 어드민 UI (v0.6.7)**: Personal AI Profile(v0.6.5)을 어드민 대시보드에 **새 ``개인화`` 탭**으로 노출 — 사용자별 프로필 목록(요청·총비용·성공률·대표 모델·주요 작업·요약, 정렬 가능)과 상세 화면(팀/역할, 비용 성향, 선호 작업/모델/언어, 스냅샷 이력 + ``스냅샷 생성`` 버튼). 운영자가 curl 없이 사용자 사용 패턴을 검토하고 시점 스냅샷을 남길 수 있음
v0.6.6:- **My AI Home (v0.6.6)**: 관리자 대시보드와 별개로 **사용자 본인이 자신의 AI 사용 현황을 보는** self-service 화면(`GET /me/dashboard`, `GET /me/recommendations`) — 호출자를 JWT(sub) 또는 API Key(user_id)로 식별. 대시보드: 오늘 사용량·이번 달 비용/요청·자주 쓰는 모델·최근 실패 요청·절감 가능 금액(본인 최저비용-충분 모델로 환산)·추천 템플릿·최근 Prompt Product. 추천: 본인 사용 패턴 기반 모델 전환(절감액 포함)·표준 템플릿 채택 제안을 생성·`personal_recommendations`에 저장. 사용자가 스스로 비용·품질을 개선
v0.6.5:- **Personal AI Profile (v0.6.5)**: 사용자별 AI 사용 프로필을 로그에서 산출하는 read-only 기능(`GET /admin/personalization/profiles`, `GET /admin/personalization/profiles/{user_id}`) — api_key→user 매핑으로 팀·역할, 요청량·총비용·요청당 평균비용, 성공/오류율, 선호 task_type·모델·언어(Top5), distinct 모델/프롬프트지문, 한 줄 요약(예: ``주로 refactor, sql_analysis를 수행하며 gpt-4.1-mini 선호, 성공률 86%``)을 집계. 상세 조회 시 최신 프로필을 `personal_profiles`에 캐시하고 `?snapshot=1`이면 `personal_profile_snapshots`에 시점 스냅샷 기록. 이후 라우팅·템플릿 추천·Text2SQL 힌트·비용 코칭의 기준점
v0.6.4:- **관리자 감사 이상탐지 (v0.6.4)**: 관리자 감사 로그에서 의심 패턴을 탐지하는 read-only 분석(`/admin/audit/anomalies?window=&destructive=&volume=`) — admin별 **파괴적 액션 버스트**(delete/revoke/remove/hard, 기본 5건↑), **권한/스코프 변경**(scope/role/grant/permission/escalat), **업무외 시간 활동**(22:00–06:00 UTC), **고볼륨**(기본 100건↑)을 집계해 flags + severity(high/medium) 부여, 위험 큰 순 정렬. sqlite/pg 공통(시간 분석은 앱에서 수행), 강제 없음
v0.6.3:- **비용센터 청구서 (v0.6.3)**: cost_center별 AI 사용료 **chargeback 청구서** 생성(`/admin/invoices?cost_center=&window=&format=`) — 지정 시 해당 센터의 모델별 라인아이템(요청·토큰·비용)+합계를 JSON 또는 **markdown 청구서**(`format=markdown`, 티켓·위키·Mattermost 붙여넣기용)로 반환, 미지정 시 전체 cost_center 요약 목록. 기존 비용 배부 데이터 재사용, read-only
v0.6.2:- **모델 마이그레이션 어드바이저 (v0.6.2)**: 프롬프트 지문 클러스터별로 **지배 모델 → 더 싸고 충분히 좋은 모델** 전환을 추천하는 read-only 어드바이저(`/admin/model-migration?window=&min_requests=`) — 동일 프롬프트 형태에서 관측된 모델별 평균비용·성공률을 비교해, 최고 성공률 대비 5pp 이내이면서 더 싼 모델을 후보로 제시하고 ``클러스터 요청수×(현재−추천 평균비용)``으로 절감액 추정. 최소 요청수(기본 20) 미만 클러스터·단일 모델 클러스터는 제외, 절감액 큰 순 정렬
v0.6.1:- **에러버짓 번레이트 (v0.6.1)**: SLA claim 정의(4xx/5xx/failover/error) 위에 **멀티윈도우 에러버짓 소진율** 산출(`/admin/insurance/burn-rate?dimension=&window=&short=&sla=`) — scope별 short(기본 1h)·long(기본 24h) 윈도우의 claim_rate를 허용치(`1-SLA`)로 나눈 burn rate, Google SRE 패턴대로 두 윈도우 모두 ``fast``(기본 14.4×, page) / long만 ``slow``(기본 3×, ticket) 분류 + 30일 버짓 기준 ``소진까지 일수``(=30/long burn) 투영. 임계값 `INSURANCE_FAST_BURN`/`INSURANCE_SLOW_BURN` 또는 `?fast=&slow=` 오버라이드. 위험 큰 순 정렬
v0.6.0:- **절감 리포트 (Savings Report) (v0.6.0)**: 게이트웨이가 만들어낸 비용 절감을 scope별로 정량화하는 read-only 리포트(`/admin/savings?dimension=&window=`) — **라우팅 다운시프트 절감**(요청 모델보다 싼 모델로 서빙된 요청을 ``요청 모델 가격으로 환산한 baseline − 실제 비용``으로 정확 산출)과 **캐시 절감**(route_reason=cache 적중 × 동일 scope 비캐시 평균 비용으로 추정)을 합산. scope별 다운시프트 요청수·절감액·캐시 적중·캐시 절감(추정)·총 절감액 + 전체 합계. 게이트웨이 ROI를 숫자로 증명
v0.5.9:- **AI 요청 보험 모드 (SLA claims) (v0.5.9)**: 보장 범위(scope)별 요청을 ``covered``로, 저하된 결과(4xx/5xx/failover/error)를 ``claim``으로 보고 SLA 목표 대비 위반을 드러내는 read-only 원장(`/admin/insurance/claims?dimension=&window=&sla=`) — scope별 covered·claims(+5xx/4xx/failover 진단 분해)·claim_rate·신뢰도, SLA 허용치(`(1-목표)*covered`) 대비 초과 claim(excess=위반 규모)와 sla_met 산출, 위반 큰 순 정렬. 목표는 `INSURANCE_SLA_TARGET`(기본 0.99) 또는 `?sla=` 오버라이드. 강제 없음
v0.5.8:- **Prompt to Product (v0.5.8)**: 반복되는 chat 프롬프트(프롬프트 지문 클러스터)를 **재사용 가능한 명명된 템플릿(제품)** 으로 승격하는 전체 루프 — 후보 발굴(`GET /admin/prompt-products/candidates` : 기존 프롬프트 지문 분석 재사용, 빈도·비용·대표/최저비용 모델·샘플 + 이미 제품화 여부 표시), 승격(`POST /admin/prompt-products` : 정식 템플릿 생성 + 출처 지문·승격 시점 reach(요청수·distinct 사용자) 스냅샷 기록), 목록(채택도=템플릿 use_count·last_used 조인), 삭제. 원천 프롬프트 로그가 없어도(LOG_RAW_PROMPTS off) 관리자가 정식 템플릿 본문을 작성해 자산화. 새 의존성 없음
v0.5.7:- **Golden Workflow (v0.5.7)**: 여러 골든 스텝을 **순서가 있는 하나의 회귀 단위**로 묶어 실행하는 1급 엔터티(`/admin/golden-workflows` CRUD, `/admin/golden-workflows/run`) — 단발 Golden Prompt나 임의 태그 그룹과 달리 워크플로우는 이름·설명·정렬된 스텝(각 prompt+expected)을 갖고, 실행 시 스텝별 통과/점수와 전체 pass_rate를 반환. `?fail_on_regression=1`+`min_pass_rate`로 CI 게이트 사용(미달 시 422). 스텝 간 출력 체이닝은 의도적으로 제외(단순 회귀 스위트). 새 의존성 없음
v0.5.6:- **AI 업무지도 (Work Map) (v0.5.6)**: ``어떤 AI 업무가 어디서 누구에 의해 일어나는가``를 한 화면에 모으는 read-only 통합 뷰(`/admin/work-map?dimension=&window=`) — work 차원(기본 project, repo·team·service 등)별로 요청량·토큰·비용·distinct 사용자/모델·오류율·대표 모델과 **task_type(generate/refactor/debug 등) 분포**를 노드 단위로 집계. 단일 지표인 비용 배부, 신뢰도 중심의 신용점수와 달리 ``업무의 성격``까지 한눈에. 새 의존성·계약 변경 없음
v0.5.5:- **Prompt Carbon Score (v0.5.5)**: 이미 적재된 토큰 사용량으로 요청의 **전력(Wh)·운영 탄소(gCO2e)** 를 추정하는 read-only 분석(`/admin/carbon-score?dimension=&window=`) — subject(model·project·api_key 등)별 토큰량에 모델별 에너지 계수를 적용해 에너지/배출/요청당 Wh 산출. 계수는 전부 설정값(`CARBON_WH_PER_1K_TOKENS` 기본 0.4Wh/1K, `CARBON_MODEL_WH_PER_1K`=model별 오버라이드, `CARBON_PUE` 기본 1.2, `CARBON_GRID_INTENSITY_G` 기본 475 gCO2e/kWh)이며, 공개 추정치 편차가 커 **절대값이 아닌 주체 간 상대 비교 신호**로 사용. 강제 없음
v0.5.4:- **SQL Digital Twin (v0.5.4)**: Golden Query **결과 동등성 검증**을 운영 DB 대신 마스킹·샘플 **트윈 DB**에서 실행하는 opt-in 경로 추가 — `TEXT2SQL_TWIN_DSN`(+`TEXT2SQL_TWIN_DRIVER`, 기본 postgres) 설정 시 기대/생성 SQL 비교를 트윈에서 수행해 프로덕션 데이터 노출 없이 검증 가능, 미설정 시 기존 실행 DB로 그대로 폴백(동작 변화 없음). 트윈 커넥션은 지연 오픈·캐시(read-only 검증 전용)
v0.5.3:- **사내 AI 신용점수 (v0.5.3)**: subject(API Key·project·model 등)별 **신용점수**(`/admin/ai-credit-score?dimension=&window=`) — 신뢰도(성공률 0.7)+비용효율(상대 cost/req 0.3) 블렌드 0~100, 표본 5건 미만은 low-confidence 표시. 안전/비용 효율이 좋은 주체를 한눈에(read-only, 강제 없음)
v0.5.2:- **데이터 상품 추천 (v0.5.2)**: 반복 Text2SQL 질문(report candidate)에 SQL 형태 기반 **추천 산출물 분류** 추가 — 집계 쿼리→`dashboard`, 다중 JOIN→`data_mart`, 단건/목록 조회→`api`. 어드민 인사이트 표에 노출되어 반복 질문을 어떤 데이터 상품으로 만들지 판단 보조
v0.5.1:- **시맨틱(임베딩) chat 캐시 (v0.5.1)**: 정확 캐시(temp 0/seed) miss 시 프롬프트를 임베딩해 코사인 유사도로 근사 응답을 재사용하는 opt-in 캐시 — `CACHE_CHAT_SEMANTIC_ENABLED`+`CACHE_CHAT_SEMANTIC_MODEL`(임베딩 모델) 필요, `CACHE_CHAT_SEMANTIC_THRESHOLD`(기본 0.95)·`CACHE_CHAT_SEMANTIC_MAX_CANDIDATES`(기본 200). 임베딩 호출 실패 시 정상 업스트림으로 안전 폴백, 만료 엔트리는 retention 워커가 정리. 적중 시 `X-Cache-Type: chat-semantic
v0.5.0:- **Text2SQL 어드민 UI 통합 (v0.5.0)**: 그동안 API 전용이던 기능들을 어드민 Text2SQL 탭에 노출 — 저장 리포트(스케줄·MM 전달 상태), 인사이트 마이너(반복 질문 → **원클릭 리포트 승격** 버튼, 업무 용어 후보), 행동 이상 탐지(usage smell·팀 누적 위험·intent drift). 운영자가 코드/curl 없이 컨트롤 플레인을 다룰 수 있음
v0.4.9:- **Text2SQL 관측 메트릭 (v0.4.9)**: Prometheus(`/metrics`)에 Text2SQL 운영 카운터 추가 — `proxy_text2sql_requests_total`, `_cache_hits_total`, `_risk_blocked_total`, `_challenge_veto_total`, `_shadow_evals_total`. 그동안 추가한 기능들(캐시·위험 차단·self-challenge·shadow)의 실제 발동량을 모니터링 가능
v0.4.8:- **저장 리포트 스케줄 실행 (v0.4.8)**: 승격된 saved report에 스케줄(`schedule_interval` 예 24h)·on/off·Mattermost 전달 설정(`POST /admin/text2sql/reports {id,interval,enabled,deliver_mattermost}`) 추가. 백그라운드 스케줄러가 도래한 리포트를 read-only로 실행하고 결과 요약을 Mattermost(`text2sql_report` 이벤트)로 전달 — 반복 질문 자산화의 마지막 단계 완성. 실행 DB(`TEXT2SQL_EXEC_DSN`) 미설정 시 자동 비활성
v0.4.7:- **정책 GitOps export/import (v0.4.7)**: 거버넌스 정책+룰을 portable JSON으로 내보내기(`GET /admin/policies/export`)·가져오기(`POST /admin/policies/import`). `?dry_run=1`이면 생성/수정 플랜만 반환(쓰기 없음)하여 repo 커밋·PR 리뷰·diff·롤백 기반 GitOps 운영 가능
v0.4.6:- **ClickHouse 상세 Text2SQL fact 적재 (v0.4.6)**: 일별 rollup에 더해 **질의 단위 fact 테이블** 적재(`CLICKHOUSE_TEXT2SQL_FACT_TABLE` 설정 시) — 질문 원문/SQL은 제외(마스킹, question_chars만)하고 team·모델·모드·스키마버전·valid·executed·row_count·EXPLAIN위험·비용·지연·실패분류를 행 단위로 전송. watermark 증분(`text2sql_fact` dimension) + 자동 sink 루프 포함 + 수동 `/admin/dw/text2sql-fact`. 첫 실행은 최근 7일 백필
v0.4.5:- **Text2SQL 응답 품질 강화 (v0.4.5)**: 검증 통과 응답에 **감사 근거 푸터**(스키마·버전·permission/glossary 지문·EXPLAIN 위험·마스킹 컬럼) 자동 첨부, 검증 거부 시 **수정 방법 안내**(사유 기반 친화 가이드) 첨부, 실행 결과 0행이면 **결과 없음 복구 제안**(기간·조건·필터 완화) 첨부
v0.4.4:- **Text2SQL 질문 자산화 + 위험 단계화 (v0.4.4)**: 반복 질문 원클릭 **승격 API**(`/admin/text2sql/promote` — target=report/golden/glossary; 저장 리포트는 `text2sql_saved_reports` + `/admin/text2sql/reports` 목록/삭제)로 Prompt DNA·리포트 후보를 리포트·골든쿼리·업무 용어 자산으로 전환. 누적 위험 enforce를 **단계화**(감지 < `TEXT2SQL_DAILY_RISK_WARN` ≤ 경고 < `TEXT2SQL_DAILY_RISK_LIMIT` ≤ 차단) — 경고 구간은 응답에 주의 문구만 첨부하고 정상 처리, 차단 구간만 차단
v0.4.3:- **Text2SQL Prompt DNA + 누적 위험 enforce 토글 (v0.4.3)**: 질문 지문 분석 **Prompt DNA**(`/admin/text2sql/prompt-dna` — 지문별 빈도·distinct 사용자·평균비용·거부율 집계 + repeated/high_cost/risky 라벨, read-only). 탐지→차단 강제를 위한 **누적 위험 한도 enforce** 토글(`cumulative_risk_enforce` + `TEXT2SQL_DAILY_RISK_LIMIT`, 기본 OFF) — API Key의 당일 위험 요청(거부·고위험 EXPLAIN·실패)이 한도를 넘으면 SQL 생성 전 차단
v0.4.2:- **Text2SQL 기능 토글 + Self Challenge·Gateway Memory (v0.4.2)**: 관리자에서 런타임 온오프 가능한 기능 토글 프레임워크(`/admin/text2sql/features`, 어드민 UI 스위치, kill-switch 동일 패널) 추가. 토글로 켜는 신규 기능 — **Self Challenge Proxy**(생성 SQL을 보조 모델이 검토, preview는 의견 첨부·execute는 unsafe 판정 시 실행 보류, 기본 OFF·켜면 요청당 추가 호출), **Gateway Memory**(사용자가 자주 쓰는 테이블을 프롬프트 힌트로 보강, 기본 OFF)
v0.4.1:- **Text2SQL 행동 이상 탐지 (탐지 전용) (v0.4.1)**: 차단 없이 운영자 가시성만 제공하는 read-only 탐지(`/admin/text2sql/anomalies`) — **AI Smell Detector**(반복 질문·권한 우회성 거부 반복·스키마 전체 요청), **누적 위험 노출**(팀별 거부·고위험 EXPLAIN·권한 탐침 가중 집계), **Intent Drift Detector**(api_key별 조회→위험/광범위 키워드 이동 감지). 모든 신호는 경고만 하며 요청을 차단하지 않음
v0.4.0:- **Text2SQL 인사이트 마이너 (v0.4.0)**: 질문 로그에서 운영 인사이트를 자동 추출하는 read-only 마이너(`/admin/text2sql/miners`) — **Report Candidate Miner**(반복되는 질문을 빈도 집계해 대시보드·배치 리포트 후보로 추천)와 **Glossary Miner**(빈출 질문 토큰 중 스톱워드·기존 정의어를 제외하고 업무 용어 사전 후보 추출). 프록시 호출 비용·사용자 계약 변경 없음
v0.3.9:- **Text2SQL 답변 근거 표시·운영 Kill Switch (v0.3.9)**: 응답에 **답변 근거** 섹션 추가(사용한 테이블 + WHERE 적용 조건을 비-SQL 사용자도 검토 가능), 런타임 **Kill Switch**(`/admin/text2sql/kill-switch` — 장애·비용·보안 사고 시 재배포 없이 Text2SQL 즉시 중지, 가상 모델은 안전 메시지 반환)
v0.3.8:- **Text2SQL 민감도 API 보완·DW 점검 강화·비용 분리 (v0.3.8)**: 컬럼 민감도 API가 `aggregate_only`·`approval_required`까지 허용(기존 normal/mask/exclude만 → UI 드롭다운과 불일치 수정), Golden Query 결과 동등성 검증을 실행 DB 있을 때 **기본 적용**(`?execute=0`로 해제), ClickHouse 정합성 검증을 **dimension별**(all·model·provider·project·cost_center) 확대, 대상 테이블 **엔진/정렬키 점검 API**(`/admin/dw/table-info` — ReplacingMergeTree·dedupe 키 확인), Text2SQL 비용을 **generation/summary/shadow로 분리** 기록, Replay Bundle **보존 정책(retention GC)·secret 마스킹** 추가
v0.3.7:- **Text2SQL 운영 가시성·재현성 확장 (v0.3.7)**: 업무 용어 사전 **충돌 탐지**(동일 용어 복수 매핑·전역/스키마 shadowing을 관리자 화면에서 경고), 위험 요청 큐 **자동 개선 제안**(기간 조건 추가·집계 변경·민감 컬럼 제외·LIMIT 축소 등 수정안 제시), **스키마 변경 영향도 리포트**(`/admin/text2sql/schema-impact` — 버전 변경 시 영향받는 golden·cache·glossary·permission 집계), **Replay Bundle**(opt-in `TEXT2SQL_REPLAY_BUNDLES` — 당시 프롬프트·스키마 컨텍스트·용어사전·권한 스냅샷을 저장해 `/admin/text2sql/replay`로 사후 재현)
v0.3.6:- **Text2SQL shadow 정책 일치·검증기 집계 정확도 (v0.3.6)**: shadow 모델 평가가 live와 **동일한 검증 정책**(테이블 allowlist·blocked·aggregate-only)을 적용해 모델 품질 지표를 운영과 일치시킴, 검증기 aggregate-only 판정을 **중첩 괄호 균형 기반**으로 고도화(`sum(coalesce(col,0))` 등 중첩 집계 내 컬럼을 정상 허용, window `OVER(PARTITION BY col)`의 원시 참조는 차단 유지)
v0.3.5:- **Text2SQL 검증 정확도·결과 보호·모델 품질 평가 강화 (v0.3.5)**: 컬럼 민감도(`mask`) 기반 **컬럼 단위 결과 마스킹**(선언된 컬럼만 마스킹, 미선언 시 기존 전체 마스킹), 검증기 in-tree **구조 검증**(괄호·따옴표 균형 → 생성 도중 잘린 SQL 차단), 실행 DB **read-only 헬스체크**(`/admin/text2sql/healthcheck` — 연결·read-only 트랜잭션·statement_timeout·계정 쓰기 제한 점검), Golden Query **결과 동등성 평가**(`/admin/text2sql/golden/run?execute=1` — 기대/생성 SQL을 read-only 실행해 행 집합 비교, 토큰 일치를 넘는 의미 검증), **shadow 모델 평가**(`TEXT2SQL_SHADOW_MODELS`·`TEXT2SQL_SHADOW_SAMPLE_RATE` — preview 샘플에서 후보 모델로 비동기 재생성해 모델별 품질 메트릭만 적립, 라이브 KPI 비오염)
v0.3.4:- **Text2SQL 재현성·권한 안전성·DW 적재 신뢰성 강화 (v0.3.4)**: Text2SQL 감사 로그에 재현성 필드(schema_name·schema_version·permission_hash·glossary_hash) 추가 — 사후에 ``왜 그 SQL이 생성됐는지`` 당시 스키마·권한·용어사전 상태로 재현 가능. preview 캐시 키에 권한 효과·용어사전 해시를 포함해 **권한이 다른 사용자 간 SQL 재사용 방지**(cross-subject reuse 차단). ClickHouse Sink에 dimension별 watermark(`clickhouse_sink_state`)와 실패 재처리 큐(`clickhouse_sink_retry`) 도입 — 자동 적재 실패가 유실되지 않고 다음 주기/수동 재처리(`/admin/dw/sink-retry`)로 복구, 진행 상태는 `/admin/dw/sink-status` 로 조회
v0.3.3:- **Text2SQL 백로그 완결 — 캐시·버전·민감도·샌드박스·재질문·품질 라우팅·위험 큐·용어사전 (v0.3.3)**: preview 결과 캐시(`text2sql_cache`, 질문·스키마·모드·스키마버전 키 + TTL, 스키마 변경 시 자동 무효화), 스키마 버전 관리(version/collected_at/source_fingerprint — 지문 변경 시 자동 증가), 민감도 정책 세분화(`approval_required` 차단 / `aggregate_only` 집계함수 내에서만 허용), 실행 샌드박스 강화(읽기 전용 트랜잭션 + `SET LOCAL statement_timeout`·`work_mem`), 자연어 재질문(clarification) 모드(모호·기간 누락 질문 게이트), 비용·품질 기반 업스트림 자동 선택(기본 모델 유효율 저하 시 정확 모델로 승격), 관리자 위험 요청 큐(`/admin/text2sql/risk-queue` — 거부·고위험 EXPLAIN·실패 분류), 업무 용어 사전(`text2sql_business_terms` — 자연어→SQL 매핑 프롬프트 주입) 추가
v0.3.2:- **Text2SQL 권한·통제·DW 운영화 (v0.3.2)**: 권한 매트릭스(`text2sql_permissions` — 팀·API Key·사용자별 schema/table/column allow·deny), 검증기 식별자 추출 강화(quoted/schema-qualified), EXPLAIN 위험도 로그 저장(cost·risk_score), 실패 원인 표준 분류(syntax/permission/cost/timeout/unknown_column/empty), ClickHouse 자동 적재 스케줄러 + 정합성 검증(`/admin/dw/consistency`) 추가
v0.3.1:- **Text2SQL 정확도·운영 통제 심화 (v0.3.1)**: 스키마 레지스트리 DB화(테이블/컬럼 + 업무 설명 + 민감도 normal/mask/exclude)와 팀·컬럼 단위 권한 필터, information_schema 자동 수집, 검증기 강화(문자열/주석 리터럴 스크럽), EXPLAIN 위험 점수화(seq scan·nested loop), 현업 친화 응답 포맷(해석/SQL/주의사항/실행여부/다음질문), Text2SQL 품질 평가 emit, Golden Query 자동 후보화, ClickHouse rollup sink(HTTP) 추가
v0.3.0:- **Text2SQL 게이트웨이 및 로드맵 Part 2 완료 (v0.3.0)**: 사용자 계약 변경 없이(`/v1/chat/completions` 유지) `vibe/text2sql-*` 가상 모델을 통해 자연어→읽기전용 SQL을 생성하는 Text2SQL 파이프라인 신규 탑재 — 내부에서 실제 업스트림 모델 선택·SQL 검증(SELECT 전용·자동 LIMIT·테이블 권한)·EXPLAIN 비용 가드·결과 PII 마스킹·few-shot 골든쿼리·모델별 SQL 품질 메트릭·DB 스키마 카탈로그/런타임 프로필. 더불어 정책 시뮬레이터(차단 예상 미리보기), 모델 가격표 버전 이력+자동 시드, Mattermost 알림 연계, 팀별 예산 소진 예측, 프롬프트 템플릿 마켓 추가
v0.2.0:- **로드맵 Part 1 완료 및 엔터프라이즈 거버넌스 대대적 확장 (v0.2.0)**: Explicit PipelineStep 리팩토링, Ops Health 대시보드 및 주간 AI 리포트 생성, 다차원 비용 할당(Repo/Branch/Project/Service/Cost-Center), CI용 골든 프롬프트 회귀 테스트 게이트, 프롬프트 인젝션 탐지 및 MCP 도구 위험등급 분류, 프로바이더 SLO 모니터링, 월말 비용 예측 및 Operational Risk Score 산출, 폐루프 자동 라우팅 강화 학습, DW 롤업 스태이징 탑재
v0.1.26~v0.1.27:- **영속적 세션 추론 및 위임 거버넌스 (v0.1.26~v0.1.27)**: 재기동에 안전한 세션 영속 추론 테이블 `inferred_sessions` 추가 및 자동 GC 탑재, team_admin 소속팀 API Key 한정 필터링 조회, 권한 상승(Privilege Escalation) 방지를 위한 계층형 롤/스코프 위임 거버넌스 구현 및 종합 기술 보고서 갱신
v0.1.25:- **사용자 팀 매핑 및 API Key 영구 폐기 관리 (v0.1.25)**: 관리자 목록 내 소속 팀 정보 연동 및 패치를 통한 소속 팀 수정·탈퇴 지원, API Key의 개별 세부 권한을 어드민 UI에서 팝업으로 조절 가능한 스코프 편집기 연동, super_admin 한정 키 영구 삭제(?hard=1) 정책 도입
v0.1.24:- **지능형 라우팅 및 기본 권한 보강 (v0.1.24)**: 클라이언트 고정(Pinned) 환경에서의 auto/auto-reasoning 모델명 rewrite 지원, PROXY_API_KEYS 및 신규 API Key 생성 시 scopes 미지정인 경우 기본 역할별 권한 자동 상속, /v1/models 익명 조회 허용
v0.1.23:- **API Key 호환성 해결 (v0.1.23)**: AUTH_ENABLED 활성화 환경에서 기존에 스코프가 부여되지 않은 채 생성되었던 API Key들의 차단 현상(scope_denied)을 해결하고, 자동 스코프 채움 마이그레이션 적용
v0.1.22:- **자동화 성능 벤치마킹 모듈 탑재 (v0.1.22)**: TTFT, TPS, 가속지연 등의 메트릭을 자동 측정하고 관리자 페이지에 실시간 반영 및 연동
v0.1.21:- **종합 환경변수 스펙 완비 (v0.1.21)**: 부트스트랩 계정 정보(AUTH_ADMIN_BOOTSTRAP_*) 및 DB 폴백 DSN 등 누락되었던 42종의 시스템 설정 변수 표준화 가이드 완료
v0.1.19~v0.1.20:- **지능형 라우팅 및 다중 인증 체계 (v0.1.19~v0.1.20)**: 서버 상태 기반 Intelligent Routing, 다중 JWT 인증, 거버넌스 룰 엔진 탑재
v0.1.17~v0.1.18:- **VCS(Git) 비동기 역추론 엔진 완성 (v0.1.17~v0.1.18)**: 오프라인 제약 속 LLM 대화 트래픽에서 Git 커밋/푸시 이벤트를 추적하는 타임라인 맵 구성
v0.1.13~v0.1.14:- **3계층 세션 및 RAG 지식 베이스 연동 (v0.1.13~v0.1.14)**: Work-Task-Request 3단계 세션 추론 모델링 및 RAG용 KB/MCP 업스트림 기능 제공
v0.1.9~v0.1.12:- **XView 분산 산점도 및 이상 비용 탐지 (v0.1.9~v0.1.12)**: 대화 맥락 복잡도, 실시간 트랜잭션 APM 가시성 극대화 및 지연 요인 설명(Explain) 모달 제공
v0.1.5~v0.1.8:- **데이터베이스 엔터프라이즈 마이그레이션 (v0.1.5~v0.1.8)**: SQLite에서 PostgreSQL로의 무결성 마이그레이션, 42803 에러 수정 및 자동 타입 변환 패치
