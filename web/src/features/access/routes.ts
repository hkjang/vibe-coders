import { defineFeatureModules } from "@/features/feature-module";

// Screens owned by the access domain. Register each implemented feature here.
export const accessFeatureModules = defineFeatureModules([]);
