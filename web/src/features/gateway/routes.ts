import { defineFeatureModules } from "@/features/feature-module";

// Screens owned by the gateway domain. Register each implemented feature here.
export const gatewayFeatureModules = defineFeatureModules([]);
